---
name: config-d-pretraining
description: "Pretraining-axis decision history — shipped Config D (ImageNet) vs governance Config D (SSL); AOQ-1 resolved in v6.0.0"
metadata:
  type: project
---

**RESOLVED 2026-06-01 (governance v6.0.0).** The V5-arm pretraining axis is settled: **RETFound is dropped** in favour of **ophthalmology-specific self-supervised pretraining** (CNN-compatible domain-adaptive SSL — DINO/BYOL/SimCLR/MoCo family, kept open, selected empirically) of the *existing* CNN backbones (ResNet-50 / EfficientNet-B3). This resolves **AOQ-1 → option (b)**, **AOQ-4** (2×2 *(preprocessing × architecture)* symmetry restored: configs **B and D reinstated**, **B′ retired**), **AOQ-3** (RETFound license moot); **AOQ-2** simplified (SSL pretrains on the 4-channel V5 tensor). The composite *(preprocessing × pretraining)* IV and **CFC-2.8 are retained** (baseline⟹ImageNet, V5⟹SSL). MAJOR bump per VERSIONING_POLICY §4. Committed `c1fa0b0` (governance) + `55576d1` (dependent sync), tag `v6.0.0`, pushed to origin/main. New supporting contribution **SC-H** records the SSL init (CFC-2.8-bounded).

**Config-D naming divergence — RESOLVED for the demo 2026-07-19.** It *was* live: the shipped demo "Config D" was the retired **v5.0 artifact = full pipeline + EfficientNet-B3 + ImageNet** (the `dreamer07/eyepacs` Kaggle run, fold 0 — see [[config-d-kaggle-source]]), while governance Config D is **full pipeline + EfficientNet-B3 + ophthalmology-SSL**. The demo now ships the real Exp-1 Config D fold-0 checkpoint (`init.source=ssl`, continual MoCo-v2 ep50), so shipped and governance Config D finally agree — see [[demo-stack]]. Elsewhere the caution still applies: when an older doc or artifact says "Config D", check whether it means the retired ImageNet/Kaggle stand-in or the governance SSL design. The Stage-7 dataset-specific input normalization was never the open issue; the pretraining/backbone axis was, and is now closed.

**SSL corpus locked — 2026-06-26.** The candidate's fundus-SSL is **from-scratch (random init), CNN-backbone-matched**, on the **EyePACS original "test" split = 53,576 images** (`EyePACS/test/*.jpeg`, labels now present in `testLabels15.csv` but **unused** — SSL is label-free). This corpus is **disjoint** from Exp 1's evaluation set (the 35,126 `trainLabels.csv` / `train/` split, 5-fold CV), verified in `experiments/src/data/datasets.py:217-248` (loader reads only train split) → **no pretraining leakage**, analogous to ImageNet being a separate corpus. **Do NOT fold the 53k into Exp 1 supervised training** (Exp 1 dataset ~35,126 is governance-fixed; the 53k is pretraining-only). `testLabels15.csv` labels (with `Usage`=Public/Private) are reserved for an optional **linear-probe gate** validating the init before Exp 1 integration. SSL design (for the brief): MAE **excluded** (ViT-only; backbones are CNN) → candidates BYOL/MoCo-v2/SimSiam/DINO; 4-channel input (RGB+FOV mask), photometric augs on RGB only; pretrain 256² (downstream 512²). Contrast stays clean: ImageNet (baseline) vs fundus-only-SSL (pipeline). Build-spec brief to be written in `experiments/docs/` (impl by ChatGPT per role split).

**Thesis synced — 2026-06-26 (governance bump 6.1.0 → 6.2.0, MINOR).** Added clause **SB-2.4** (SSL corpus = unlabeled EyePACS "test" 53,576, disjoint from ~35,126 Exp-1 set by image+patient id → INV-SSL-1/2, pretraining-only) and the **linear-probe acceptance gate** to INVARIANTS/DGL-6; fixed a lingering v6.0.0 miss where HYPOTHESIS Premise 4 + Conclusion still named RETFound (rewritten to ophthalmology-SSL); refined SC-H; updated RESEARCH_ARCHITECTURE §4.2bis/§9.1, VERSION_SYNC, CHANGELOG; §3.3.2 draft+KZ+continuity; glossary EN/KZ +2 terms. No SSL results asserted (arm not trained). **Pre-existing non-SSL gaps still OPEN:** SC-F says fallback σ=13.0° but INVARIANTS v6.1.0 set 15.0°; CORE_OBJECTIVE.md still v5.0 (no SSL text); thesis/methods/implementation.md pending downstream sync.

**Genuine remaining work (net-new):** new SSL-family literature cards already exist (#84–#92); the experiments/ SSL **pretraining implementation** is DONE (`experiments/src/ssl/` + scripts + tests, BYOL primary, leakage assert verified live), pending actual GPU pretraining runs + the linear-probe gate before Configs B/D can run (they now fail-fast until a gated checkpoint exists). Code impl was done by an Opus subagent at the candidate's explicit request (overriding the usual ChatGPT-implements role split).

**Why:** the candidate chose a CNN-native in-domain initialization so architecture is not confounded with preprocessing — a defensible causal interpretation of the H-1 contrast, unlike RETFound (ViT-L) which changes the backbone.

**How to apply:** Treat v6.0.0 as authoritative; don't reintroduce RETFound as the V5-arm source. When touching demo/experiments Config D, check whether the context means the *shipped ImageNet artifact* or the *governance SSL design*.
