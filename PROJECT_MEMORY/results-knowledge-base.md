---
name: results-knowledge-base
description: "results/ — переносимая база знаний по экспериментам (реальные метрики, вердикты, таблицы, выводы) — точка входа для сборки материалов и написания гл.4/5/7"
metadata:
  type: project
---

Создана `E:\dissertation-project\results\` (2026-07-24) как **переносимая база знаний по
экспериментам** — чтобы новый чат продолжал сборку результатов и написание экспериментальных глав,
прочитав только эту папку, без повторной разведки репозитория. Точка входа: `results/README.md`.

Состав: `STATUS.md` (статус exp1–7+SSL + реальные метрики), `INTEGRITY_NOTE.md` (⚠️ демо/защита
противоречат реальным данным), `CHAPTER_STATUS.md`, `TOOLING.md`, `hypotheses/` (карточки-вердикты),
`findings/` (словесные выводы = заготовка прозы), `tables/` (TAB-4.2/4.6/4.8/4.9/4.10 из реальных
чисел), `data/` (копии канонических JSON + MANIFEST).

**Ключевые факты (источник истины `experiments/outputs/`):**
- Завершены ВСЕ 7 экспериментов + SSL (exp1–exp7) + категория B. Все прогоны прошли аудит на полноту
  (конфиги × фолды × эпохи) 2026-07-27: единственная дырка — exp2 `full`/fold2 — добита 2026-07-28 (см. ниже).
  Остаётся заявленным ограничением: σ-свип flat-field (exp2 Part C) НЕ реализован в коде; exp3/5/6 считаны
  с чекпойнтов только fold0 (нет межфолдовой дисперсии); exp1 `C`/fold3 остановлен на ep13 (best=ep13, не по правилу).
- **H-1/H-4/H-6/H-7 = `*_supported=false`; H-5 = `true` (единственная, но слабо); H-2 частично (CLAHE ✓).**
- **exp2 (H-2, 15%/3-fold):** стадии препроцессинга по отдельности НЕ улучшают wF1 (кроме canonical flip +0.91пп); CLAHE-свип — профиль с оптимумом ✓; σ-свип не прогнан. Подтверждает CFC-2.8 (эффект exp1 = композит с SSL-init, не препроцессинг) — но опирать надо на «препроцессинг не даёт прироста», НЕ на «вредит».
- **⚠️ exp2 ПЕРЕСЧИТАН 2026-07-28 — числа `full` в `results/` УСТАРЕЛИ.** Уровень `full`/fold2 был оборван на 2 эпохах из 20 (прогон убит 2026-07-24), и его best-of-2 F1=0.6819 усреднялся в заголовочное число. Фолд добит до правила останова (best ep13, ранняя остановка ep18, 19ч42м): **`full` = 0.7463 ± 0.0111, Δ = −1.47пп (было 0.7197 ± 0.0268, −4.14пп)**; фолды 0.7361/0.7410/0.7618. Следствия: (1) `full` больше НЕ худший уровень — худший `+augmentation` (−1.75пп); (2) σ упала втрое → −1.47пп при σ≈0.011–0.016 = **в пределах шума, «full не отличим от baseline»**, а не «заметно хуже»; (3) вывод H-2 по существу устоял. Канонический `outputs/exp2/ablation_summary.json` пересобран (все 6 уровней). **НЕ обновлены под новые числа:** `results/STATUS.md`, `results/tables/TAB-4.4_exp2_ablation.md`, `results/findings/exp2.md`, `results/hypotheses/H-2.md`.
- **Инструменты добивки (exp2):** `run_exp2_partA.py --fold-list N` добивает отдельный фолд (не трогая `ablation_summary.json` — пишет `ablation_partial_foldsN.json`); `scripts/rebuild_exp2_summary.py` пересобирает канонический summary из `metrics.csv` (последняя строка на эпоху побеждает → добитые фолды перебивают оборванные) + `--recompute-quality`. Оговорка для текста: fold2 уровня `full` обучен отдельным запуском, поэтому его seed-состояние отличается от фолдов 0/1 (разбиение то же, инициализация — нет).
- **⚠️ exp4 (H-5) ПЕРЕСЧИТАН 2026-07-28 — вердикт ПЕРЕВЕРНУЛСЯ на `h5_alo_supported=false`.** Прежний прогон
  сэмплировал 50 изображений из IDRiD «B. Disease Grading», а маски лежат в «A. Segmentation» → пересечение
  n=5. Пересчёт по всем **54** изображениям с масками (`scripts/rerun_exp4_gradcam.py`, eval-only с тех же
  чекпойнтов, канон `outputs/exp4/iou_results_maskset.json`): ALO выше лишь у **2/4** типов (критерий ≥3/4),
  **значимости нет ни по одному типу** (Wilcoxon p ≥ 0.38, bootstrap-CI включают 0) и **ни при каком пороге**
  бинаризации (0.2→3/4, но 0.3/0.5/0.7→2/4,2/4,1/4). soft exudates деградируют 0.0242→0.0012. Новые числа
  **побитово воспроизводят** старые на тех же 5 снимках: из них 2 нулевые, 2 ухудшились, всё «улучшение»
  давал один IDRiD_017. Эффект пола: у 37/54 ALO=0 в обеих арках. Классификация арок B4 не изменилась
  (full 0.7766 vs baseline 0.7545). **Следствие: подтверждённых гипотез не осталось вовсе**; опора работы —
  exp7 и стат. значимый прирост AUC в exp1. Обновлены STATUS/TAB-4.7/TAB-5.2/H-5/findings/exp4/summary.
- **Категория B ✅ ЗАКРЫТА (2026-07-27):** ре-инференс A–D×5 фолдов (n=35126, GPU/WSL) → `outputs/exp1/predictions.npz`;
  таблицы `results/tables/{exp1_per_class, TAB-4.3_exp1_calibration, exp1_clinical_indomain, TAB-5.1_statistical}.md`.
  **Ключевое:** прирост AUC конвейера СТАТ. ПОДТВЕРЖДЁН (DeLong B-vs-A z=17.2/D-vs-C z=14.0, p<10⁻⁴) → claim AUC = STRONG;
  +macro-F1/детекция меньшинств; +referable-sensitivity; НО калибровка хуже (ECE ~3×). Числа воспроизвели обучение.
  Гейт eval для EffNet: `mixed_precision:false` (AMP роняет EffNet cuDNN). Скрипты: `dump_/analyze_exp1_predictions.py`.
- **GPU доступен ЛОКАЛЬНО через WSL (важно на будущее).** Эта рабочая машина = GPU-бокс: в WSL Ubuntu
  `nvidia-smi` видит RTX 3060, окружение `dr-classifier` (`/home/yesmu/miniconda3/envs/dr-classifier/bin/python`)
  = torch **2.5.1+cu121, cuda True**. Из WSL всё по `/mnt/e/...`. Системный Python `C:\Python312` (Git Bash/PowerShell)
  = torch **cpu-only** → для GPU-работ звать WSL-интерпретатор, НЕ системный. Рабочий каталог (E:/C:) к GPU отношения не имеет.
  Долгий WSL-джоб запускать через Bash `run_in_background` с foreground-вызовом `wsl` (Start-Process/nohup-detach тут не поднялись).
- Честные позитивы: конвейер стабильно +AUC (0.037–0.045)/+κ (0.03–0.05) на обоих бэкбонах (exp1);
  **exp7 — главный позитив**: full−baseline +0.09 F1 на малых клинических данных.
- Общий механизм отрицательных результатов: доменно-настроенный препроцессинг сохраняет
  дискриминацию (ROC-AUC высок везде), но теряет мультиклассовую точность/калибровку порогов при
  zero-shot переносе (APTOS/камеры/клиника). Помогает при обучении В целевом домене (exp7), не при переносе.
- **Стратегия — честный реврейминг** (согласовано с кандидатом): строить тезис на реальных
  нюансах, не на «доминировании». Только реальные числа; демо/защиту позже пересобрать из outputs.

Связь: [[continual-ssl-init-decision]] (confound CFC-2.8 для H-1), [[thesis-writing-status]]
(гл.4/5/7 Phase-2, blocked), [[master-orchestrator-all-experiments]] (история прогонов).

**Сверка с буквой HYPOTHESIS.md (2026-07-28) → `results/HYPOTHESIS_COVERAGE.md`** — 12 пробелов G-1…G-12
по цепочке «гипотеза → outputs → results». **G-1/G-2/G-4 закрыты 2026-07-28.**
**G-4 (exp3/H-4): вердикт УСТОЙЧИВ к фиксу Stage-2** — ре-прогон на исправленном коде дал G_full
0.7617 → 0.7619 (wF1 0.60567 → 0.60584), baseline воспроизвёлся побитово, хотя входы реально
различались (до фикса `detect_fov_bbox` возвращал None на APTOS → центральный кроп 480×480 из 640×480).
Побочно: нечувствительность к потере ~25% периферии кадра ⇒ модель опирается на центральную/глобальную
статистику, не на периферические поражения (сходится с провалом H-5). Прочее: σ-свип и сетка (clip_factor × global_threshold) не
реализованы; свипы шли на IDRiD, а не EyePACS; Stage-1 и Stage-3 не изолированы в аблации (маска —
4-й канал во ВСЕХ уровнях exp2, переключателя нет); exp1 C/fold3 оборван на ep13; офлайн-предсказания
B/D не воспроизводят обучение (живой конвейер vs кэш; A/C воспроизводят точно); в exp4 нет клинической
Grad-CAM ветки. **A7 закрыт 2026-07-28**: `experiments/scripts/benchmark_compute.py` (FLOPs через
штатный `torch.utils.flop_counter`) → 4-й канал стоит +0.9% FLOPs; EffNet дешевле по FLOPs в 4.3×, но
по latency не быстрее; VRAM обучения EffNet 13.7 ГиБ vs 3.7 у ResNet.

Следующие шаги (в `results/README.md`): **перенести новые числа exp2 в `results/`** (STATUS/TAB-4.4/
findings/H-2 — сейчас там устаревшее −4.14пп); синхронизация демо/защиты; писать главы 4/5/7.
