# SECTION BRIEF
## §6.2.1 Preprocessing Engine with Configurable Pipeline

**Chapter:** 6 — Architecture of an Automated DR Screening System for Resource-Limited Environments
**Section Function:** methodological / design-specification (module internals)
**Word Count Target:** 800–1,000 words — **aim MID-BAND (880–920)**
**Paragraph Count Target:** 5–7 paragraphs (no new table; reuse DIA-6.2 by reference)

> **Pre-step gate check (PLAN §10):** PASSED. No result-dependent resource required. DIA-6.2 reuses FIG-3.1 (`✅ AVAILABLE`, real pipeline-overview render). No `❌ MISSING (real result)` dependency.

---

### GOVERNANCE BINDINGS

**Hypotheses tested/supported:** None — design specification.
**Paradigm positioning:** **P2 (central here).** The Preprocessing Engine is specified as a first-class, *configurable* processing module — preprocessing as an integral, parameterizable system component, not ancillary data preparation. No Gulshan/P1 citation; CFC-2.9/SIR-9 not-applicable-by-design.
**Primary claims advanced:** PC-5 (design-only). Links to PC-1 (the preprocessing-CNN pipeline is the module's analytical core) — **but PC-1 is CONDITIONAL and NOT yet experimentally validated**; the module core is the Chapter-3 pipeline *as specified*, with validation reserved.
**Sub-claims sourced here:** SC-5.1 (architecture/module specification).
**Forbidden claims:** CFC-2.3 (deployment outcome as result); CFC-2.4 (validated/clinical-grade/deployment-ready); CFC-2.8 (no preprocessing-alone H-1 attribution — N/A, no H-1 result cited); CFC-2.1 (no universal "always improves").
**Non-claims:** NC-17 (no universally-optimal preprocessing configuration — the configurability is a design affordance, not a claim of an optimal setting).
**Source interpretation rules:** SIR-4 (#22 prior own work); SIR-1 (no amplification); SIR-2 (#22 §II.6 limitation at first citation here).
**Scope boundaries:** **SB-4.1** (design-only; restate); DGL-4 (deployment statements are design specs subject to prerequisites). **Do NOT write "validated pipeline" as attained fact** — PC-1 CONDITIONAL; OD-5 effectiveness is an acceptance target, validation reserved to Phase 2.
**Evidence thresholds:** None.

---

### CONTENT SPECIFICATION

**Section objective:** Specify the internals of the Preprocessing Engine module (deferred by Table 6.3) as the configurable realization of FR-2 — the eight-stage pipeline of Chapter 3 made parameterizable and stage-selectable so it fits within the OD-6 envelope (NFR-2 memory, NFR-3 near-real-time) and is independently replaceable (NFR-5) — as a design specification whose diagnostic effectiveness is an acceptance target, not an attained property.

**Argumentative spine (required):**
- **Thesis:** The Preprocessing Engine realizes FR-2 by exposing the Chapter-3 eight-stage pipeline as a *configurable* module — stage on/off, train-vs-inference behaviour, dataset-specific normalization — and this configurability, not the pipeline's effectiveness (which remains an acceptance target), is what makes FR-2 admissible inside the OD-6 envelope and replaceable across contexts.
- **Reasoning chain:**
  1. Open from §6.1.2 (Table 6.3 deferred the Preprocessing Engine internals); restate design-only.
  2. Name the module core as the eight-stage pipeline *as specified in Chapter 3 (§3.1)* — reference, do not re-derive — and frame its effectiveness as the OD-5 acceptance target, validation reserved (SB-4.1; no "validated pipeline" as fact).
  3. Specify the configurability: each always-on stage is parameterized (e.g., flat-field σ, CLAHE clip-limit) and the engine distinguishes train-time (stochastic CLAHE, augmentation Stage 6) from inference-time (deterministic, no augmentation) behaviour.
  4. Argue configurability realizes the NFRs: stage selection / parameter budgeting lets the engine fit NFR-2 (sub-16 GB) and NFR-3 (near-real-time) on a given host, and the clean module interface realizes NFR-5 (independently replaceable).
  5. P2 framing: this is the architectural expression of the central thesis (model = preprocessing + CNN) — preprocessing is a configurable first-class module, not pre-processing-as-afterthought.
- **Conclusion / hand-off:** The Preprocessing Engine is fixed as a configurable design spec realizing FR-2; hands the inference stage to §6.2.2.

**Required content elements:**
1. Design-only restatement; module core = Chapter-3 pipeline *as specified* (not validated).
2. Configurability specification (stage on/off, parameters, train/inference modes).
3. NFR realization argument (NFR-2/NFR-3 via configuration; NFR-5 via interface).
4. P2 first-class-module framing.

**Required tables/figures:**
- DIA-6.2 (preprocessing engine; reuse of FIG-3.1) — reference by ID + caption + real path `defense/presentation/assets/preprocessing/10_input/04_preprocessing_pipeline_vertical.png`. Do NOT embed.

**Required equations:** None (stage parameters referenced from §3.1, not re-derived).

---

### SOURCE MAPPING

| Literature Card | Role | Content |
|----------------|------|---------|
| `yesmukhamedov-nan-rk.md` (#22, 🔹SELF) | Image Processing component precedent | EB-02 Image Processing / Image Processor component (Fig. 1, p. 83; Table 3, p. 83); §II.6 no-validation limitation |
| Chapter 3 §3.1 (internal) | the module core specification | eight-stage pipeline spec (Stages 0–7), reference only — do not re-derive |
| Governance OD-3 / OD-5 / OD-6 | augmentation def; effectiveness *target*; envelope | OD-3 (augmentation); OD-5 (acceptance target, not result); OD-6 (NFR envelope from §6.1.1) |

**⚠️ Coverage gaps:** No new figures/metrics. Any specific per-stage latency/memory figure is unsourced → must NOT be invented (flag `[UNSOURCED CLAIM]` if asserted). Stage-5 tile-vs-polar default is a §3.1.2 detail → reference §3.1, do not re-litigate here.

---

### CROSS-REFERENCES

**Backward:** §6.1.2 (Table 6.3 deferral of Preprocessing Engine internals); §3.1 (the eight-stage pipeline specification — the module core); §6.1.1 (FR-2, NFR envelope).
**Forward:** §6.2.2 (Inference module consumes the Preprocessing Engine's output, 4-channel tensor); §6.4.1 (no security role here).

---

### BOUNDARY WARNINGS

1. Do **not** write "validated pipeline" / "the validated pipeline" as attained fact — PC-1 is CONDITIONAL; frame as the Chapter-3 pipeline *as specified*, effectiveness = OD-5 target, validation reserved (SB-4.1).
2. Do **not** assert any preprocessing configuration as optimal (NC-17) — configurability is a design affordance.
3. Do **not** invent per-stage latency/memory numbers — flag `[UNSOURCED CLAIM]` if asserted.
4. Do **not** embed images; reference DIA-6.2 by path.
5. Do **not** attribute any H-1 / ablation effect to preprocessing (CFC-2.8) — no experimental result is cited here.

---

### COUNTER-ARGUMENT & INHERITED LIMITATIONS

- **Counter-position:** treating an unvalidated pipeline as a deployable module core risks asserting effectiveness not yet shown. Pre-empt by framing the module core as the *specification* (Chapter 3) whose effectiveness is the OD-5 acceptance target reserved for Phase 2 (SB-4.1), and by locating the section's contribution in the *configurability/modularization*, which is a design property independent of the pipeline's measured effect.
- **Inherited limitations (SIR-2):** #22 §II.6 — no empirical validation / prototype, inherited by the Image-Processing-component reference at first citation.

---

### ACCEPTANCE CRITERIA

- [ ] Spine realized: module core named (as specified, not validated), configurability specified, NFR realization argued, P2 framing made, hand-off to §6.2.2.
- [ ] "Validated pipeline" NOT asserted as fact; OD-5 framed as target; SB-4.1 restated.
- [ ] DIA-6.2 referenced by ID + caption + real path; no embedded image.
- [ ] Every binding checkable with a verbatim PART 3 quote.
- [ ] No invented per-stage metric (or `[UNSOURCED CLAIM]`-flagged).

---

### WRITING DIRECTIVES

- **Tense:** present (architecture/definitions); past only for what #22 reported.
- **Self-citation:** SIR-4 — #22 Image-Processing component re-identified as prior own work at first use.
- **Terminology:** preprocessing engine, configurable pipeline, four-channel tensor, train-time vs inference-time, flat-field correction, CLAHE, FOV mask — standard / from §3.1 + GLOSSARY.
- **Analytical depth:** ≥ 1 analytical claim per paragraph; the configurability→NFR-realization argument is the analytical core; reference §3.1 stages, do not re-derive.
