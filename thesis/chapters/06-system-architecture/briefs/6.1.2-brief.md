# SECTION BRIEF
## §6.1.2 Modular Architecture with PACS and EHR Integration

**Chapter:** 6 — Architecture of an Automated DR Screening System for Resource-Limited Environments
**Section Function:** methodological / design-specification (architectural decomposition)
**Word Count Target:** 1,100–1,300 words (4–5 pages) — **aim MID-BAND (1,180–1,220)**
**Paragraph Count Target:** 7–9 paragraphs (+ 1 module→requirement mapping table)

> **Pre-step gate check (PLAN §10):** PASSED. §6.1.2 requires **no result-dependent resource**. The only referenced figures are DIA-6.1 (`✅ AVAILABLE`, real architecture diagram) and the deferred conceptual asset DIA-6.3 (UML), which under PLAN §3.2 does **not** block the prose — it is inserted as a `TO BE CREATED` placeholder. Nothing the section needs is `❌ MISSING (real result)`.

---

### GOVERNANCE BINDINGS

**Hypotheses tested/supported:** None — design specification, not an empirical test.
**Paradigm positioning:** P2 (lightly). The preprocessing pipeline is decomposed as a first-class, independently-replaceable *module* (the Preprocessing Engine), consistent with P2 (preprocessing is an integral model component). **No Gulshan/P1 citation is introduced** — neither permitted nor forbidden P1 phrasings appear; CFC-2.9/SIR-9 are not-applicable-by-design and reported as such.
**Primary claims advanced:** PC-5 (modular, scalable design specification deployable in resource-limited environments; design/conceptual claim only — CONDITIONAL per ARGUMENT_MAP §VI; not prototyped or field-tested).
**Sub-claims sourced here (SC-x.y):** SC-5.1 — architecture specification and UML decomposition formally specifiable from the #22 component decomposition (LC-2025-Yesmukhamedov-01, §II.4). (SC-5.2, comparative analysis of existing systems, belongs to §1.4 / §5.3 — **not** advanced here.)
**Supporting contributions evidenced (SC-A…SC-H):** None directly (design contribution sits under PC-5).
**Forbidden claims (must not appear):** CFC-2.3 (deployment outcomes as results); CFC-2.4 (clinical-grade / validated / deployment-ready); CFC-2.2 (no superiority over named systems — and do **not** echo #36's "outperforms rule-based" as a claim of this system); CFC-2.8 (N/A — no H-1 result cited).
**Non-claims (must not assert):** NC-9 / SB-4.2 (GDPR/HIPAA framing is a design spec, NOT a certification; full detail deferred to §6.4.1); NC-16 (N/A — no device certification claim).
**Source interpretation rules:** SIR-4 (#22 is prior own work — frame as previously published; state the dissertation's extension); SIR-1 (no amplification of #22 or #36 beyond stated content); SIR-2 (inherit #22 §II.6 limitation — no empirical validation — at first citation here); SIR-3 (#36's metrics are on MIMIC-III + synthetic HL7, internal validation only — cite with dataset/validation context, do NOT import as this system's metrics); SIR-5 (#22 and #36 address different layers — do NOT cite as independent confirmation of the same claim); SIR-8 (projected Kazakhstan outcomes not attributed as findings).
**Scope boundaries:** **SB-4.1** (Chapter 6 is a design/conceptual contribution; no prototype or field-testing — keep the design-only register live, restate at section open); SB-4.2 (GDPR/HIPAA = design spec, not certified); DGL-4 (deployment statements are design specs subject to infrastructure prerequisites — store-and-forward / cloud-optional topology stated as design choice respecting prerequisites). **OD-6** (resource-limited envelope — the constraint the topology must respect; reference, do not re-derive — established §6.1.1).
**Evidence thresholds:** None (non-experimental).

---

### CONTENT SPECIFICATION

**Section objective:** Decompose the screening system into a set of independently-replaceable modules, map each module to the functional requirements of Table 6.1, and demonstrate that the chosen modular topology — including the PACS/EHR integration layer — is an admissible realization of the OD-6 non-functional envelope fixed in §6.1.1 (the NFRs prune the topology), as a bounded design specification.

**Argumentative spine (required):**
- **Thesis of this section:** The system is decomposed into a set of loosely-coupled, independently-replaceable modules whose *topology* — not merely whose membership — is determined by the OD-6 non-functional envelope: in particular, PACS/EHR integration is realized as an asynchronous interoperability layer (FHIR/HL7, store-and-forward) that satisfies the interoperability requirement (NFR-6) without placing a hospital-system round trip on the inference critical path, which intermittent connectivity (NFR-4) and no-GPU/sub-16 GB local operation (NFR-1/NFR-2) would otherwise render inadmissible.
- **Reasoning chain:**
  1. Open by taking Tables 6.1–6.2 (from §6.1.1) as the specification to satisfy; restate the design-only status (SB-4.1). Position §6.1.2 as the decomposition obligated by §6.1.1's hand-off.
  2. Present the module set (Ingestion, Preprocessing Engine, Inference, Decision-Support/Reporting, Clinician Interface, Data-Management/PACS-EHR Integration, Orchestration/Error-Handling), derived from #22's component decomposition (EB-02), and map each module to its originating FR(s) in a compact table. Note that the *internals* of several modules are deferred (Preprocessing Engine → §6.2.1; Inference / model-selection → §6.2.2; Clinician Interface → §6.3.2; security/compliance → §6.4.1).
  3. Argue that NFR-5 (modularity / independent replaceability) is realized by loose coupling at module boundaries, and that this is the property that licenses adaptation across clinical contexts without re-architecting.
  4. **Core analytical move:** show the PACS/EHR integration layer realizes NFR-6 (FHIR/HL7 interoperability; DMP mediation — #22 EB-06) but is constrained by NFR-4 to an asynchronous, store-and-forward exchange so inference does not depend on a live round trip; conclude that a cloud-centric topology binding inference to a synchronous PACS/EHR call is *inadmissible* under the envelope. Cite #36 as external corroboration that AI-driven HL7/FHIR interoperability is a studied, feasible integration approach (with SIR-3 dataset/validation caveat), **not** as evidence of this system's performance.
  5. Note the interoperability/persistence boundary (the ER-model entities: Patient, Medical Record, Device, Diagnostic Result, AI Model — #22 EB-06) is also where security-by-design (NFR-7) is concentrated (security gateway, encryption, authentication — #22 EB-04), with the qualification that GDPR/HIPAA framing is a design spec, not certification (SB-4.2/NC-9), full detail deferred to §6.4.1.
  6. Reference DIA-6.1 (component diagram) by ID + caption + real path; insert the deferred UML as `[DIA-6.3: System architecture UML — ASSET TO BE CREATED]`.
- **Conclusion / hand-off:** The module set and topology are fixed as a design specification that respects the OD-6 envelope; hands off to §6.2.1 (Preprocessing Engine internals), §6.2.2 (Inference module / model-selection — design-only, no Exp-6 numbers), §6.3 (telemedicine + clinician interface), and §6.4.1 (data-management security/compliance).

**Required content elements:**
1. Restatement of design-only status (SB-4.1) at section open; Tables 6.1–6.2 taken as the spec.
2. Module set traced to #22 EB-02 component decomposition; each module → FR mapping (table).
3. NFR-5 modularity argument (loose coupling → independent replaceability → cross-context adaptation).
4. PACS/EHR integration layer: FHIR/HL7 + DMP mediation (NFR-6), constrained by NFR-4 to store-and-forward; topology-pruning analytical claim.
5. Security/persistence boundary (NFR-7) noted as design spec (SB-4.2/NC-9), detail deferred §6.4.1.
6. Design-spec boundary registration; hand-off to §6.2–§6.4.

**Required tables/figures:**
- Table 6.3 (candidate-original synthesis): Module → originating FR(s) → governing NFR(s) → realization-deferred-to. Inline Markdown; no metrics.
- DIA-6.1 (component diagram) — reference by ID + caption + real path `defense/presentation/assets/architecture/06_system/02_system_architecture.png`. Do NOT embed.
- DIA-6.3 (UML) — deferred conceptual asset; insert as `TO BE CREATED` placeholder (PLAN §3.2).

**Required equations:** None.

---

### SOURCE MAPPING

| Literature Card | Role in This Section | Specific Content to Extract |
|----------------|---------------------|---------------------------|
| `yesmukhamedov-nan-rk.md` (#22, 🔹SELF, LC-2025-Yesmukhamedov-01) | primary design precedent (prior own work) — component decomposition + DMP/interoperability + security | EB-02 component architecture & UML component diagram (Fig. 1, p. 83; Tables 2–3, pp. 82–83); EB-06 DMP taxonomy + FHIR/HL7 interoperability + ER model (Table 4, p. 86; Fig. 5, p. 88); EB-04 security gateway / encryption / authentication (Table 3 p. 83; p. 90); §II.6 limitation (no empirical validation) |
| `nandal-2024.md` (#36, external) | external corroboration that AI-driven HL7/FHIR interoperability is a studied integration approach | EB: HL7/FHIR semantic mapping + interoperability prototype; **SIR-3** — MIMIC-III + synthetic HL7 v2, internal/sandbox validation only, no external validation, no CIs; do NOT import its 93% precision / F1=0.89 / 97% as this system's metrics |
| Governance OD-6 | the non-functional envelope the topology must respect | resource-limited definition (reference; established §6.1.1) |
| Governance SC-5.1 / PC-5 (ARGUMENT_MAP) | claim advanced (design-only) | architecture specification / UML decomposition; CONDITIONAL strength |

**⚠️ Coverage gaps:** (a) Specific middleware/queue technologies for store-and-forward beyond #22's DMP technology list (Kafka/ETL/etc.) have no source binding them to *this* system → keep at design-pattern level, do not assert a specific stack as chosen/validated; flag `[UNSOURCED CLAIM]` if a concrete product is asserted as adopted. (b) Any throughput/latency figure for the integration layer is unsourced → must NOT be invented (OD-6 conditions are qualitative; #36's 220 ms is its own prototype, not this system — SIR-3).

---

### CROSS-REFERENCES

**Backward:** §6.1.1 (Tables 6.1–6.2 requirements envelope — the spec this section satisfies; OD-6; design-only status); §1.1.2 / §3 (the integrated model embedded as the Inference + Preprocessing modules — reference, do not repeat).
**Forward:** §6.2.1 (Preprocessing Engine internals realize FR-2); §6.2.2 (Inference module / model-selection realizes FR-3 — design-only, no Exp-6 numbers); §6.3.1–§6.3.2 (telemedicine + physician-in-the-loop interface realize FR-5 and the deployment topology); §6.4.1 (GDPR/HIPAA-aligned data-management protocols expand NFR-7).

---

### BOUNDARY WARNINGS

1. Do **not** state any module, interface, or topology as implemented/deployed — these are design specifications (SB-4.1). No prototype, no benchmark numbers.
2. Do **not** import #36's prototype metrics (93% precision, F1=0.89, 97% interoperability, 220 ms) as this system's properties; cite only as external evidence that the integration approach is studied/feasible, with dataset/validation context (SIR-3, SIR-1).
3. Do **not** present GDPR/HIPAA as certified/compliant — design requirement only (SB-4.2, NC-9); defer detail to §6.4.1.
4. Do **not** introduce model-selection logic or any Exp-6 device-shift numbers — deferred to §6.2.2 (design-only).
5. Do **not** cite #22 and #36 as independent confirmation of the same interoperability claim (SIR-5); they sit at different layers (self architecture vs external interoperability prototype).
6. Do **not** invent middleware/queue products or quantitative thresholds absent from sources — flag `[UNSOURCED CLAIM]` if asserted.

---

### COUNTER-ARGUMENT & INHERITED LIMITATIONS

- **Counter-position to address (CA from ARGUMENT_MAP PC-5):** a modular architecture with no prototype risks reading as a re-labelling of the prior publication's diagram. The section must pre-empt this by (a) deriving the *topology* from the OD-6 envelope (an analytical move absent from #22, which presented the architecture without a resource-limited constraint binding its arrangement), and (b) explicitly marking the whole as a design specification whose realization is reserved (SB-4.1, CFC-1.5 framing). The dissertation's extension over #22 is the requirement-driven pruning of the admissible topology, not the component list itself.
- **Inherited limitations (SIR-2):** #22 (LC-2025-Yesmukhamedov-01) §II.6 — the architecture has no empirical validation, no prototype, no quantitative testing; inherited by every module/topology claim traced to #22, noted at first citation here. #36 (nandal-2024) — internal/sandbox validation only, no external validation, no statistical testing, dataset sizes not reported; inherited by any interoperability-feasibility statement that leans on it (SIR-3).

---

### ACCEPTANCE CRITERIA

A draft of this section is **strong** when:
- [ ] The argumentative spine is realized: modules derived and mapped (not merely listed), the topology-pruning claim argued from OD-6, design-only status fixed, hand-off made to §6.2–§6.4.
- [ ] Every module/interface traces to #22 (or governance); #36 used only as external feasibility corroboration with SIR-3 context; no unsourced product/figure asserted (or `[UNSOURCED CLAIM]`-flagged).
- [ ] The counter-position (re-labelled-diagram risk) is engaged via the requirement-driven-topology extension argument.
- [ ] Table 6.3 is interpreted in prose, not dropped in raw; DIA-6.1 cited by ID + caption + real path; DIA-6.3 inserted as `TO BE CREATED`.
- [ ] Every binding above is checkable with a verbatim PART 3 quote.
- [ ] SB-4.1 design-only status restated in the opening; PACS/EHR-as-asynchronous-layer is the analytical core.

A **passable** draft merely satisfies word count, tense, and forbidden-claim compliance. Aim above passable.

---

### WRITING DIRECTIVES (section-specific)

- **Tense:** present (architecture/definitions); past only when relaying what #22 or #36 reported.
- **Self-citation handling:** SIR-4 — first reference to #22 in this section re-identifies it as prior own work and states the extension (requirement-driven topology pruning bound to OD-6), even though §6.1.1 already introduced it.
- **Terminology watch:** PACS, EHR (standardize — not EMR), DMP (define once), CDSS, FHIR/HL7, store-and-forward, modular architecture, loose coupling, physician-in-the-loop. Flag any term absent from glossary with `[TERM NOT IN GLOSSARY: …]`.
- **Analytical depth:** ≥ 1 analytical claim per body paragraph; the section must *derive and prune* the topology from the NFR envelope (synthesis of OD-6 + #22), not enumerate components descriptively. The PACS/EHR-as-asynchronous-layer constraint is the analytical core.
