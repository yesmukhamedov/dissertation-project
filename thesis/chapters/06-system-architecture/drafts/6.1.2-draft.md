# §6.1.2 Modular Architecture with PACS and EHR Integration

> Draft generated per `prompts/writing-session-system-prompt.md` v6.0.0 · Brief: `briefs/6.1.2-brief.md` · Binding reference: INVARIANTS.md v6.0.0 · Sources: #22 (`yesmukhamedov-nan-rk.md`, 🔹SELF, LC-2025-Yesmukhamedov-01); #36 (`nandal-2024.md`, external); governance OD-6, SC-5.1/PC-5. Figures: DIA-6.1 referenced by path (not embedded); DIA-6.3 **discharged by Appendix C** (four views as Mermaid source + traceability Table C.1) — the deferral wording was corrected 2026-08-12. Table 6.3: candidate-original synthesis, inline.

---

## PART 1: SECTION TEXT

The preceding section fixed the specification that the architecture must satisfy: a functional set (Table 6.1, FR-1…FR-7) describing what the screening system does, and a non-functional set (Table 6.2, NFR-1…NFR-8) describing the resource-limited envelope under which it must do it, with the analytical conclusion that the non-functional envelope — not the functional list, which any screening system shares — is the binding design driver because it prunes the admissible realizations of each function. This section discharges the obligation that conclusion creates: it decomposes the system into modules and shows that the chosen arrangement, including its integration with hospital Picture Archiving and Communication Systems (PACS) and Electronic Health Records (EHR), is an admissible realization of that envelope. The design-only status established for the chapter is carried unchanged: the decomposition below is a **design specification**, not an implemented or field-tested system — no module has been built and integrated, and no integration behaviour is reported as a finding (SB-4.1; DGL-4).

The module set is derived from the component decomposition of the candidate's prior published architecture. In prior work by the candidate, Yesmukhamedov et al. (2025) specified an AI-driven healthcare information system as a set of components — Image Capture, Image Processing, Recognition Model, Diagnosis, Reporting, User Interface, Data Storage, and Error Handling — connected through a UML component diagram and formalized through entity-relationship and data-management models (Fig. 1, p. 83; Tables 2–4, pp. 82–86). That publication is prior own work; it presented the architecture as a design and reported neither a prototype implementation nor any quantitative validation of it (its acknowledged limitation, §II.6). The present section does not merely restate that component list. Its extension is analytical: it binds the components to the integrated preprocessing-CNN model fixed in Chapter 3, and — the move absent from the prior publication — it constrains their *arrangement* by the OD-6 envelope, so that the topology, not only the membership, of the module set is determined by the deployment context. Table 6.3 records the resulting modules, the functional requirement each realizes, the non-functional constraints that govern its arrangement, and the section in which its internal realization is specified.

**Table 6.3 — Module decomposition (design specification; each module traced to its originating FR and governing NFRs, with realization deferred as noted).**

| Module | Realizes (FR) | Governing NFR(s) | Internal realization |
|--------|---------------|------------------|----------------------|
| Ingestion | FR-1, FR-7 | NFR-4, NFR-8 | this section (boundary); §6.3.1 (capture/telemedicine) |
| Preprocessing Engine | FR-2 | NFR-2, NFR-3, NFR-5 | §6.2.1 |
| Inference | FR-3 | NFR-1, NFR-2, NFR-3 | §6.2.2 (design-only) |
| Decision-Support / Reporting | FR-4 | NFR-3, NFR-5 | §6.2.2, §6.3.2 |
| Clinician Interface | FR-5 | NFR-5 | §6.3.2 |
| Data-Management / PACS-EHR Integration | FR-6 | NFR-4, NFR-6, NFR-7 | this section; §6.4.1 (security/compliance) |
| Orchestration / Error-Handling | FR-7 | NFR-4, NFR-8 | this section |

The first design property the decomposition must secure is modularity itself (NFR-5). The modules are specified as loosely coupled: each communicates with its neighbours through a defined interface rather than through shared internal state, so that any single module can be replaced without re-architecting the others. This property is what licenses the system's intended adaptation across clinical contexts — a site with a different capture device replaces only the Ingestion module, a site with stricter local data-residency rules replaces only the Data-Management module — and it is therefore the structural counterpart of the resource-limited-deployment goal rather than a generic software-engineering preference. Loose coupling is also the precondition for the deferrals recorded in Table 6.3: because the Inference module is bounded by its interface, the model-selection logic internal to it can be specified later (§6.2.2) without disturbing the surrounding topology, and the Preprocessing Engine's eight-stage internals can be specified in §6.2.1 as the realization of FR-2.

The analytically load-bearing module is the Data-Management / PACS-EHR integration layer, because it is where the non-functional envelope most sharply prunes the design. Functionally, this layer must persist images, grades, and metadata and link them to patient records through PACS and EHR interfaces (FR-6), and it must interoperate with hospital systems through standardized exchange (NFR-6). The prior architecture specifies the mechanism for the latter: a Data Management Platform (DMP) mediating storage, integration, and interoperability, with interoperability defined as data exchange through the FHIR and HL7 standards, and an entity-relationship model fixing the persisted entities — Patient, Medical Record, Doctor, Appointment, Telemedicine Session, AI Model, Diagnostic Result, and Device (Yesmukhamedov et al., 2025, Table 4, p. 86; Fig. 5, p. 88). The constraint the envelope imposes is on *how* this exchange is scheduled. Under NFR-4, connectivity is intermittent and store-and-forward operation is required; under NFR-1 and NFR-2 the inference path must run within a local, non-GPU, sub-16 GB host. Together these make any topology in which a grade can only be produced after a synchronous round trip to a hospital PACS or EHR inadmissible: such a topology would couple the inference critical path to a network resource the envelope does not guarantee. The admissible realization is therefore an *asynchronous* integration layer — inference and preprocessing complete locally, results and images are queued, and the FHIR/HL7 exchange with PACS/EHR is reconciled opportunistically when connectivity permits. This pruning of the topology is dissertation-original analysis; it is the specific sense in which the non-functional envelope, and not the component inventory, shapes the architecture. That AI-assisted HL7/FHIR interoperability is an actively studied and prototyped integration approach is corroborated externally by Nandal (2024), who implemented an AI-enhanced HL7/FHIR exchange and evaluated it on the MIMIC-III database and synthetic HL7 messages in a sandbox setting; that study reports internal evaluation only, without external or clinical validation, so it is cited here as evidence that the integration approach is feasible and current, not as a measure of this system's behaviour.

The same integration boundary is where data security is concentrated (NFR-7). The persisted entities of the data-management layer are precisely the patient-identifying records, so the security mechanisms the prior architecture specifies — a security gateway for inter-component transfer, encryption, and user authentication (Yesmukhamedov et al., 2025, Table 3, p. 83; p. 90) — are positioned at this boundary by design. Consistent with the chapter's epistemic status, this is a security *requirement* expressed in the architecture, not an attained or certified property: the GDPR/HIPAA framing is a design specification and not a tested or certified compliance status (SB-4.2; NC-9), and its detailed protocol-level treatment is deferred to §6.4.1.

The component-level view of this module set is given by DIA-6.1, the modular system-architecture diagram showing the capture → preprocessing → inference → reporting flow with parallel data-storage and error-handling components [DIA-6.1: Modular system architecture (capture→preprocess→inference→report) — `defense/presentation/assets/architecture/06_system/02_system_architecture.png`]. The formal treatment of the decomposition is given in Appendix C, which elaborates the interfaces named above in four structural views — a component view, a deployment view, a sequence view of one screening episode, and the persisted data model — each traceable back to this section and to the requirement specification of §6.1.1 [DIA-6.3: System architecture UML — Appendix C]. Those views are design specifications on the same terms as this section: they state what the system would be, not what a built system does, and none of them carries evidence about behaviour.

The decomposition is thus fixed as a design specification: a set of loosely-coupled, independently-replaceable modules whose arrangement — and in particular whose asynchronous PACS/EHR integration layer — is an admissible realization of the OD-6 envelope rather than an arbitrary partition of functions. As with the requirements of §6.1.1, the realization and validation of this design are reserved for future work; the contribution here is the requirement-driven decomposition itself (CFC-1.5). The following sections specify the internals the table defers: §6.2.1 details the Preprocessing Engine as the configurable realization of FR-2, §6.2.2 the Inference module and its model-selection logic, §6.3 the clinician interface and the telemedicine deployment of the Ingestion module, and §6.4.1 the security and data-protection protocols of the integration layer.

---

## PART 2: CONTINUITY NOTE

*(saved separately to `continuity/6.1.2-continuity.md`)*

---

## PART 3: COMPLIANCE CHECKLIST

Each governance code in the Section Brief is reported below with auditable evidence (verbatim quote from PART 1 + location, or explicit not-applicable).

**SB-4.1 (Chapter 6 is a design/conceptual contribution; no prototype or field test) — restated at open** — ✅ Satisfied. Verbatim (¶1): *"the decomposition below is a **design specification**, not an implemented or field-tested system — no module has been built and integrated, and no integration behaviour is reported as a finding (SB-4.1; DGL-4)."* Restated at close (¶ final): *"the realization and validation of this design are reserved for future work; the contribution here is the requirement-driven decomposition itself (CFC-1.5)."*

**DGL-4 (deployment statements are design specs subject to infrastructure prerequisites)** — ✅ Satisfied. Same opening quote cites DGL-4; the store-and-forward / asynchronous topology is framed as a design choice respecting the intermittent-connectivity prerequisite, not an operational realization: *"any topology in which a grade can only be produced after a synchronous round trip to a hospital PACS or EHR [is] inadmissible … The admissible realization is therefore an *asynchronous* integration layer"* (¶ on the integration layer).

**OD-6 (resource-limited envelope — the constraint the topology must respect; referenced, not re-derived)** — ✅ Satisfied. Verbatim (¶2): *"it constrains their *arrangement* by the OD-6 envelope, so that the topology, not only the membership, of the module set is determined by the deployment context."* Applied (¶ on integration): *"under NFR-1 and NFR-2 the inference path must run within a local, non-GPU, sub-16 GB host."* OD-6 is referenced, not re-stated verbatim (established §6.1.1).

**SC-5.1 (architecture specification / UML decomposition advanced)** — ✅ Satisfied. Table 6.3 + the module-derivation paragraph advance the formal decomposition; DIA-6.1 cited and DIA-6.3 discharged in Appendix C. Verbatim (¶ on DIA-6.3): *"The formal treatment of the decomposition is given in Appendix C, which elaborates the interfaces named above in four structural views — a component view, a deployment view, a sequence view of one screening episode, and the persisted data model."* **Note (2026-08-12):** the original sentence deferred DIA-6.3 as `ASSET TO BE CREATED` and enumerated component, sequence, class, activity and ER diagrams. Appendix C has since discharged it, and it delivers component, deployment, sequence and data views — so the enumeration was corrected to what the appendix actually supplies rather than merely re-pointed. The SB-4.1 design-specification framing is carried in the replacement sentence.

**SC-5.2 (comparative analysis of existing systems — must NOT be advanced here)** — ⚪ Not applicable, by design. No commercial/named system is compared or analysed in this section; comparative analysis is reserved for §1.4 / §5.3. Closest-in-topic sentence checked (the Nandal (2024) citation) corroborates an integration *approach*, not a system comparison; no superiority framing present.

**PC-5 (system-level design claim, CONDITIONAL — design-only)** — ✅ Satisfied. PC-5 advanced at its mandated strength: the architecture is presented as a design specification throughout, with realization reserved (¶ final, quoted under SB-4.1). No deployed/validated framing.

**SIR-4 (self-citation transparency — #22 framed as prior own work + extension stated)** — ✅ Satisfied. Verbatim (¶2): *"In prior work by the candidate, Yesmukhamedov et al. (2025) specified an AI-driven healthcare information system as a set of components … That publication is prior own work … Its extension is analytical: it binds the components to the integrated preprocessing-CNN model fixed in Chapter 3, and … it constrains their *arrangement* by the OD-6 envelope."*

**SIR-1 (no amplification of #22 or #36 beyond stated content)** — ✅ Satisfied. #22 is cited only for its stated component/DMP/security content; the topology-pruning is explicitly marked dissertation-original (*"This pruning of the topology is dissertation-original analysis"*, ¶ on integration). #36 is cited only for what its card states (an implemented HL7/FHIR prototype evaluated internally). Closest-in-topic sentence checked (the #36 citation) attributes feasibility, not a validated conclusion.

**SIR-2 (inherited limitation noted at first citation)** — ✅ Satisfied. #22 §II.6 noted at first citation (¶2): *"it presented the architecture as a design and reported neither a prototype implementation nor any quantitative validation of it (its acknowledged limitation, §II.6)."* #36 limitation noted at its citation: *"that study reports internal evaluation only, without external or clinical validation."*

**SIR-3 (metric/dataset context for cross-source metrics)** — ✅ Satisfied. #36's evaluation context is stated and its metrics are deliberately not imported: *"evaluated it on the MIMIC-III database and synthetic HL7 messages in a sandbox setting; that study reports internal evaluation only … cited here as evidence that the integration approach is feasible and current, not as a measure of this system's behaviour"* (¶ on integration). No 93%/F1=0.89/97%/220 ms figure appears.

**SIR-5 (no cross-source aggregation — #22 and #36 not independent confirmation of same claim)** — ✅ Satisfied. #22 (own architecture/DMP) and #36 (external interoperability prototype) are cited for distinct propositions — the architecture's mechanism vs. the external feasibility of AI-assisted FHIR/HL7 — never combined as two confirmations of one claim. Closest-in-topic check: the integration paragraph keeps #22's DMP specification and #36's feasibility corroboration in separate sentences with separate functions.

**SIR-8 (projected Kazakhstan outcomes not attributed as findings)** — ⚪ Not applicable. No Kazakhstan deployment outcome, coverage, cost, or complication figure (#22 p. 87–88) appears in this section. Closest-in-topic sentence checked: the cross-context-adaptation example (¶ on modularity) names device/data-residency variation only, with no projected-outcome figure.

**SB-4.2 / NC-9 (GDPR/HIPAA = design spec, NOT certification; detail deferred §6.4.1)** — ✅ Satisfied. Verbatim (¶ on security): *"this is a security *requirement* expressed in the architecture, not an attained or certified property: the GDPR/HIPAA framing is a design specification and not a tested or certified compliance status (SB-4.2; NC-9), and its detailed protocol-level treatment is deferred to §6.4.1."*

**CFC-2.3 (no deployment outcome stated as a result)** — ✅ Satisfied. No deployment/operational outcome is asserted. Closest-in-topic sentence checked: the integration-layer paragraph describes admissible *design* behaviour ("results and images are queued … reconciled opportunistically"), framed as specification, not measured outcome.

**CFC-2.4 (no clinical-grade / validated / deployment-ready claim)** — ✅ Satisfied. No module or interface is described as validated, clinical-grade, or deployment-ready. Closest-in-topic sentence checked: ¶ final ("realization and validation … reserved for future work") explicitly denies attained status.

**CFC-2.2 (no superiority over named systems; #36's "outperforms rule-based" not echoed)** — ✅ Satisfied. No comparative/superiority claim for this system appears, and #36's "outperforms rule-based" claim is not echoed — #36 is cited only for feasibility/currency of the integration approach. No named-system comparison present.

**CFC-2.8 (no preprocessing-alone attribution of an H-1 effect)** — ⚪ Not applicable. No H-1 result or performance difference is cited; preprocessing appears only as the Preprocessing Engine module (FR-2). No causal-attribution phrasing present.

**CFC-2.9 / SIR-9 (paradigm attribution)** — ⚪ Not applicable, by design. No Gulshan/P1 source is cited; no permitted or forbidden P1 phrasing appears. P2 alignment is implicit in treating preprocessing as a first-class replaceable module.

**NC-16 (device domain shift ≠ certification)** — ⚪ Not applicable. No device-robustness or certification claim is made; "Device" appears only as a persisted ER-model entity (FR-6 boundary). No certification phrasing present.

**Paradigm positioning (P2)** — ✅ Satisfied (light). The Preprocessing Engine is specified as an independently-replaceable module (Table 6.3; ¶ on modularity), treating preprocessing as an integral system component (P2); no P1 contrast drawn.

**Hypotheses (H-x) / EH-3 / EH-4** — ⚪ Not applicable. Non-experimental design-specification section; no hypothesis, dominance, or validation claim advanced.

**Terminology** — ✅ Satisfied. PACS, EHR (EMR avoided), DMP (defined at first use as "a Data Management Platform (DMP) mediating storage, integration, and interoperability"), FHIR/HL7, store-and-forward, loose coupling, physician-in-the-loop used in standard forms; no `[TERM NOT IN GLOSSARY]` flag raised.

---

### Coverage gaps / flags

- No `[UNSOURCED CLAIM]` flags were required. The asynchronous/store-and-forward topology is grounded in NFR-4 (established §6.1.1) and labelled dissertation-original analysis; no concrete middleware product or latency/throughput figure was asserted (the brief's identified gaps were avoided, not filled with invented specifics).
- Candidate-original synthesis: Table 6.3 and the topology-pruning argument are the dissertation's own requirement-driven decomposition of #22's component set; this is dissertation-original analysis (SIR-1), not attributed to #22.
- No new corpus-integrity flags raised. (#36 carries a known card caveat — no DOI, non-core journal, internal validation only — handled here via SIR-3/SIR-2 and confined to a feasibility/currency corroboration.)

---

### Word count

PART 1 body (prose, excluding table rows and headings; figure-placeholder caption/path tokens included): **1,155 words** (verified) — within the 1,100–1,300 target; held to the mid-band per the directive.
