# SECTION BRIEF
## §3.1.1 Pipeline Stage Specification: 8-Stage System

**Chapter:** 3 — Methodology
**Section Function:** methodological (specification — engineering realization of the P2 paradigm)
**Word Count Target:** 1,800–2,200 words (aim 2,000, mid-band) (5.5–6.5 pages)
**Paragraph Count Target:** 13–16 paragraphs

> Generated per `prompts/section-brief-template.md` v6.0.0 · Binding reference: INVARIANTS.md v6.0.0 · Plan row: PLAN.md §6. Chapter-3 opener; carries the §2.C continuity note into its opening. Honors Chapter-3 README Task 2.5 (paradigmatic-framing insertion in the §3.1 preamble).

---

### GOVERNANCE BINDINGS

**Hypotheses tested/supported:** None tested. This section *specifies the independent-variable construct* (the pipeline) whose effect is evaluated in Experiment 1 (H-1, §4.2) and ablated in Experiment 2 (H-2, §4.3). No empirical performance claim is made here.

**Paradigm positioning:** **P2 advanced; P1 contrast required (Chapter-3 README Task 2.5).** The section preamble must state explicitly that *the decision to formalize preprocessing as part of the model is conceptual*, motivated by paradigm P2 (INVARIANTS §I IT-1; CENTRAL_THESIS §Paradigmatic framing); the eight OD-3 stages that follow are the *engineering realization* of P2. One paragraph contrasts the P1 practice (preprocessing as ancillary data preparation) using **only permitted phrasings** (CFC-2.9): "Gulshan et al. (2016) treat preprocessing as ancillary data preparation," "defer preprocessing details to the supplement," "exemplify the methodological practice that this dissertation identifies as paradigm P1." Cite `gulshan-2016.md` §15 *Paradigmatic Role* / §18 as the literature-card source of the framing (SIR-9). **Forbidden:** "Gulshan claims preprocessing is unimportant," "rejects preprocessing," "Gulshan is our baseline," "we outperform Gulshan."

**Primary claims advanced:** PC-1 at the level of *construct definition* (the integrated pipeline as the H-1 independent variable); PC-8 at the level of *defining the ablatable stage decomposition* (the 7-level Exp-2 ladder is named, not evaluated). No experimental claim advanced.

**Sub-claims sourced here (SC-x.y):** None proven; the pipeline is specified, not validated.

**Supporting contributions evidenced (SC-A…SC-H):** None claimed as evidenced; the section *defines* the methodological contribution (8-stage pipeline with explicit FOV-mask stage, adaptive flat-field, dataset-specific normalization, OD-fovea canonical orientation) per RESEARCH_ARCHITECTURE §10.

**Forbidden claims (must not appear):**
- **CFC-2.8** — no attribution of any (future) H-1 effect to preprocessing alone. Because this is a spec section, keep all efficacy statements at design/intent level; where the pipeline's *purpose* (reduce domain variability, preserve features) is stated, frame it as design objective, not demonstrated result, and never as "preprocessing alone produces the improvement."
- **CFC-2.1** — no "improves on all datasets / universally" claim for the pipeline.
- **CFC-2.2** — no superiority claim over named systems.
- **CFC-2.5** — no perfect-performance claim (cf. #23 STARE 100%, SB-1.2).
- **CFC-2.9** — no false theoretical attribution to Gulshan/P1.
- **CFC-2.7** — self-precursors (#23/#24/#19/#21) cited as-is; not retroactively recharacterized as the pipeline.

**Non-claims (must not assert):**
- **NC-15** — the external/"dirty-data" ingestion protocol is bound to Kazakh clinical data; here it is only *named and forward-referenced to §3.1.4*, not validated for arbitrary sources.
- **NC-17** — the 8-stage configuration is not asserted as the universally optimal preprocessing configuration; stage necessity is an empirical question for Exp 2.

**Source interpretation rules:**
- **SIR-1** (non-amplification) — external cards (#02, #15, #26, #27, #53, #78) cited only for what they state (motivating context for standardization / image-quality), not as proof the pipeline works.
- **SIR-3** (metric consistency) — any cited metric (e.g., #27 CLAHE effect, #78 EyeQ) carries dataset/task context; no cross-study numeric comparison.
- **SIR-4** (self-citation transparency) — #19, #21, #23, #24 are prior own work ("previously published results") with an extension statement (the pipeline formalizes and generalizes those precursors).
- **SIR-5** (no cross-source aggregation) — #23/#24 are the same EEJET article (one thread); #19/#21 are overlapping EfficientNetB0 studies (one thread). Not independent confirmations.

**Scope boundaries:**
- **IT-1 / SB-1.4** — fundus photography only; 5-class DR.
- **DGL-5** — CLAHE clip-limit parameters not assumed portable (full treatment §3.1.2); state at Stage 5.
- **DGL-1** — pipeline bounded to the dataset architecture.
- **SB-2.1** — EyePACS class imbalance acknowledged where Stage 7 / training context arises.

**Evidence thresholds:** None (non-experimental). EH-3/EH-4 apply only in §4.2.

---

### CONTENT SPECIFICATION

**Section objective:** Specify, stage by stage and in execution order, the canonical 8-stage preprocessing pipeline (OD-3) as the engineering realization of the P2 paradigm — defining for each stage its transformation, its always-on/train-only status, and its design rationale — so that the pipeline is reproducible and so that later sections (Exp 1/2) can treat it as a precisely defined independent variable; and to ground the two stages backed by real validation artifacts (Stage 1 OD/fovea detection — FIG-3.10/RES-VAL; Stage 7 dataset-specific normalization — RES-NORM) in those artifacts with honest reporting of detector reliability.

**Argumentative spine (required):**
- **Thesis of this section:** The diagnostic model is a two-stage system, `model = preprocessing + CNN`; the preprocessing stage is a deterministic, order-dependent, eight-stage transformation (OD-3) that is specified here as an integral model component — the engineering realization of the P2 commitment — rather than as ancillary data preparation.
- **Reasoning chain:**
  1. Frame: under P2 the feature space the CNN sees is co-determined by preprocessing; therefore the pipeline must be specified with the same rigor as the network (contrast with P1 practice, CFC-2.9).
  2. Specify the eight stages in order (0→7) per OD-3, each with transformation, status, and rationale; identify which are always-on and which is train-only (Stage 6).
  3. Ground the empirically-validated stages: Stage 1 rests on classical OD/fovea detection whose reliability was measured on IDRiD (RES-VAL/FIG-3.10) — report honestly that OD localization is moderate and fovea localization is unreliable, which is *why* the design uses a low-confidence fallback and an adaptive augmentation σ; Stage 7 uses dataset-specific statistics actually computed on EyePACS (RES-NORM).
  4. Define the pipeline's ACTIVE (full pipeline, 4-channel) vs ABSENT (baseline, 3-channel) states (OD-3) — the operational construct that Exp 1 contrasts — and name the 7-level ablation decomposition (Exp 2) without evaluating it.
- **Conclusion / hand-off:** The pipeline is fully specified as the H-1 independent variable; §3.1.2 deepens Stage 5 (dual-constraint CLAHE), §3.1.3 Stage 6 (augmentation), §3.1.4 the external-ingestion protocol that precedes Stage 0 for clinical data.

**Required content elements:**
1. Paragraph-1 paradigmatic preamble: `model = preprocessing + CNN`; P2 conceptual decision vs P1 practice (CFC-2.9 permitted phrasings; cite gulshan-2016.md §15/§18). Connect to §2.C hand-off (Chapter 2 foundations → Chapter 3 methodology).
2. Stage 0 — Canonical flip (L→R, OD right/macula left); always on. [FIG-3.2]
3. Stage 1 — OD-fovea rotation normalization (classical CV: OD brightest-region, fovea darkest-with-distance-prior; rotate OD→fovea axis horizontal; fallback skip on low confidence, fallback σ=13.0°; augmentation σ adaptive from detection uncertainty); always on. **Ground in RES-VAL/FIG-3.10 with honest reliability reporting.** [FIG-3.3, FIG-3.10]
4. Stage 2 — FOV crop + isotropic resize to 512×512 + centered zero-padding (foreground detection, not Hough); always on. [FIG-3.4]
5. Stage 3 — FOV mask generation (binary 1.0/0.0 → 4th channel); always on. The explicit-mask-as-stage is a named design feature (RESEARCH_ARCHITECTURE §10). [FIG-3.5]
6. Stage 4 — Flat-field correction (corrected = image − GaussianBlur(image, σ) + 128; σ = 0.07·D, D = FOV diameter from mask; inside mask only); always on. [FIG-3.6]
7. Stage 5 — CLAHE (dual-constraint clip limit on LAB L-channel; tile grid 8×8; stochastic p=0.8 train / deterministic inference). State the rule, defer full formalization to §3.1.2; DGL-5 portability caveat. [FIG-3.7]
8. Stage 6 — Augmentation (train only; unified affine [rotation σ from Stage 1, zoom 0.9–1.1, shear/stretch] + ColorJitter [brightness/contrast/saturation/hue] + Gaussian noise + JPEG compression); defer detail to §3.1.3. [FIG-3.8]
9. Stage 7 — Dataset-specific normalization (ToTensor HWC→CHW [0,1]; channel-wise (x−mean)/std from EyePACS training set after Stages 0–4, mask=1.0 pixels only; output 4×512×512). **Ground in RES-NORM (real EyePACS stats).** [FIG-3.9]
10. ACTIVE vs ABSENT definition (OD-3): full pipeline (4-ch) vs baseline (stretch-resize + ImageNet normalize, 3-ch, no mask). Name the construct as the Exp-1 contrast; name the 7-level Exp-2 ablation ladder (NC-17 caveat). [FIG-3.1, FIG-3.14]
11. Closing hand-off to §3.1.2/§3.1.3/§3.1.4.

**Required tables/figures:**
- [FIG-3.1] 8-stage pipeline overview (vertical) — `defense/presentation/assets/preprocessing/10_input/04_preprocessing_pipeline_vertical.png`
- [FIG-3.2…FIG-3.9] Stage 0–7 renders — defense/demo paths per ASSET_INVENTORY §2.3
- [FIG-3.10] OD/fovea detector validation montage (IDRiD, 516 imgs) — `experiments/outputs/validation/od_fovea_idrid_montage.png`
- [FIG-3.14] End-to-end pipeline flowchart (model = preprocessing + CNN) — `defense/figures/figures_mine/fig4_flowchart.png`
- No inline table required (the per-stage spec is prose; a compact stage-summary table is optional and may be rendered inline from OD-3 governance text if it aids readability — permitted since OD-3 is `✅ AVAILABLE` governance text).

**Required equations:**
- Flat-field correction (Stage 4): `corrected = image − GaussianBlur(image, σ) + 128`, σ = 0.07·D (OD-3 Stage 4).
- CLAHE dual-constraint clip limit (Stage 5): `CL = min(clip_factor·tile_area/256, global_threshold·tile_area)` — stated, full treatment in §3.1.2 (OD-3 Stage 5).

---

### SOURCE MAPPING

| Literature Card | Role | Specific Content to Extract |
|----------------|------|---------------------------|
| `gulshan-2016.md` (**#12**) | paradigmatic contrast (P1 practice) — SIR-9 | §15 Paradigmatic Role / §18: preprocessing deferred to supplement (scale-normalization to 299 px, FOV-mask exclusion per supplementary card). Permitted phrasings only (CFC-2.9). |
| `yesmukhamedov-scopus-q3.md` (**#24**, 🔹SELF) | self precursor: preprocessing→performance | Preprocessing raised val acc 71%→86%; ROC-AUC 0.9638; 4-layer CNN (p. 83–84). One thread with #23. |
| `yesmukhamedov-scopus-q2.md` (**#23**, 🔹SELF) | self precursor: CLAHE/enhancement toward the integrated pipeline | Upgraded CLAHE on STARE (SB-1.2: 100% not transferable; SIR-3 sensitivity anomaly). Same article as #24 (SIR-5). |
| `yesmukhamedov-conf.md` / `…-kbtu.md` (**#19/#21**, 🔹SELF) | self precursor: EfficientNetB0 transfer baseline | Frozen vs fine-tuned F1 0.62→0.74 (one thread, SIR-5). Cited only as prior pipeline lineage. |
| `saxena-2020.md` (**#02**) | external: cross-dataset variability motivates standardization | Cross-dataset AUC drop (Messidor); preprocessing as exogenous step (P1). SIR-3. |
| `liu-2022.md` (**#15**) | external: image-quality matters for DR grading | DeepDRiD κw 0.93; image-quality accuracy 0.70. SIR-3. |
| `shaout-2025.md` (**#26**) | external: enhancement-method context | FCE+CLAHE preference; CLAHE clipLimit 2.0, 8×8 (low epistemic weight, arXiv/subjective). |
| `hayati-2023.md` (**#27**) | external: CLAHE effect is architecture-dependent | CLAHE helped 3/4 CNNs, ResNet34 −12.02% (APTOS, accuracy-only). SIR-3. |
| `wang-2004-ssim.md` (**#53**) | methodology ref: structural preservation | SSIM as structural-similarity criterion (Stage-rationale: no destructive artifacts). |
| `fu-2020-eyeq.md` (**#78**) | external: image quality → DR-detection degradation | EyeQ (28,792 EyePACS imgs, 3-level quality); quality affects detection. EyePACS-derived single-source (SIR-3). |
| RES-VAL (`od_fovea_idrid_metrics.json`) | **real artifact** — Stage 1 grounding | OD success within 1 OD-radius 0.673 train / 0.612 test; within 0.5 OD-radius 0.349/0.320; fovea within 1 OD-radius 0.0/0.0097; confident-flag 100% (uninformative). Candidate-original analysis. |
| RES-NORM (`eyepacs_norm_stats.json`) | **real artifact** — Stage 7 grounding | EyePACS mean ≈ [0.506, 0.505, 0.504], std ≈ [0.090, 0.074, 0.058]; 5,000 images, stages 0–4, mask=1.0 only. |

**⚠️ Coverage gaps:** The pipeline specification itself is governance text (OD-3 / RESEARCH_ARCHITECTURE §3.1), not literature-derived — attributed to the dissertation's own design (SIR-1). The Stage-1 reliability analysis and the Stage-7 statistics are candidate-original readings of real artifacts (RES-VAL, RES-NORM) — label as such, do not source to external literature. No fabricated values.

---

### CROSS-REFERENCES

**Backward:** §2.C (Chapter 2 foundations → Chapter 3 methodology hand-off); §2.1.1–§2.1.2 (CLAHE theory → Stage 5); §2.1.3 (spatial filtering, non-adoption); §2.6 (image-quality metrics → stage rationale).
**Forward:** §3.1.2 (Stage 5 dual-constraint CLAHE detail, FIG-3.7); §3.1.3 (Stage 6 augmentation detail, FIG-3.8); §3.1.4 (external image ingestion preceding Stage 0; NC-15); §3.4 (norm-stat / training context); §4.2 (Exp 1, pipeline as IV); §4.3 (Exp 2, 7-level ablation).

---

### BOUNDARY WARNINGS

1. **CFC-2.8:** This is a spec section — keep every efficacy statement at design-intent level ("the pipeline is designed to reduce domain variability while preserving features"), never as a demonstrated H-1 result and never attributed to preprocessing alone.
2. **CFC-2.9 / SIR-9:** Use only permitted Gulshan/P1 phrasings; the contrast is between methodological *practices*, not attributed theoretical claims. Keep the operational baseline (configs A/C, OD-3) terminologically distinct from Gulshan (SB-1.12).
3. **Honest Stage-1 reporting:** RES-VAL shows OD localization moderate and fovea localization unreliable (and the `confident` flag uninformative — 100% True). Report this faithfully as candidate-original analysis; frame the fallback + adaptive-σ design as the response. Do NOT overstate detector accuracy.
4. **SIR-4/5:** Self-precursors framed as prior own work; #23/#24 one thread, #19/#21 one thread.
5. **DGL-5 / SB-1.2 / CFC-2.5:** No portability of CLAHE parameters assumed; no 100%/perfect-performance claim.
6. **NC-17:** Do not call the 8-stage configuration optimal; stage necessity is for Exp 2.
7. **Figures as placeholders only** — never embed; real paths from ASSET_INVENTORY.

---

### COUNTER-ARGUMENT & INHERITED LIMITATIONS

- **Counter-position to address:** the P1 position that a sufficiently large CNN learns the relevant invariances from raw pixels, making an elaborate pipeline redundant. The section must (i) state this as the live alternative, (ii) give the P2 design rationale (preprocessing co-determines the feature space; standardization across devices/illumination is not free for the network to infer under class imbalance and domain shift), and (iii) concede that whether each stage earns its place is an empirical question deferred to Exp 1/2 — not settled here.
- **Inherited limitations (SIR-2):**
  - #23/#24 (self): STARE 100% non-transferable; sensitivity-formula anomaly; single-dataset.
  - #19/#21 (self): EfficientNetB0 only, not generalizable to the efficient-CNN class (SIR-7); APTOS/private only.
  - #27 Hayati: accuracy-only, no CI, architecture-dependent CLAHE effect.
  - #78 Fu/EyeQ: EyePACS-derived single source, no external validation.
  - RES-VAL: detector validated on IDRiD only (Kowa camera), 516 images; reliability figures bounded to that set.

---

### ACCEPTANCE CRITERIA

A draft is **strong** when:
- [ ] The opening realizes `model = preprocessing + CNN` and the P2-vs-P1 contrast with permitted phrasings only, connecting to §2.C.
- [ ] All eight stages (0–7) are specified in order with transformation, status (always-on/train-only), and rationale; OD-3 values verbatim (σ=0.07·D, 8×8 grid, p=0.8, 512×512, 4-channel).
- [ ] Stage 1 reliability is reported honestly from RES-VAL/FIG-3.10 as candidate-original analysis; the fallback + adaptive-σ design is motivated by it.
- [ ] Stage 7 statistics are grounded in RES-NORM (real EyePACS values), mask=1.0-only computation noted.
- [ ] ACTIVE vs ABSENT construct defined (the Exp-1 IV); 7-level ablation named with NC-17 caveat.
- [ ] Self-precursors framed per SIR-4/5; external cards per SIR-1/3.
- [ ] CFC-2.8 respected (no preprocessing-alone/result framing); CFC-2.9 respected.
- [ ] Every governance code satisfiable with a verbatim PART 3 quote; figures as placeholders with real paths.
- [ ] Word count 1,800–2,200, mid-band.

A **passable** draft merely satisfies word count and forbidden-claim compliance. Aim above passable.

---

### WRITING DIRECTIVES (section-specific)

- **Tense:** Present for the specification/definitions; past for cited study findings and for the (completed) validation/norm-stat computations.
- **Self-citation handling:** #23/#24 one thread; #19/#21 one thread; "previously published results" + extension statement (formalizes/generalizes the precursors).
- **Terminology watch:** "pipeline," "FOV mask," "flat-field correction," "dual-constraint clip limit," "canonical orientation," "dataset-specific normalization" per OD-3/GLOSSARY. "Field of view (FOV)," "optic disc (OD)," "fovea" canonical. Stage numbering 0–7 per OD-3.
- **Analytical depth:** Each stage paragraph must give rationale (what problem the stage addresses, what it protects the downstream CNN from), not just the operation; the two validated stages (1, 7) must interpret their real artifacts, not merely cite them. Specification density high; this is the chapter's anchor section.
