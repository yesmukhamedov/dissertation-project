# SECTION BRIEF
## §3.2.1 ResNet-50 and EfficientNet-B3 as Primary Experimental Architectures

**Chapter:** 3 — Methodology
**Section Function:** methodological (architecture specification)
**Word Count Target:** 900–1,100 words (aim 1,000, mid-band) (3 pages)
**Paragraph Count Target:** 6–8 paragraphs

> Generated per `prompts/section-brief-template.md` v6.0.0 · Binding reference: INVARIANTS.md v6.0.0 · Plan row: PLAN.md §6. Carries the §3.1.4 continuity note. Opens §3.2 (CNN component of `model = preprocessing + CNN`).

---

### GOVERNANCE BINDINGS

**Hypotheses tested/supported:** None tested. Specifies the two backbones whose factorial contrast with the baseline is Exp 1 (H-1) and whose cross-family agreement is the EH-4 replication requirement.

**Paradigm positioning:** Not invoked (no Gulshan attribution). The CNN-centred design choice is noted as the dissertation's own (per INVARIANTS v6.0.0 rationale).

**Primary claims advanced:** PC-1 at construct level (the two architectures of the factorial). No experimental claim.

**Sub-claims sourced here (SC-x.y):** None proven.

**Supporting contributions evidenced (SC-A…SC-H):** None evidenced; defines the two-architecture replication design.

**Forbidden claims (must not appear):**
- **CFC-2.2** — no superiority claim between the two backbones or over named systems/ViT.
- **CFC-2.1** — no universal "best architecture."
- **CFC-2.7** — #24 self cited as-is (prior 4-layer CNN), not recharacterized.

**Non-claims (must not assert):**
- **NC-6** (no architectural optimality) and **SB-3.1 / SB-1.7** — no claim that ResNet-50 / EfficientNet-B3 is globally optimal or that the architecture space was searched exhaustively.
- **SIR-7** — EfficientNetB0 results (self) not generalized to the efficient-CNN class; the present backbone is EfficientNet-**B3**, distinct from the B0 of the self-publications.

**Source interpretation rules:**
- **SIR-1** — architecture cards (#66, #68, #69, #70, #09) cited only for stated architectural facts.
- **SIR-3** — any cited metric carries dataset/task context.
- **SIR-4** — #24 prior own work (4-layer CNN) with extension statement (dissertation moves to established backbones).
- **SIR-7** — central here (B0→efficient-CNN-class prohibition).

**Scope boundaries:**
- **SB-3.1** — two primary architectures (ResNet-50, EfficientNet-B3); EfficientNet-B4 for explainability (§4.6); no optimality.
- **DGL-6** — pretraining regimes named (baseline=ImageNet; integrated=ophthalmology-SSL of the SAME backbone); full treatment §3.3.2; no guaranteed transfer.
- **DGL-2** — hardware-bound (mixed-precision note).
- IT-1 — fundus only.

**Evidence thresholds:** EH-4 named as the *design rationale* for two architecture families (replication), not evaluated here.

---

### CONTENT SPECIFICATION

**Section objective:** Specify the two primary CNN backbones — ResNet-50 (residual-connection family, #66) and EfficientNet-B3 (compound-scaling family, #68) — justify the choice of two established architectures from distinct families as the substrate for the H-1 factorial and the EH-4 cross-architecture replication, specify the 3-channel (baseline) vs 4-channel (integrated) input handling and the mixed-precision configuration, and bound the choice (SB-3.1/NC-6: not optimal; SIR-7: B3 ≠ the B0 of prior self-work; CNN-centred by design, DGL-6/§3.3.2 for pretraining).

**Argumentative spine (required):**
- **Thesis of this section:** The CNN component of the model is instantiated by two established backbones from distinct architecture families — ResNet-50 and EfficientNet-B3 — chosen not as claimed-optimal networks but as a matched pair that lets the preprocessing contrast of H-1 be tested for replication across architecture families (EH-4), under a controlled training configuration and a defined 3-vs-4-channel input contract.
- **Reasoning chain:**
  1. From §3.1.4 hand-off: the network consumes the 3-channel (baseline) or 4-channel (integrated) tensor; the architecture is the second model component.
  2. ResNet-50: residual learning (#66), the residual-connection family; depth via identity shortcuts. Dissertation backbone.
  3. EfficientNet-B3: compound scaling of depth/width/resolution (#68), the compound-scaling family. Dissertation backbone. (Context: #69 EfficientNetV2; #70 ViT and #09 hybrids define the CNN-vs-ViT/hybrid landscape — the dissertation is CNN-centred by design, so the integrated-arm SSL is CNN-native, preserving the architecture across arms; ViT not adopted.)
  4. Two families, not one: the replication of any H-1 effect across ResNet-50 (A↔B) and EfficientNet-B3 (C↔D) is the EH-4 cross-architecture requirement; a single architecture could not provide it.
  5. Input-channel handling: baseline 3-channel; 4-channel (RGB+FOV mask) — first-convolution adaptation (copy pretrained weights to channels 0–2, initialize channel 3; AOQ-2 simplified since in-house SSL can pretrain on 4 channels directly, INVARIANTS v6.0.0 §X / RESEARCH_ARCHITECTURE §4.2bis).
  6. Training/hardware config: mixed precision enabled for ResNet-50, disabled for EfficientNet (fp16 overflow) — DGL-2; full config TAB-3.1 (§3.4.2/§4.1.3).
  7. Bound: SB-3.1/NC-6 (not optimal, no exhaustive search); SIR-4/7 (#24 prior 4-layer CNN; B3 ≠ B0); pretraining → §3.3.2 (DGL-6).
- **Conclusion / hand-off:** The two backbones are fixed; §3.2.2 documents the historical reference architectures (OD-2 low/high-complexity), then §3.3 specifies how the backbones are adapted, pretrained, and fine-tuned.

**Required content elements:**
1. Network as second model component; input tensor from §3.1.
2. ResNet-50 (residual learning, #66).
3. EfficientNet-B3 (compound scaling, #68); CNN-vs-ViT/hybrid context (#69, #70, #09) and the CNN-centred design rationale.
4. Two-family replication design (EH-4).
5. 3-vs-4-channel input handling (channel-3 init; AOQ-2 simplified).
6. Mixed-precision/hardware (DGL-2; TAB-3.1 ref).
7. Bounds SB-3.1/NC-6/SIR-7/SIR-4; pretraining → §3.3.2.

**Required tables/figures:**
- [FIG-3.11] ResNet-50 / EfficientNet-B3 backbone architecture — `defense/presentation/assets/architecture/08_comparison/01_abstract_model_architecture.png`; `defense/figures/figures_mine/fig5_architecture_artistic.png`.
- [FIG-3.14] End-to-end pipeline flowchart (model = preprocessing + CNN) — `defense/figures/figures_mine/fig4_flowchart.png`.

**Required equations:** None required (architecture descriptive). May reference compound-scaling/residual concepts qualitatively.

---

### SOURCE MAPPING

| Literature Card | Role | Specific Content to Extract |
|----------------|------|---------------------------|
| `he-2016-resnet.md` (**#66**) | architecture ref: ResNet-50 | Residual learning, identity shortcuts; ResNet-50 = dissertation backbone. SIR-1. |
| `tan-2019-efficientnet.md` (**#68**) | architecture ref: EfficientNet-B3 | Compound scaling (depth/width/resolution); B3 = dissertation backbone. SIR-1. |
| `tan-2021-efficientnetv2.md` (**#69**) | context: scaling lineage | EfficientNetV2 training-aware NAS (context only). |
| `dosovitskiy-2021-vit.md` (**#70**) | context: CNN-vs-ViT | ViT patch transformer; "large-scale training trumps inductive bias" — frame CNN-centred choice. SIR-1. |
| `xu-2024.md` (**#09**) | context: hybrid | EfficientNet+Swin hybrid (APTOS); landscape context only. SIR-3. |
| `yesmukhamedov-scopus-q3.md` (**#24**, 🔹SELF) | self precursor: prior 4-layer CNN | Prior 4-layer CNN (OD-2 high-complexity ref); SIR-4 extension to established backbones. |

**⚠️ Coverage gaps:** Backbone choice and input-channel/training config are governance/own-design (RESEARCH_ARCHITECTURE §4.0–§4.2bis; TAB-3.1; INVARIANTS §X AOQ-2). Architecture facts sourced to #66/#68. No fabricated metrics; no Exp-1 numbers.

---

### CROSS-REFERENCES

**Backward:** §3.1.4 / §3.1 (input tensor); §1.3.1 (CNN architectures landscape); §2.2.1 (convolution/pooling theory).
**Forward:** §3.2.2 (historical reference architectures, OD-2); §3.3.1 (5-class adaptation); §3.3.2 (ophthalmology-SSL pretraining, DGL-6/CFC-2.8); §3.3.3 (fine-tuning); §3.4.2/§4.1.3 (TAB-3.1 training config); §4.2 (Exp 1); §4.6 (EfficientNet-B4 explainability).

---

### BOUNDARY WARNINGS

1. **SB-3.1 / NC-6:** no optimality / no exhaustive search claim.
2. **SIR-7:** EfficientNet-**B3** here ≠ EfficientNet**B0** of #19/#21/#24; do not transfer B0 results to B3 or to the efficient-CNN class.
3. **CFC-2.2:** no superiority ranking (ResNet vs EfficientNet, or CNN vs ViT).
4. **DGL-6:** pretraining regimes only named; full treatment §3.3.2; no guaranteed transfer.
5. **DGL-2:** mixed-precision/hardware claims hardware-bound.
6. **SIR-4:** #24 prior own work (4-layer CNN), extension statement.

---

### COUNTER-ARGUMENT & INHERITED LIMITATIONS

- **Counter-position:** that a transformer (ViT, #70) or a hybrid (#09) would be a stronger backbone, or that a single best architecture should be chosen. Answer: the dissertation's design isolates the preprocessing/pretraining contrast within a fixed CNN family (so the integrated-arm SSL is CNN-native and architecture is not confounded, per INVARIANTS v6.0.0); two CNN families are used precisely to test replication (EH-4), not to crown an optimal network (SB-3.1/NC-6). The CNN-vs-ViT question is out of scope.
- **Inherited limitations (SIR-2):** #66/#68 benchmarked on ImageNet (natural images) — transfer to fundus not guaranteed (DGL-6); #24 self, single 4-layer CNN, APTOS/private; #09 APTOS-only hybrid.

---

### ACCEPTANCE CRITERIA

A draft is **strong** when:
- [ ] Both backbones specified with their family (residual #66; compound-scaling #68).
- [ ] Two-family choice justified by EH-4 replication, not by optimality.
- [ ] 3-vs-4-channel input handling specified (channel-3 init; AOQ-2 simplified).
- [ ] Mixed-precision/hardware noted (DGL-2; TAB-3.1 ref).
- [ ] CNN-centred design rationale stated; ViT/hybrid as out-of-scope context (CFC-2.2 respected).
- [ ] SB-3.1/NC-6 (no optimality) and SIR-7 (B3≠B0) honored; #24 self (SIR-4).
- [ ] Pretraining named, deferred to §3.3.2 (DGL-6).
- [ ] Opening connects to §3.1.4 continuity; hand-off to §3.2.2/§3.3.
- [ ] Every governance code satisfiable with a verbatim PART 3 quote.
- [ ] Word count 900–1,100, mid-band.

A **passable** draft merely satisfies word count and forbidden-claim compliance. Aim above passable.

---

### WRITING DIRECTIVES (section-specific)

- **Tense:** Present for architecture description; past for cited benchmark facts and the prior self-work.
- **Self-citation handling:** #24 prior own work (4-layer CNN), extension to established backbones.
- **Terminology watch:** "ResNet-50," "EfficientNet-B3," "residual learning," "compound scaling," "backbone," "mixed precision" per GLOSSARY/#66/#68; "architecture family."
- **Analytical depth:** Justify each design choice (why two families; why CNN-native; why channel-3 init); avoid a bare architecture catalogue.
