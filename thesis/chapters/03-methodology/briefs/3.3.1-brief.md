# SECTION BRIEF
## §3.3.1 Architecture Adaptation for Five-Class DR Classification

**Chapter:** 3 — Methodology
**Section Function:** methodological (adaptation specification)
**Word Count Target:** 600–800 words (aim 700, mid-band) (2 pages)
**Paragraph Count Target:** 4–6 paragraphs

> Generated per `prompts/section-brief-template.md` v6.0.0 · Binding reference: INVARIANTS.md v6.0.0 · Plan row: PLAN.md §6. Carries the §3.2.2 continuity note. Opens §3.3 (transfer learning / pretraining methodology).

---

### GOVERNANCE BINDINGS

**Hypotheses tested/supported:** None tested. Specifies the head adaptation common to all H-1 configurations.

**Paradigm positioning:** Not invoked.

**Primary claims advanced:** PC-1 at construct level (identical adaptation across backbones → fair factorial). No experimental claim.

**Forbidden claims (must not appear):** CFC-2.2 (no superiority); CFC-2.7 (#19/#21/#23 as-is); CFC-2.1 (no universality).

**Non-claims (must not assert):** SIR-7 (B0 not generalized); NC-6/SB-3.1 (no optimality).

**Source interpretation rules:** SIR-1; SIR-4 (#19/#21/#23 self); SIR-5 (#19/#21 one thread; #23 with #24 one thread); SIR-7.

**Scope boundaries:**
- **DGL-6** — adaptation operates under transfer; no guaranteed transfer.
- **SB-3.1** — same adaptation applied to both primary backbones.
- IT-1 — 5-class DR 0–4.

**Evidence thresholds:** **EH-4** named as design rationale — identical adaptation across both architectures enables cross-architecture replication; not evaluated here.

---

### CONTENT SPECIFICATION

**Section objective:** Specify how the two primary backbones (§3.2.1) are adapted for the five-class DR task — replacement of the pretraining-task classification head with a five-way softmax head over DR 0–4, applied identically to ResNet-50 and EfficientNet-B3 across all configurations — so that the factorial contrast is read under a common output structure (EH-4), distinguishing this head adaptation from the pretraining source (§3.3.2) and the fine-tuning schedule (§3.3.3).

**Argumentative spine (required):**
- **Thesis of this section:** Both backbones are adapted to the five-class DR task by replacing their pretraining-task head with a common five-way softmax classification head; applying the identical adaptation to both architectures and all preprocessing arms is what keeps the Experiment 1 factorial a clean contrast and supplies the cross-architecture replication structure (EH-4).
- **Reasoning chain:**
  1. The pretrained backbones carry a head sized to their pretraining task; for DR the head is replaced with a five-way softmax over DR 0–4 (5-class, IT-1), with the feature extractor retained.
  2. The adaptation is identical across ResNet-50 and EfficientNet-B3 and across baseline/integrated arms — only preprocessing and pretraining source vary (the factorial); the head does not (SB-3.1, EH-4).
  3. This is distinct from (a) the initialization/pretraining source (§3.3.2) and (b) the fine-tuning schedule by which the adapted network is trained (§3.3.3); the prior frozen-vs-fine-tuned record (#19/#21, one thread; #23/#24 high-complexity 5-class softmax precedent) motivates the schedule but the adaptation itself is the head replacement.
  4. Transfer is not guaranteed (DGL-6); the adaptation is the mechanism by which transfer is attempted, evaluated empirically in Ch 4.
- **Conclusion / hand-off:** With the output structure fixed, §3.3.2 specifies the integrated-arm initialization (ophthalmology-specific SSL), and §3.3.3 the fine-tuning schedule.

**Required content elements:**
1. Head replacement (pretraining head → 5-way softmax over DR 0–4); feature extractor retained.
2. Identical adaptation across both backbones and both arms (factorial cleanliness; EH-4).
3. Distinction from pretraining source (§3.3.2) and fine-tuning schedule (§3.3.3); prior 5-class softmax precedent (#23/#24) and frozen/fine-tuned record (#19/#21).
4. DGL-6 (transfer not guaranteed).

**Required tables/figures:** None (Resource IDs: —).
**Required equations:** None (softmax may be named, not derived — derived in §2.2.1/§2.2.2).

---

### SOURCE MAPPING

| Literature Card | Role | Specific Content to Extract |
|----------------|------|---------------------------|
| `yesmukhamedov-conf.md` (**#19**, 🔹SELF) | self: transfer adaptation precedent | EfficientNetB0 head adaptation/transfer (one thread #21; SIR-7). |
| `yesmukhamedov-kbtu.md` (**#21**, 🔹SELF) | self: same thread as #19 | (cite as one thread). |
| `yesmukhamedov-scopus-q2.md` (**#23**, 🔹SELF) | self: 5-class softmax precedent | 5-class output precedent (one thread with #24). |

**⚠️ Coverage gaps:** Adaptation spec is governance/own-design (RESEARCH_ARCHITECTURE §4.1/§4.2). Softmax theory in §2.2. No fabricated metrics.

---

### CROSS-REFERENCES

**Backward:** §3.2.1 (backbones); §3.2.2 (OD-2 references); §2.2.1/§2.2.2 (softmax/loss theory).
**Forward:** §3.3.2 (pretraining source); §3.3.3 (fine-tuning); §3.3.4 (loss); §4.2 (Exp 1, EH-4).

---

### BOUNDARY WARNINGS

1. **EH-4:** adaptation identical across both architectures — state it; it is the replication basis.
2. **DGL-6:** transfer not guaranteed.
3. **SIR-4/5/7:** self-threads; B0 not generalized.
4. Keep adaptation distinct from pretraining-source (§3.3.2) and fine-tuning (§3.3.3).

---

### COUNTER-ARGUMENT & INHERITED LIMITATIONS

- **Counter-position:** that head replacement is trivial and needs no specification. Answer: the cleanliness of the H-1 factorial depends on the head being identical across all four cells; specifying it is what licenses attributing any contrast to the preprocessing/pretraining axes rather than to an output-structure difference (EH-4). Concede transfer is not guaranteed (DGL-6).
- **Inherited limitations (SIR-2):** #19/#21 B0-only; #23/#24 STARE/APTOS single-study.

---

### ACCEPTANCE CRITERIA

A draft is **strong** when:
- [ ] Head replacement (→ 5-way softmax over DR 0–4) specified; feature extractor retained.
- [ ] Identical adaptation across both backbones/arms stated as the EH-4 replication basis.
- [ ] Adaptation distinguished from §3.3.2 (source) and §3.3.3 (schedule).
- [ ] DGL-6 stated; SIR-4/5/7 honored.
- [ ] Opening connects to §3.2.2 continuity; hand-off to §3.3.2.
- [ ] Every governance code satisfiable with a verbatim PART 3 quote.
- [ ] Word count 600–800, mid-band.

A **passable** draft merely satisfies word count and forbidden-claim compliance. Aim above passable.

---

### WRITING DIRECTIVES (section-specific)

- **Tense:** Present for the adaptation spec; past for the prior self-work.
- **Self-citation handling:** #19/#21 one thread; #23 one thread with #24; prior own work + extension.
- **Terminology watch:** "classification head," "softmax," "feature extractor," "fine-tuning" (vs frozen-base) per GLOSSARY.
- **Analytical depth:** Explain why identical adaptation matters for the factorial (EH-4); avoid trivializing.
