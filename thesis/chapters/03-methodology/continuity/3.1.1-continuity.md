# CONTINUITY NOTE — §3.1.1 Pipeline Stage Specification: 8-Stage System

**Version:** 6.0.0 · Produced as PART 2 of the §3.1.1 writing session · Carry into the §3.1.2 session (step c).

---

**Key concepts established:**
- The diagnostic model as a two-stage system `model = preprocessing + CNN` (P2 paradigm) is now operationalized as a concrete, ordered, deterministic 8-stage transformation (OD-3). The conceptual P2 commitment and its engineering realization are explicitly distinguished.
- The eight stages, in order, each specified with transformation + status + rationale: **Stage 0** canonical flip (L→R); **Stage 1** OD-fovea rotation normalization (with low-confidence fallback σ=13.0° and uncertainty-adaptive augmentation σ); **Stage 2** FOV crop + isotropic resize to 512×512 + centered zero-padding; **Stage 3** FOV mask → 4th channel; **Stage 4** flat-field correction (corrected = image − GaussianBlur(image,σ) + 128, σ=0.07·D, in-mask only); **Stage 5** dual-constraint CLAHE (LAB L-channel, 8×8, p=0.8 train / deterministic infer); **Stage 6** augmentation (train only); **Stage 7** dataset-specific normalization (4×512×512 output).
- **ACTIVE (full pipeline, 4-channel) vs ABSENT (baseline = stretch-resize + ImageNet normalize, 3-channel, no mask)** — the operational construct manipulated as the Exp-1 preprocessing axis; distinct from the P1 representative (SB-1.12).
- The 7-level Exp-2 ablation ladder is named (baseline → +flip → +rotation → +isotropic+mask → +flat-field → +CLAHE → full pipeline), with NC-17 caveat.

**Terms introduced (carry forward, available without re-definition):** pipeline; canonical flip / canonical orientation; OD-fovea rotation normalization; FOV crop; isotropic resize; centered zero-padding; FOV mask (4th channel); flat-field correction; adaptive σ = 0.07·D; dual-constraint clip limit (named, full treatment §3.1.2); LAB L-channel; tile grid 8×8; stochastic CLAHE (p=0.8); unified affine augmentation; ColorJitter / Gaussian noise / JPEG compression (detail §3.1.3); dataset-specific normalization; ACTIVE/ABSENT pipeline states.

**Argument thread:** The pipeline is fixed as a precise, reproducible construct — the H-1 independent variable and the H-2 ablation substrate. Its efficacy is stated only as a design objective (reduce domain variability, preserve features, no destructive artifacts); evaluation is deferred to Chapter 4. The thread now narrows from the whole pipeline to its individual stages: §3.1.2 (Stage 5), §3.1.3 (Stage 6), §3.1.4 (pre-Stage-0 external ingestion).

**Empirical groundings established (real artifacts):**
- **Stage 1 (RES-VAL / FIG-3.10):** OD localization moderate (within 1 OD-radius 67.3% train / 61.2% test; within 0.5 radius 34.9% / 32.0%); **fovea localization unreliable** (within 1 OD-radius 0.0% / 0.97%); detector `confident` flag uninformative (100% True). Bounded to IDRiD (DGL-1). This is the empirical justification for Stage 1's conservative fallback design — and it underwrites the §3.1.2 point that the polar CLAHE pivots on the FOV centroid rather than on a detected fovea.
- **Stage 7 (RES-NORM):** EyePACS mean ≈ [0.506, 0.505, 0.504], std ≈ [0.090, 0.074, 0.058] (5,000 imgs, 1.25B in-mask pixels, stages 0–4, mask=1.0 only). Channel-asymmetric std motivates dataset-specific (not ImageNet) normalization.

**Final topic:** The closing paragraph fixes the pipeline as the dissertation's own design, situates it in the self-precursor lineage (71%→86%, ROC-AUC 0.9638; STARE CLAHE non-transferable) and the external standardization-motivating literature, states the design objective (not a result), and hands off to §3.1.2/§3.1.3/§3.1.4.

**Setup for next section (§3.1.2 — Upgraded CLAHE with Dual-Constraint Clip Limit):** Open by taking Stage 5 as introduced here (dual-constraint clip limit named, LAB L-channel, 8×8, stochastic) and the §2.1.2 theoretical formalization (Eqs. 2.1–2.3) as established; §3.1.2 is the *implementation/methodology* counterpart — how the rule is realized in the pipeline (FIG-3.7), the two hyperparameters and how they are fixed (validation in Exp 2, §4.3.2, DGL-5), and the relationship to the candidate's prior T/80 form (SIR-4/5, #23/#24). The §3.1.1 Stage-1 finding (fovea unreliable) feeds the polar-variant framing if §3.1.2 discusses the polar CLAHE shown in FIG-3.7: pivot on FOV centroid, not detected fovea.

**Unresolved threads / flags (carry forward):**
- **Governance/implementation divergence (Stage 5):** project memory records polar CLAHE shipped as the default; OD-3 still specifies the 8×8 tile-grid dual-constraint CLAHE. The draft follows governance. Flag for a governance pass before §4.3. Relevant to §3.1.2.
- **#23 card integrity flag** (ID/self-authorship) — non-blocking; treat as self-work, one thread with #24.
- **SIR-7** (EfficientNetB0 not generalizable to efficient-CNN class) — to be honored in §3.2.2 / §3.3.
- All empirical evaluation (Exp 1 H-1, Exp 2 H-2 incl. CLAHE/σ sweeps, image-quality metrics) → Chapters 4–5; not claimable until experiments run.
