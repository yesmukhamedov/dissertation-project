# CONTINUITY NOTE — §1.3.2 Transfer Learning and Self-Supervised Pretraining in Ophthalmic Diagnostics

**Version:** 6.0.0 · Produced as PART 2 of the §1.3.2 writing session · Carry into the §1.3.3 session (step c).

---

**Key concepts established:**
- Initialization/adaptation is a **first-order, co-equal determinant** of a fixed backbone's DR performance (not a footnote); it is the axis along which the integrated arm most fundamentally departs from baseline.
- **ImageNet transfer is the field default** (Gulshan #12 Inception-v3; Saxena #02 InceptionResNetV2, AUC 0.958/0.92 Messidor-1/2 binary; Xu #09 EfficientNet+Swin, 0.97 APTOS 5-class) — cited as prevailing *practice*, not compared (SIR-3).
- **Domain gap**: ImageNet natural images ≠ fundus tissue; transfer is an in-practice success, not guaranteed (DGL-6).
- Label scarcity motivates semi-/self-supervised learning (Cheplygina #73, *general* survey — framing only, no retinal result; SIR-1/2).
- **In-domain SSL** = learning retinal representations from unlabeled fundus images (no DR labels). **Explicit THIN/GAP flag:** no retinal-SSL primary sources (SimCLR/MoCo/BYOL/DINO-on-fundus) in corpus; integrated-arm SSL is the **candidate's own methodological design** (→ §3.3.2), not a literature result.
- **Dissertation's two-regime design (v6.0.0):** baseline arm ⟹ ImageNet; integrated arm ⟹ ophthalmology-specific CNN-native SSL on the **same** backbone (ResNet-50 / EfficientNet-B3), no DR labels. RETFound (ViT-Large) reversed — architecture held identical across arms to avoid an architecture confound.
- **CFC-2.8 composite IV (load-bearing):** integrated arm differs from baseline along *both* preprocessing and pretraining axes → H-1 effect is a **joint** contribution; preprocessing-alone attribution forbidden; only integrated-configuration claims permissible. Stated as a deliberate scoping decision; factor decomposition deferred to future work.

**Terms introduced (now available without re-definition):** transfer learning, fine-tuning, pretrained weights, ImageNet transfer, domain gap, self-supervised pretraining (SSL), in-domain / domain-adaptive pretraining, pretext objective, ophthalmology-specific SSL, composite independent variable, integrated configuration, baseline arm / integrated arm.

**Argument thread:** Two of the three methodological pillars are now fixed — architecture (§1.3.1) and pretraining/initialization (§1.3.2) — and the composite-IV framing (CFC-2.8) is locked in as a deliberate scoping decision. The active thread closes by naming the third pillar: how a trained model's decisions can be inspected (explainability).

**Final topic:** The last paragraph fixes the composite-IV consequence and hands to §1.3.3 (explainability) as the third and final methodological pillar, which will complete the §1.3 methods survey. Reader ends understanding *why* the H-1 contrast is joint and primed to consider model interpretability.

**Setup for next section (§1.3.3 — Explainability Methods in Medical Image Classification):** Open by taking the architecture + pretraining pillars as given and pivot to interpretability — how the decisions of these trained CNN backbones can be inspected. Sources: #46 Selvaraju (Grad-CAM), #57 Zhou (CAM), #58 Chattopadhyay (Grad-CAM++), #59 Tjoa-Guan (XAI survey), #60 Samek (XAI/LRP), #61 Lundberg-Lee (SHAP), #62 Ribeiro (LIME). **Central binding: NC-14** — Grad-CAM activation ≠ clinical localization of pathology (it is an interpretability tool, not a diagnostic delineation). Establish Grad-CAM as the dissertation's primary lesion-localization-adjacent tool (for ALO/IoU in Exp 4) while SHAP/LIME are XAI background (not the operational metric). Frame for §2.5 (theory) and Exp 4 (§4.6). Do **not** re-discuss pretraining — reference §1.3.2 as established.

**Unresolved threads:**
- The retinal-SSL primary-source GAP is flagged here; partial resolution depends on acquiring DINO/BYOL/SimCLR/MoCo-on-fundus sources (governance task; §2.3.3/§3.3.2 currently THIN/GAP).
- The integrated-arm SSL protocol selection (which of DINO/BYOL/SimCLR/MoCo) is "selected empirically" per v6.0.0; specified in §3.3.2.
- CFC-2.8 composite IV is fixed here; operationalized in H-1/§4.2; factor decomposition deferred to future work.
- EH-4 two-backbone replication foreshadowed (same backbones in both arms); realized in §4.2.
