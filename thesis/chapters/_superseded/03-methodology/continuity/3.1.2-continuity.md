# CONTINUITY NOTE — §3.1.2 Modified CLAHE Algorithm with Dual-Constraint Clip Limit

**Version:** 6.0.0 · PART 2 of the §3.1.2 session · Carry into §3.1.3 (step c).

---

**Key concepts established:**
- Stage 5 is the *realization* of the §2.1.2 dual-constraint rule (CL = min(clip_factor·tile_area/256, global_threshold·tile_area)), not a re-derivation.
- Realization choices, each with rationale: **LAB L-channel only** (contrast without hue shift); **8×8 tile grid**; clipped-excess **redistribution** + **bilinear interpolation** (no block artifacts); **stochastic p=0.8 at train, deterministic at inference** (controlled variability coordinated with Stage 6; reproducible production transform).
- The two hyperparameters (clip_factor, global_threshold) are **free by design**, fixed by the Exp 2 sweep (§4.3.2), not imported from STARE T/80 (DGL-5). NC-17: not claimed optimal in advance.
- Spatially-adaptive (polar/vessel-density) refinement shown in FIG-3.7 pivots on the **FOV-mask centroid, not a detected fovea** — justified by the §3.1.1 Stage-1 finding (fovea localization unreliable).

**Terms carried (available without re-definition):** dual-constraint clip limit; clip_factor; global_threshold; LAB L-channel; tile grid 8×8; histogram redistribution; bilinear interpolation; stochastic CLAHE (p=0.8); polar/vessel-density CLAHE variant (FIG-3.7); FOV-mask centroid pivot.

**Argument thread:** Stage 5 is fully specified as a parameterized, luminance-channel, stochastic operation whose parameters are determined empirically (Exp 2). Efficacy is not asserted; the contrast operation's effect is explicitly contingent (Hayati #27). The thread continues stage-by-stage: §3.1.3 (Stage 6 augmentation) is next, and Stage 5's train-time stochasticity is coordinated with it.

**Final topic:** The closing sentence hands off to §3.1.3 (Stage 6), naming the coordination of Stage 5's stochasticity with augmentation.

**Setup for next section (§3.1.3 — Augmentation Strategy for Class Imbalance Mitigation):** Open by taking Stage 6 as introduced in §3.1.1 (train-only; unified affine [rotation σ adaptive from Stage 1, zoom 0.9–1.1, shear/stretch] + ColorJitter [brightness/contrast/saturation/hue] + Gaussian noise + JPEG compression) and the Stage-5 stochasticity coordination just established. §3.1.3 specifies the augmentation in full — three families (geometric / photometric–chromatic ColorJitter / acquisition-variability), the fixed-hyperparameter ranges and per-component probabilities, the dual role (overfitting control via OD-3 / §2.2.3 + class-imbalance mitigation, SC-1.4), and FIG-3.8. Honor SIR-4 (#19 LC-CONF self), OD-3 Stage 6 verbatim, SB-2.1 (EyePACS imbalance).

**Unresolved threads / flags (carry forward):**
- **[VERIFY] Stage-5 governance/implementation divergence** (OD-3 8×8 tile grid vs shipped polar default) — recommend governance pass before §4.3. Logged in §3.1.1 + §3.1.2.
- **#23 card integrity flag** — non-blocking; treat as self-work, one thread with #24.
- CLAHE parameter values + H-2 sensitivity profile → §4.3.2 (not claimable until Exp 2 runs).
- Image-quality metrics per stage (incl. SSIM no-destructive-artifact check) → §4.3.3 / §3.4.1.
