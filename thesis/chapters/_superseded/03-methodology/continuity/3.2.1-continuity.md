# CONTINUITY NOTE — §3.2.1 ResNet-50 and EfficientNet-B3 as Primary Experimental Architectures

**Version:** 6.0.0 · PART 2 of the §3.2.1 session · Carry into §3.2.2 (step c). **Opens §3.2 (CNN component).**

---

**Key concepts established:**
- The CNN is the second component of `model = preprocessing + CNN`, consuming the 3-channel (baseline) or 4-channel (integrated) tensor from §3.1.
- **Two primary backbones from distinct families:** **ResNet-50** (residual learning, #66, residual-connection family) and **EfficientNet-B3** (compound scaling, #68, compound-scaling family).
- **Two families by design (EH-4):** preprocessing contrast read within each fixed backbone (A↔B ResNet-50; C↔D EfficientNet-B3) → cross-architecture replication. Not an optimality claim (SB-3.1, NC-6).
- **CNN-centred by design:** integrated-arm SSL is CNN-native so architecture is not confounded with preprocessing/pretraining (INVARIANTS v6.0.0 §X); ViT (#70)/hybrids (#09) are out-of-scope context.
- **Input-channel handling:** baseline 3-ch; 4-ch (RGB+FOV mask); first-conv adaptation (copy ch 0–2, init ch 3 from per-channel mean) OR direct 4-channel SSL pretraining (AOQ-2 simplified).
- **Training/hardware:** standardized config (TAB-3.1; Adam, batch 16, 512×512, focal loss, 5-fold CV, seed 42); mixed precision ON for ResNet-50, OFF for EfficientNet (fp16 overflow); DGL-2 hardware-bound.

**Terms carried:** ResNet-50; EfficientNet-B3; residual learning; compound scaling; backbone; architecture family; first-convolution channel adaptation; mixed precision; configs A/B/C/D.

**Argument thread:** The two model components are now both specified at the architecture level. The thread continues within §3.2: §3.2.2 documents the historical reference architectures (OD-2 low/high-complexity bounds); then §3.3 specifies adaptation (5-class head), pretraining (ophthalmology-SSL, the integrated-arm initialization, CFC-2.8), fine-tuning, and loss.

**Final topic:** Closing sentence defers initialization to §3.3 (DGL-6) and hands off to §3.2.2 (OD-2 historical reference architectures).

**Setup for next section (§3.2.2 — Historical Reference Architectures, Reference Only):** Word band 500–700. Open by taking the two primary backbones as fixed and frame the historical v1.0 architectures as **reference only** (OD-2): the low-complexity reference (2 conv blocks, 32–64 filters, no BN/dropout, sigmoid) and the high-complexity reference (4 conv blocks, 32–256 filters, BN, dropout 0.4, softmax 5-class). Card #19 (self, EfficientNetB0 transfer baseline) — SIR-4, and **SIR-7** (B0 not generalized). These are documented to bound OD-2's architectural-complexity definition, NOT proposed for the experiments. No optimality (SB-3.1/NC-6).

**Unresolved threads / flags (carry forward):**
- **SIR-7** (B0 ≠ efficient-CNN class) — central in §3.2.2/§3.3.
- **CFC-2.8 / ophthalmology-SSL** initialization detail → §3.3.2 (composite IV; lit-GAP #73 only).
- **[VERIFY] Stage-5 polar CLAHE** divergence; **#23 card integrity flag** — non-blocking.
- All Exp-1 numbers (TAB-4.2, EH-3 verdict) → §4.2 (not claimable until run).
