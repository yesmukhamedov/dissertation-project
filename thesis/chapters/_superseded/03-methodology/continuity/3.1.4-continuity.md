# CONTINUITY NOTE — §3.1.4 External Image Ingestion Protocol

**Version:** 6.0.0 · PART 2 of the §3.1.4 session · Carry into §3.2.1 (step c). **Closes §3.1 (Pipeline Formalization).**

---

**Key concepts established:**
- The pipeline's **Stage-0 input contract** (single-eye RGB fundus, recoverable FOV, known laterality, valid DR 0–4 grade) is made explicit; clinical exports may violate it.
- A **four-component ingestion protocol** (candidate-original, lit-GAP) precedes Stage 0: (1) format/container normalization → uint8 RGB; (2) FOV/quality gating (admissibility check, OD-1-aligned); (3) laterality + metadata reconciliation (feeds Stage 0; preserves patient identity for CV grouping); (4) label/grade sanity (DR 0–4).
- **Boundaries:** NC-15 (validated only for Kazakh clinical data); SB-2.2 (private, reproducibility limited); de-identification is a design spec → §6.4.1 (SB-4.2); not a validated general cleaner (CFC-2.4).

**Terms carried:** ingestion protocol; input contract; FOV/quality gating; laterality reconciliation; label sanity; admissibility check.

**Argument thread:** §3.1 is complete — from clinical-data entry conditions, through the eight ordered stages, to the four-channel tensor. The methodological account now moves from the preprocessing component of `model = preprocessing + CNN` to the **CNN component**: §3.2 specifies the architectures that consume the tensor.

**Final topic:** Closing sentence hands off to §3.2 ("the convolutional architectures that consume that tensor").

**Setup for next section (§3.2.1 — ResNet-50 and EfficientNet-B3 as Primary Experimental Architectures):** Open by taking the four-channel tensor (and the 3-channel baseline tensor) as the defined inputs from §3.1, and specify the two primary backbones. Bindings: **SB-3.1** (two primary architectures; no architectural-optimality claim), **NC-6** (no architectural optimality), **DGL-6** (pretraining regimes: baseline=ImageNet, integrated=ophthalmology-SSL — but full SSL treatment is §3.3.2; here state backbone families + input-channel handling). Cards: #66 (ResNet, dissertation backbone), #68 (EfficientNet-B3, dissertation backbone), #69 (EfficientNetV2), #70 (ViT — CNN-vs-ViT framing), #09 (EfficientNet+Swin hybrid context), #24 (self, prior 4-layer CNN → why established backbones now). Assets: FIG-3.11 (backbone architecture), FIG-3.14 (flowchart). Honor **SIR-7** (EfficientNetB0 results not generalized to efficient-CNN class) and **SIR-4** (#24 self). 4-channel input adaptation (copy-and-mean-init for channel 3) is relevant — note AOQ-2-simplified per INVARIANTS v6.0.0 (SSL can pretrain on 4-channel directly; full detail §3.3.2).

**Unresolved threads / flags (carry forward):**
- **[UNSOURCED CLAIM]** marker in §3.1.4 is intentional (candidate methodological position) — keep through assembly.
- **[VERIFY] Stage-5 governance/implementation divergence** (polar CLAHE) — governance pass before §4.3.
- **#23 card integrity flag** — non-blocking.
- **SIR-7** to honor in §3.2.x/§3.3.x.
- 4-channel input adaptation detail and the ophthalmology-SSL initialization → §3.3.2 (CFC-2.8 composite IV).
