# CONTINUITY NOTE — §3.3.4 Weighted Loss Function Formulation for Ordinal Class Structure

**Version:** 6.0.0 · PART 2 of the §3.3.4 session · Carry into §3.4.1 (step c). **Closes §3.3 (transfer learning / pretraining methodology).**

---

**Key concepts established:**
- Training objective = **focal loss** FL(p_t) = −α_t (1−p_t)^γ log(p_t), **γ=2**, **α_t = inverse-frequency class weights** (RESEARCH_ARCHITECTURE §4.0; FIG-3.12).
- Two complementary mechanisms: focal modulation (1−p_t)^γ down-weights easy/correct examples (hard-example focus); inverse-frequency α up-weights rare grades (from §4.1.2 distribution).
- **Loss = principal class-imbalance lever; augmentation (§3.1.3) = secondary** (SC-1.4). Neither resolves imbalance. Cost-sensitive learning under-validated as a field (#40: 2/173 validation studies, SIR-3).
- **Loss/evaluation coordination:** focal loss handles imbalance at training; **quadratic-weighted Cohen's κ** penalizes ordinal error at evaluation (EH-1 → §3.4.1).
- **Focal-loss primary source (Lin et al. 2017) gap** flagged [VERIFY]; specified per governance, not fabricated.

**Terms carried:** focal loss; focusing parameter γ=2; class weight α; inverse-frequency weighting; cross-entropy; quadratic-weighted Cohen's κ; cost-sensitive learning.

**Argument thread:** §3.3 is complete — adaptation, initialization, schedule, loss. The full two-stage model (`preprocessing + CNN`) is now methodologically specified end-to-end. The thread moves to §3.4: how trained models are evaluated and how reliability is established.

**Final topic:** Closing sentence hands off to §3.4 (evaluation framework).

**Setup for next section (§3.4.1 — Multi-Metric Assessment Framework):** Word band 1,100–1,300. **TAB-3.2 (metrics & diagnostic thresholds, EH-1/OD-5) and TAB-3.3 (image-quality metrics CNR/VVI/Entropy/SSIM) rendered inline from governance.** Open by taking the trained model + the loss/κ coordination as given. Specify the multi-metric framework: primary metrics in EH-1 order (weighted F1 > ROC-AUC > quadratic-weighted κ > accuracy); secondary (per-class P/R, macro avg, training metrics for overfitting); clinical screening (sens/spec/PPV/NPV referable DR ≥2); calibration (ECE, Brier — #52 Guo); image-quality (CNR/VVI/Entropy/SSIM — #53 Wang SSIM; TAB-3.3); ALO(primary)/IoU(secondary) explainability (#64 PASCAL VOC IoU criterion). Diagnostic-effectiveness thresholds OD-5 (Acc≥0.80, wF1≥0.80, AUC≥0.90, κ≥0.70 — derived from #24/LC-SAPAKOVA self). Generalization ratio G (OD-4). Cards #21, #40, #52, #64. Bindings EH-1, EH-2, OD-4, OD-5. SIR-3 (metric context); SIR-4 (#24/#21 self thresholds — SB-1.2 not perfect-performance). NC-14 (Grad-CAM ≠ localization, for ALO/IoU framing).

**Unresolved threads / flags (carry forward):**
- **Focal-loss (Lin 2017) card gap** → corpus queue.
- **CFC-2.8 composite IV** → §4.2; **lit-GAP SSL primary sources** + **SSL arm untrained**; **SIR-7**; **[VERIFY] Stage-5 polar CLAHE**; **#23 integrity flag** — carry.
