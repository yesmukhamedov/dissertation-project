# CONTINUITY NOTE — §3.3.3 Two-Stage Fine-Tuning Protocol Design

**Version:** 6.0.0 · PART 2 of the §3.3.3 session · Carry into §3.3.4 (step c).

---

**Key concepts established:**
- **Two-stage fine-tuning schedule:** Stage 1 — backbone frozen, train new 5-class head only (protects pretrained weights from untrained-head gradients); Stage 2 — progressively unfreeze upper layers, joint fine-tuning (adapt task-specific high-level features). Standard progressive strategy (§2.3.2).
- **Prior basis:** EfficientNetB0 frozen-vs-progressive record (F1 ≈ 0.62→0.74; precision 65%→75%), #19/#21 one thread (SIR-4/5), B0-only (SIR-7) — motivates, doesn't establish, the schedule for the present backbones.
- **H-3 dropped (V3):** frozen/progressive is a **training method**, not a tested hypothesis; not reinstated.
- **Applied identically** across both backbones and both arms → factorial cleanliness, EH-4. Transfer/fine-tuning efficacy not guaranteed (DGL-6).

**Terms carried:** two-stage fine-tuning; frozen-feature configuration; progressive fine-tuning; head training; unfreezing.

**Argument thread:** The full training pipeline of §3.3 is now specified except the objective: adaptation (§3.3.1) → initialization (§3.3.2) → schedule (§3.3.3). The thread closes §3.3 with the loss the schedule minimizes.

**Final topic:** Closing sentence hands off to §3.3.4 (weighted loss for the imbalanced ordinal task).

**Setup for next section (§3.3.4 — Weighted Loss Function Formulation for Ordinal Class Structure):** Word band 600–800. FIG-3.12 (focal-loss weighting schematic). Open by taking the two-stage schedule as the optimizer of a loss yet to be specified. Specify **Focal Loss (γ=2)** with **α = inverse-frequency class weights** (RESEARCH_ARCHITECTURE §4.0; TAB-3.1) as the second class-imbalance lever (SC-1.4, complementing Stage-6 augmentation §3.1.3). Cards: #21 (self, prior weighted/transfer work), #40 (Araf, cost-sensitive learning survey — only 2/173 validation studies; SIR-3). The focal-loss primary source (Lin et al. 2017) is a flagged corpus gap (from §2.2.2 continuity) — note it, do not fabricate. Bindings SC-1.4, SB-2.1; ordinal structure (quadratic-weighted κ connection → §3.4.1). §2.2.2 supplied the loss theory; §3.3.4 = the pipeline's chosen loss.

**Unresolved threads / flags (carry forward):**
- **CFC-2.8 composite IV** → §4.2.
- **Focal-loss (Lin et al. 2017) card gap** — flag in §3.3.4; corpus-completion queue.
- **lit-GAP SSL primary sources**; **SSL B/D arm untrained**; **SIR-7**; **[VERIFY] Stage-5 polar CLAHE**; **#23 integrity flag** — carry.
