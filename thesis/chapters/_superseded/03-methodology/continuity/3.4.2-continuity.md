# CONTINUITY NOTE — §3.4.2 Cross-Validation and Statistical Reliability Protocols

**Version:** 6.0.0 · PART 2 of the §3.4.2 session · Carry into §3.C (step c). **Closes §3.4 and the methodological body of Chapter 3.**

---

**Key concepts established:**
- **5-fold patient-level stratified CV** (4 train / 1 test ×5); **patient-level grouping prevents leakage** (no patient in train+test within a fold); composes with train-only Stage-6 augmentation (no augmented image in test). FIG-3.13.
- **Reporting:** mean ± std across folds for all primary metrics.
- **Statistical-test suite:** McNemar (paired classification, Exp 1); DeLong (ROC-AUC, Exp 1/3); bootstrap 95% CI (≥1000 resamples, all exp); mixed-effects model (fold = random effect, Exp 1); Bonferroni/Holm multiple-comparison correction (Exp 1/2).
- **Reproducibility (TAB-3.1 inline):** Adam (lr 1e-4, wd 1e-4), batch 16, max 20 epochs (early stop patience 5), focal loss (γ=2, inverse-freq), 512×512, 3/4 channels, mixed precision ResNet-on/EfficientNet-off, 5-fold patient-level CV, seed 42 deterministic; fixed augmentation + LR schedule. **DGL-2** hardware-bound; **SB-2.2** private-data reproducibility limit.
- **EH-3 / EH-4 restated** as the criteria the statistics feed (dominance margins; replication across external + both architectures) — **applied in §4.2, not here.**

**Terms carried:** patient-level 5-fold CV; leakage control; McNemar; DeLong; bootstrap CI; mixed-effects model; Bonferroni/Holm; deterministic; seed 42.

**Argument thread:** The full methodology is now specified end-to-end: preprocessing (§3.1) + CNN architecture (§3.2) + adaptation/initialization/fine-tuning/loss (§3.3) + evaluation metrics and statistical protocols (§3.4). The thread closes with §3.C, which consolidates the reproducibility conditions and confirms the framework is fully specified (no new claims).

**Final topic:** Closing sentence hands off to §3.C (reproducibility consolidation).

**Setup for next section (§3.C — Conclusions to Chapter 3):** Word band 400–600. **Synthesis only; no new claims.** Consolidate: model = preprocessing + CNN realized as (a) the 8-stage pipeline with explicit input contract (§3.1), (b) two CNN backbones from distinct families (§3.2), (c) common 5-class adaptation + arm-specific initialization (ImageNet baseline / ophthalmology-SSL integrated arm, composite IV CFC-2.8) + two-stage fine-tuning + weighted focal loss (§3.3), (d) multi-metric EH-1 framework + patient-level 5-fold CV + statistical suite (§3.4). State the reproducibility conditions confirmed; restate the bounded/honest items (composite IV non-attribution CFC-2.8; SSL arm specified-not-trained; OD/fovea detector reliability honesty; parameter non-portability DGL-5; lit-GAPs §3.1.4/§3.3.2; not-optimal NC-17). Confirm Chapter 3 hands the precisely-defined model and evaluation framework to Chapter 4 (experiments). No figures/tables required.

**Unresolved threads / flags (carry forward to §3.C and beyond):**
- **CFC-2.8 composite IV** → §4.2 (attribution at results time).
- **lit-GAPs:** §3.1.4 (ingestion, candidate contribution); §3.3.2 (SSL, #73-only) + **SSL arm untrained**.
- **Corpus gaps:** focal-loss (Lin 2017); primary in-domain SSL sources (blocks §2.3.3).
- **[VERIFY] Stage-5 polar CLAHE** governance/implementation divergence.
- **#23 card integrity flag**; **SIR-7** discipline — non-blocking.
- All Exp numbers → Ch 4 (not claimable until run).
