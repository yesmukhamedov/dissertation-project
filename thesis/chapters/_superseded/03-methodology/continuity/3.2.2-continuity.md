# CONTINUITY NOTE — §3.2.2 Historical Reference Architectures (Reference Only)

**Version:** 6.0.0 · PART 2 of the §3.2.2 session · Carry into §3.3.1 (step c). **Closes §3.2 (CNN architectures).**

---

**Key concepts established:**
- **OD-2 complexity scale anchored:** low-complexity reference (2 conv blocks, 32–64 filters, no BN, no dropout, sigmoid) and high-complexity reference (4 conv blocks, 32–256 filters, BN, dropout 0.4, softmax 5-class = the candidate's prior 4-layer CNN). Architectures outside these bounds not evaluated.
- **Prior EfficientNetB0 lineage** (#19, one thread with #21): frozen vs progressively fine-tuned; fine-tuned outperformed frozen (source's own finding). SIR-4/5; **SIR-7** (B0 ≠ efficient-CNN class; B3 distinct).
- **Reference-only status:** these are NOT the experimental backbones; the §3.2.1 ResNet-50 / EfficientNet-B3 pair carries the experiments. No optimality (SB-3.1/NC-6).
- BN/dropout in the references connect to §2.2.3 regularization theory.

**Terms carried:** architectural complexity (OD-2); low-/high-complexity reference; EfficientNetB0; frozen vs fine-tuned; batch normalization; dropout.

**Argument thread:** §3.2 is complete — primary backbones (§3.2.1) and reference architectures (§3.2.2). The thread moves to §3.3: how the primary backbones are adapted (5-class head), initialized (pretraining — incl. the ophthalmology-SSL integrated-arm source), fine-tuned, and trained against a weighted loss.

**Final topic:** Closing sentence hands off to §3.3 (adaptation, pretraining, fine-tuning).

**Setup for next section (§3.3.1 — Architecture Adaptation for Five-Class DR Classification):** Word band 600–800. Open by taking the §3.2.1 backbones as fixed and specify the adaptation for 5-class DR (DR 0–4): replacing the pretrained classification head with a 5-way softmax head; the prior frozen-vs-fine-tuned record (#19/#21, one thread, SIR-4/5/7) and the high-complexity 5-class softmax precedent (#23/#24). Bindings: **DGL-6** (adaptation under transfer, no guaranteed transfer); **EH-4** (replication across ≥2 architectures — the adaptation is applied identically to both backbones). Cards #19, #21, #23. The fine-tuning *protocol* (two-stage) is §3.3.3; the pretraining *source* is §3.3.2; here = head adaptation + 5-class output.

**Unresolved threads / flags (carry forward):**
- **SIR-7** central through §3.3.
- **CFC-2.8 / ophthalmology-SSL** → §3.3.2 (composite IV; lit-GAP #73 only; flag absent primary SSL sources).
- **[VERIFY] Stage-5 polar CLAHE**; **#23 integrity flag** — non-blocking.
- All Exp numbers → Ch 4.
