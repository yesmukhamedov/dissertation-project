# CONTINUITY NOTE — §3.3.1 Architecture Adaptation for Five-Class DR Classification

**Version:** 6.0.0 · PART 2 of the §3.3.1 session · Carry into §3.3.2 (step c). **Opens §3.3 (transfer learning / pretraining methodology).**

---

**Key concepts established:**
- **Head adaptation:** the pretraining-task head of each backbone is replaced with a **five-way softmax head over DR 0–4** (IT-1 5-class); the convolutional feature extractor is retained. Softmax/loss theory ← §2.2.1/§2.2.2.
- **Three decisions explicitly separated:** (a) structural adaptation = head replacement (this section); (b) pretraining *source* (§3.3.2); (c) fine-tuning *schedule* (§3.3.3).
- **Identical adaptation across all four configs** (both backbones, both arms) → clean H-1 factorial (no output-structure confound) and well-posed **EH-4** cross-architecture replication. Only the preprocessing/pretraining axes vary.
- Transfer is **not guaranteed** (DGL-6); the adaptation is the mechanism, evaluated in Ch 4.
- 5-class softmax precedent ← #23/#24 (one thread); frozen-vs-fine-tuned record ← #19/#21 (one thread) → motivates §3.3.3, not generalized (SIR-7).

**Terms carried:** classification head; five-way softmax; feature extractor; head adaptation; fine-tuning vs frozen-feature; factorial cleanliness.

**Argument thread:** Output structure is now fixed in common across all configurations. The thread proceeds to the *initialization* that distinguishes the integrated arm — the next section — then to the fine-tuning schedule and the loss.

**Final topic:** Closing sentence hands off to §3.3.2 (ophthalmology-specific SSL initialization of the integrated arm) and §3.3.3 (fine-tuning schedule).

**Setup for next section (§3.3.2 — Ophthalmology-Specific Self-Supervised Pretraining of the CNN Backbone, integrated Arm):** Word band 900–1,100. **This is the v6.0.0 NEW section and a lit-GAP (#73 general survey only).** Critical bindings: **CFC-2.8** — the integrated arm's SSL initialization is part of the COMPOSITE independent variable (preprocessing × pretraining source); never frame any H-1 effect as preprocessing-alone; the integrated arm = full preprocessing + ophthalmology-SSL, the baseline arm = stretch-resize + ImageNet. **DGL-6** — integrated arm = ophthalmology-specific SSL of the SAME CNN backbone on an unlabeled fundus corpus (DINO/BYOL/SimCLR/MoCo family, selected empirically); no DR labels in pretraining; no guaranteed transfer. **SIR-1 / lit-GAP** — flag where primary in-domain SSL sources (DINO/BYOL/SimCLR/MoCo-on-fundus) are absent; #73 (Cheplygina survey) is the only mapped card; mark candidate-original/[UNSOURCED CLAIM] as needed, do not fabricate. AOQ-1→(b), AOQ-2 simplified (4-channel SSL), AOQ-3 retired, AOQ-4 (symmetry restored) per INVARIANTS v6.0.0 §X. Rationale: CNN-native SSL isolates architecture (no confound). Note divergence (project memory): shipped Config-D is the retired ImageNet artifact; the v6.0.0 SSL B/D arm is **not yet trained** — this is a methodology spec, not a result; do not present SSL results.

**Unresolved threads / flags (carry forward):**
- **CFC-2.8 composite IV** — central in §3.3.2.
- **lit-GAP #73-only** for SSL — flag absent primary sources.
- **SIR-7**; **[VERIFY] Stage-5 polar CLAHE**; **#23 integrity flag** — non-blocking.
- SSL B/D arm not yet trained (project memory) — keep §3.3.2 as spec, not result.
