# SECTION BRIEF
## §3.1.3 Augmentation Strategy for Class Imbalance Mitigation

**Chapter:** 3 — Methodology
**Section Function:** methodological (Stage 6 specification)
**Word Count Target:** 800–1,000 words (aim 900, mid-band) (2.5–3 pages)
**Paragraph Count Target:** 6–8 paragraphs

> Generated per `prompts/section-brief-template.md` v6.0.0 · Binding reference: INVARIANTS.md v6.1.0 · Plan row: PLAN.md §6. Carries the §3.1.2 continuity note.

> **⚠️ SUPERSEDED AUGMENTATION SPEC (updated 2026-06-26).** The Stage-6 chromatic augmentation was changed from **PCA colour jitter** to a **ColorJitter** transform (brightness, contrast, saturation each factor ∈ [0.9, 1.1]; hue shift ∈ [−0.02, 0.02]; each component applied independently with p = 0.5), and two **acquisition-variability** augmentations were added: **Gaussian noise** (σ ∈ [2, 6] on the 8-bit RGB scale, p = 0.15) and **JPEG compression** (quality ∈ [70, 100], p = 0.20). Final on-the-fly order: (1) unified affine; (2) ColorJitter; (3) Gaussian noise; (4) JPEG compression. The current §3.1.3 draft/translation and OD-3 Stage 6 (INVARIANTS v6.1.0) reflect this. The PCA / RES-PCA passages below (eigenvalue analysis, #65 method attribution, DGL-1 basis-binding) are **historical and no longer apply** — RES-PCA is no longer a project artifact and #65 is no longer cited in §3.1.3.

---

### GOVERNANCE BINDINGS

**Hypotheses tested/supported:** None tested. Specifies Stage 6 (train-only augmentation), a component of the IV evaluated in Exp 1/2.

**Paradigm positioning:** Not invoked.

**Primary claims advanced:** Supports PC-1 at construct level (augmentation as part of the pipeline). No experimental claim.

**Sub-claims sourced here (SC-x.y):** **SC-1.4** (class-imbalance handling) at the level of *one design lever* — augmentation contributes to imbalance mitigation alongside the weighted loss (§3.3.4); not validated here.

**Supporting contributions evidenced (SC-A…SC-H):** None claimed evidenced.

**Forbidden claims (must not appear):**
- **CFC-2.1** — no "augmentation always improves / universally."
- **CFC-2.2** — no superiority claim.
- **CFC-2.7** — #19 cited as-is.
- **CFC-2.8** — N/A (no H-1 result), but keep efficacy at design level.

**Non-claims (must not assert):** **NC-17** — augmentation config not asserted optimal.

**Source interpretation rules:**
- **SIR-1** — #65 (AlexNet/PCA color) and #19 cited only for what they state.
- **SIR-4** — #19 prior own work; **SIR-5** — #19/#21 one overlapping thread (cite #19 as LC-CONF; do not aggregate).
- **SIR-7** — #19/#21 EfficientNetB0-only; do not generalize to efficient-CNN class.

**Scope boundaries:**
- **OD-3 Stage 6** — verbatim component list; train-only.
- **SB-2.1** — EyePACS severe class imbalance documented.
- **DGL-1** — PCA basis dataset-specific (EyePACS).
- IT-1 — fundus only.

**Evidence thresholds:** None (non-experimental).

---

### CONTENT SPECIFICATION

**Section objective:** Specify Stage 6, the train-only augmentation, in full — the unified affine transform (rotation with Stage-1-adaptive dispersion, zoom [0.9, 1.1], optional shear/stretch), brightness/contrast jitter, and the Krizhevsky-style PCA color jitter whose colour basis was actually estimated from the EyePACS training set (RES-PCA) — and explain its dual role: generic overfitting control (regularization, §2.2.3) and, applied with class-conditional emphasis, one lever against EyePACS's severe class imbalance (SC-1.4), with the loss-side lever specified in §3.3.4.

**Argumentative spine (required):**
- **Thesis of this section:** Stage 6 augmentation enlarges and re-weights the effective training distribution through geometry-preserving affine, photometric, and dataset-specific PCA-colour perturbations applied at training time only; it serves simultaneously as regularization and as one of two design levers (with the weighted loss) addressing EyePACS class imbalance, while leaving inference deterministic.
- **Reasoning chain:**
  1. Train-only status and its reason (deterministic inference; augmentation must not leak into validation/test — risk-control, RESEARCH_ARCHITECTURE §9.1).
  2. Unified affine: rotation dispersion adaptive from Stage-1 detection uncertainty (ties to §3.1.1), zoom [0.9, 1.1], shear/stretch; geometry-preserving plausible viewpoint variation.
  3. Brightness/contrast jitter + coordination with Stage-5 stochastic CLAHE (§3.1.2) → contrast-profile robustness.
  4. PCA color jitter (Krizhevsky-style, σ=0.1): add along eigenvectors scaled by eigenvalues × N(0,σ); the basis is the real EyePACS basis (RES-PCA: eigvals ≈ [20.4, 128.1, 2374.0]; one dominant chromatic axis ≈18× the next) — perturbation is predominantly along the dataset's dominant colour-variation direction, a principled colour augmentation rather than arbitrary jitter (cite #65 for the method, RES-PCA for the realized basis).
  5. Dual role: regularization (§2.2.3) + class-imbalance mitigation lever (SC-1.4), the second lever being the weighted loss (§3.3.4); SB-2.1.
- **Conclusion / hand-off:** Stage 6 completes the train-time pipeline; Stage 7 (already specified §3.1.1) finalizes the tensor; §3.1.4 specifies the external-ingestion protocol preceding Stage 0.

**Required content elements:**
1. Train-only status + leakage rationale.
2. Unified affine (rotation Stage-1-adaptive, zoom, shear/stretch).
3. Brightness/contrast + Stage-5 coordination.
4. PCA color jitter with the real RES-PCA basis; interpret the dominant-axis structure.
5. Dual role (regularization + SC-1.4 imbalance lever; loss lever → §3.3.4); SB-2.1.
6. Hand-off.

**Required tables/figures:**
- [FIG-3.8] Stage 6 augmentation (rotation/scale/shear/PCA-color/brightness) — `defense/presentation/assets/preprocessing/19_aug_rotation/…/stage6_augmentation.png`.
- RES-PCA artifact (real EyePACS PCA basis) referenced for the colour-jitter values.

**Required equations:**
- PCA color jitter (Krizhevsky-style): per-pixel additive perturbation `[p1 p2 p3]·[α1λ1, α2λ2, α3λ3]ᵀ`, αi  N(0, σ=0.1) (OD-3 Stage 6; #65). Eigen-basis (pi, λi) from RES-PCA.

---

### SOURCE MAPPING

| Literature Card | Role | Specific Content to Extract |
|----------------|------|---------------------------|
| `yesmukhamedov-conf.md` (**#19**, 🔹SELF, LC-CONF) | self precursor: augmentation/transfer pipeline lineage | EfficientNetB0 transfer pipeline; one thread with #21 (SIR-5); EfficientNetB0-only (SIR-7). |
| `krizhevsky-2012-alexnet.md` (**#65**) | method ref: PCA color augmentation | Krizhevsky-style PCA colour augmentation (add multiples of principal components scaled by eigenvalue × Gaussian); also ReLU+dropout context. SIR-1. |
| RES-PCA (`eyepacs_pca_eigvals.npy`/`eigvecs.npy`) | **real artifact** — realized colour basis | eigvals ≈ [20.42, 128.06, 2374.04]; dominant eigenvector ≈ [−0.761, −0.552, −0.340]; one axis ≈18× the next. Candidate-original reading. |

**⚠️ Coverage gaps:** Augmentation spec is governance/own-design (OD-3 Stage 6); the realized PCA basis is a candidate-original reading of RES-PCA. The PCA *method* is attributed to #65; the *basis values* to the dissertation's estimation on EyePACS. §2.2.3 supplies the regularization theory. No fabricated values.

---

### CROSS-REFERENCES

**Backward:** §2.2.3 (augmentation as regularization, dual role); §3.1.1 (Stage 6 introduced; Stage-1 adaptive σ); §3.1.2 (Stage-5 stochasticity coordination).
**Forward:** §3.3.4 (weighted loss — second imbalance lever); §4.1.2 (class distribution); §4.3 (Exp 2 ablation, augmentation level).

---

### BOUNDARY WARNINGS

1. **SIR-5/SIR-7:** #19/#21 one thread, EfficientNetB0-only — no generalization.
2. **CFC-2.1 / NC-17:** augmentation not "always beneficial"/optimal.
3. **SC-1.4 framing:** augmentation is *one* lever; the loss is the other (§3.3.4) — do not overclaim imbalance resolution.
4. **DGL-1:** PCA basis is EyePACS-specific.
5. **Leakage:** augmentation train-only; never in val/test (RESEARCH_ARCHITECTURE §9.1).

---

### COUNTER-ARGUMENT & INHERITED LIMITATIONS

- **Counter-position:** that augmentation alone can correct severe class imbalance. Answer: augmentation is a distribution-enlarging regularizer and a partial imbalance lever; the principal imbalance mechanism is the inverse-frequency-weighted focal loss (§3.3.4); their joint effect, and the marginal value of the augmentation level, is an Exp-2 question (NC-17).
- **Inherited limitations (SIR-2):** #19 EfficientNetB0-only, APTOS/private; #65 PCA-colour benchmarked on ImageNet (natural images), not fundus — method borrowed, basis re-estimated.

---

### ACCEPTANCE CRITERIA

A draft is **strong** when:
- [ ] Train-only status + leakage rationale stated.
- [ ] Unified affine (Stage-1-adaptive rotation, zoom, shear/stretch), brightness/contrast, PCA colour all specified.
- [ ] PCA colour jitter realized with the real RES-PCA basis, and the dominant-axis structure interpreted (not just listed).
- [ ] Dual role stated; SC-1.4 framed as one of two levers (loss → §3.3.4); SB-2.1.
- [ ] #19/#21 one thread (SIR-5), EfficientNetB0-only (SIR-7); #65 method-only.
- [ ] Opening connects to §3.1.2 continuity.
- [ ] Every governance code satisfiable with a verbatim PART 3 quote.
- [ ] Word count 800–1,000, mid-band.

A **passable** draft merely satisfies word count and forbidden-claim compliance. Aim above passable.

---

### WRITING DIRECTIVES (section-specific)

- **Tense:** Present for spec; past for the (completed) PCA-basis estimation and cited findings.
- **Self-citation handling:** #19 prior own work, one thread with #21.
- **Terminology watch:** "augmentation," "unified affine," "PCA color jitter," "brightness/contrast jitter" per OD-3/GLOSSARY; "principal component" for eigenvectors.
- **Analytical depth:** Interpret the PCA basis (dominant chromatic axis) and the dual role; avoid a bare parameter list.
