# SECTION BRIEF
## §3.1.2 Modified CLAHE Algorithm with Dual-Constraint Clip Limit

**Chapter:** 3 — Methodology
**Section Function:** methodological (algorithm realization of Stage 5)
**Word Count Target:** 900–1,100 words (aim 1,000, mid-band) (3 pages)
**Paragraph Count Target:** 6–8 paragraphs

> Generated per `prompts/section-brief-template.md` v6.0.0 · Binding reference: INVARIANTS.md v6.0.0 · Plan row: PLAN.md §6. Carries the §3.1.1 continuity note. Methodological counterpart to the *theoretical* formalization in §2.1.2 — must not re-derive the rule, but realize it as a pipeline stage.

---

### GOVERNANCE BINDINGS

**Hypotheses tested/supported:** None tested. Specifies the Stage-5 operation whose two hyperparameters are swept in Experiment 2 (H-2, §4.3.2). Sets up, does not evaluate.

**Paradigm positioning:** Not invoked. No P1/P2 attribution, no Gulshan citation. (Stage 5 is an internal pipeline component.)

**Primary claims advanced:** PC-2 at the level of *algorithmic specification* (the dual-constraint CLAHE whose parameters Exp 2 sweeps). No experimental claim.

**Sub-claims sourced here (SC-x.y):** None proven.

**Supporting contributions evidenced (SC-A…SC-H):** Defines (does not yet evidence) the "dual-constraint stochastic CLAHE in LAB color space" contribution (RESEARCH_ARCHITECTURE §10).

**Forbidden claims (must not appear):**
- **CFC-2.5 / SB-1.2** — the #23 STARE 100% result not transferable; no perfect-performance framing.
- **CFC-2.1** — no "always improves" for CLAHE (cf. #27 ResNet34 −12.02%).
- **CFC-2.2** — no superiority ranking of CLAHE variants as a general claim.
- **CFC-2.7** — #23/#24 cited as-is (single-threshold T/80), not retroactively credited with the dual-constraint rule.

**Non-claims (must not assert):** **NC-17** — the chosen clip configuration is not asserted universally optimal.

**Source interpretation rules:**
- **SIR-1** — #23/#24 cited only for what they state.
- **SIR-3** — sensitivity-formula anomaly in #23/#24 (Sen=TP/(TP+TN)) flagged if validation metrics cited; #27 metric context noted.
- **SIR-4** — #23/#24 prior own work; extension statement (dissertation generalizes T/80 → dual-constraint, validates in own framework).
- **SIR-5** — #23/#24 same EEJET article, one self-cited thread.

**Scope boundaries:**
- **DGL-5** (central) — CLAHE parameters not assumed portable from STARE; fixed by Exp 2 validation.
- **SB-1.2** — STARE 100% not transferable.
- IT-1 — fundus only.

**Evidence thresholds:** None (non-experimental); H-2 EH criteria apply in §4.3.2.

---

### CONTENT SPECIFICATION

**Section objective:** Specify how the dual-constraint CLAHE (theoretically formalized in §2.1.2) is realized as Stage 5 of the pipeline — colour-space handling (LAB L-channel), tile grid (8×8), clipped-excess redistribution and bilinear interpolation, stochastic train / deterministic inference application — identify its two free hyperparameters and state precisely how their values are fixed (Exp 2 sweep, DGL-5), and document the algorithmic refinement shown in FIG-3.7, honoring the governance/implementation divergence with a [VERIFY] flag.

**Argumentative spine (required):**
- **Thesis of this section:** Stage 5 realizes the dual-constraint clip limit of §2.1.2 as a concrete, reproducible image operation on the LAB luminance channel with an 8×8 tile grid, applied stochastically at training time and deterministically at inference, whose two hyperparameters are left free by design and fixed only by validation within the dissertation's framework (DGL-5).
- **Reasoning chain:**
  1. Recall (one paragraph, from §2.1.2 / §3.1.1) the dual-constraint rule CL = min(clip_factor·tile_area/256, global_threshold·tile_area); this section is its realization, not its derivation.
  2. Colour-space choice: convert RGB→LAB, equalize the L (luminance) channel only, convert back — enhances contrast without hue shift; tile grid 8×8; clipped excess redistributed; bilinear interpolation across tile boundaries (standard CLAHE mechanics).
  3. Stochastic application: p = 0.8 at train (a source of controlled variability coupled to Stage 6's augmentation role), deterministic at inference (reproducible production transform).
  4. The two hyperparameters (clip_factor, global_threshold) are free; values are fixed by the Exp 2 sweep (§4.3.2), not imported from the STARE T/80 result (DGL-5); SIR-4/5 self-precursor framing.
  5. Refinement shown in FIG-3.7: the polar/vessel-density variant explored in the implementation pivots adaptation on the FOV centroid — consistent with the §3.1.1 finding that fovea localization is unreliable. Honor the governance/implementation divergence: OD-3 specifies the 8×8 tile-grid dual-constraint CLAHE as canonical; the polar variant's adoption-as-default is recorded in implementation but not yet in governance → [VERIFY] flag, present canonical per OD-3.
- **Conclusion / hand-off:** Stage 5 is fully specified as a parameterized, stochastic, luminance-channel operation; §3.1.3 specifies Stage 6 (augmentation) with which Stage 5's stochasticity is coordinated.

**Required content elements:**
1. One-paragraph recall of the dual-constraint rule (point to §2.1.2 Eqs. 2.1–2.3; do not re-derive).
2. LAB L-channel realization; 8×8 tile grid; redistribution + bilinear interpolation.
3. Stochastic p=0.8 train / deterministic inference; rationale.
4. Two hyperparameters; fixed by Exp 2 (§4.3.2); DGL-5 non-portability; SIR-4/5 self-framing.
5. FIG-3.7 refinement (polar/vessel-density variant; FOV-centroid pivot tying to §3.1.1 Stage-1 finding); governance/implementation divergence [VERIFY].
6. Hand-off to §3.1.3.

**Required tables/figures:**
- [FIG-3.7] Stage 5 — dual-constraint CLAHE (incl. polar variant + vessel maps) — `defense/presentation/assets/preprocessing/17_clahe_polar/stage5_clahe.png`; `demo/web/public/pipeline/dr04/preprocessing/stage_5_clahe/polar/`.

**Required equations:**
- Dual-constraint clip limit restated (reference Eq. 2.3): `CL = min(clip_factor·tile_area/256, global_threshold·tile_area)` (OD-3 Stage 5). No new derivation.

---

### SOURCE MAPPING

| Literature Card | Role | Specific Content to Extract |
|----------------|------|---------------------------|
| `yesmukhamedov-scopus-q2.md` (**#23**, 🔹SELF) | self precursor: T/80 CLAHE | Single-threshold T/80 on STARE; SB-1.2 (100% non-transferable); SIR-3 sensitivity anomaly. One thread with #24. |
| `yesmukhamedov-scopus-q3.md` (**#24**, 🔹SELF) | self precursor: CLAHE params | clip limit 2.0, grid 8×8 (p. 83) as prior parameter choice; do not aggregate with #23. |
| `hayati-2023.md` (**#27**) | external: CLAHE architecture-dependence (optional, brief) | ResNet34 −12.02% (accuracy-only, APTOS); supports DGL-5/NC-17 framing. |

**⚠️ Coverage gaps:** The dual-constraint rule and its realization are governance/own-design (OD-3 Stage 5; RESEARCH_ARCHITECTURE §3.2); the polar/vessel-density refinement shown in FIG-3.7 is candidate-original implementation — label as such, no external source. No fabricated parameter values (clip_factor/global_threshold numbers reserved for §4.3.2).

---

### CROSS-REFERENCES

**Backward:** §2.1.2 (theoretical dual-constraint formalization, Eqs. 2.1–2.3); §3.1.1 (Stage 5 introduced; Stage-1 fovea-localization-unreliable finding).
**Forward:** §3.1.3 (Stage 6 augmentation, stochasticity coordination); §4.3.2 (CLAHE clip-limit sensitivity sweep, H-2); §4.3.3 (image-quality metrics per stage).

---

### BOUNDARY WARNINGS

1. **Do not re-derive** the dual-constraint rule (that is §2.1.2's job); realize it.
2. **DGL-5 / SB-1.2 / CFC-2.5:** No portable parameters; no STARE-100% framing.
3. **CFC-2.1 / NC-17:** CLAHE effect is contingent (cf. #27); chosen config not "optimal."
4. **CFC-2.7 / SIR-4/5:** prior work = T/80 single-threshold (one thread); dual-constraint is the dissertation's advance.
5. **Governance/implementation divergence:** OD-3 = 8×8 tile-grid dual-constraint CLAHE (canonical). Polar-variant-as-default is implementation-only (project memory) → present canonical, flag [VERIFY].

---

### COUNTER-ARGUMENT & INHERITED LIMITATIONS

- **Counter-position to address:** that fixing CLAHE parameters by a single rule (e.g., the prior T/80, or a literature clip-limit of 2.0) suffices and a sweep is unnecessary. Answer: contrast effect is architecture-/dataset-contingent (#27), parameters are not assumed portable (DGL-5), so the values are determined empirically (§4.3.2) — conceding this is a methodological choice, not a demonstrated optimum (NC-17).
- **Inherited limitations (SIR-2):** #23/#24 STARE single-dataset, 100% non-transferable, sensitivity anomaly; #27 accuracy-only, no CI.

---

### ACCEPTANCE CRITERIA

A draft is **strong** when:
- [ ] The dual-constraint rule is recalled (referenced to §2.1.2), not re-derived.
- [ ] LAB L-channel, 8×8 grid, redistribution + interpolation, stochastic p=0.8 / deterministic inference all specified with rationale.
- [ ] Two hyperparameters identified; fixed by Exp 2 (§4.3.2); DGL-5 stated.
- [ ] FIG-3.7 refinement documented; FOV-centroid pivot tied to §3.1.1 finding; governance/implementation divergence flagged [VERIFY].
- [ ] #23/#24 one self-cited thread (SIR-4/5); CFC-2.7 honored.
- [ ] Opening connects to §3.1.1 continuity (Stage 5).
- [ ] Every governance code satisfiable with a verbatim PART 3 quote.
- [ ] Word count 900–1,100, mid-band.

A **passable** draft merely satisfies word count and forbidden-claim compliance. Aim above passable.

---

### WRITING DIRECTIVES (section-specific)

- **Tense:** Present for the algorithm specification; past for cited findings.
- **Self-citation handling:** #23/#24 one thread; "previously published results" + extension.
- **Terminology watch:** "dual-constraint clip limit," "CLAHE," "LAB L-channel," "tile grid," "clip\_factor," "global\_threshold" per OD-3/§2.1.2 canonical forms.
- **Analytical depth:** Each paragraph gives the engineering rationale of a realization choice (why LAB L only; why stochastic; why parameters free); avoid restating §2.1.2 theory.
