# SECTION BRIEF
## §3.3.2 Ophthalmology-Specific Self-Supervised Pretraining of the CNN Backbone (integrated Arm)

**Chapter:** 3 — Methodology
**Section Function:** methodological (integrated-arm initialization specification; NEW v6.0.0; candidate methodological contribution)
**Word Count Target:** 900–1,100 words (aim 1,000, mid-band) (3 pages)
**Paragraph Count Target:** 6–8 paragraphs

> Generated per `prompts/section-brief-template.md` v6.0.0 · Binding reference: INVARIANTS.md v6.0.0 · Plan row: PLAN.md §6. Carries the §3.3.1 continuity note. **NEW v6.0.0 section; lit-GAP (#73 general survey only).** Specification, NOT results — the SSL (configs B/D) arm is not yet trained (project memory); no SSL performance may be reported.

---

### GOVERNANCE BINDINGS

**Hypotheses tested/supported:** Supports H-1 by specifying the integrated-arm *initialization* half of the composite independent variable. None tested.

**Paradigm positioning:** Not a P1/P2 attribution section, but the CNN-native SSL choice is the dissertation's own design rationale (INVARIANTS v6.0.0 §X). No Gulshan attribution.

**Primary claims advanced:** PC-1 at construct level (the integrated-arm pretraining source). No experimental claim.

**Sub-claims sourced here (SC-x.y):** None proven.

**Supporting contributions evidenced (SC-A…SC-H):** Defines (does not evidence) the ophthalmology-specific SSL initialization as a candidate methodological contribution.

**Forbidden claims (must not appear):**
- **CFC-2.8 (CENTRAL)** — the integrated arm differs from baseline along BOTH the preprocessing axis (vs stretch-resize) AND the pretraining axis (ophthalmology-SSL vs ImageNet); therefore no H-1 effect may be attributed to preprocessing alone, and equally none to pretraining alone. Permissible framing only: "the integrated preprocessing + ophthalmology-SSL configuration vs the baseline + ImageNet configuration." Single-factor attribution is forbidden and explicitly relinquished.
- **CFC-2.1** — no "SSL universally improves."
- **CFC-2.2** — no superiority over RETFound or named foundation models.
- **CFC-2.4** — no clinical-grade claim.
- **CFC-2.7** — self-pubs as-is.

**Non-claims (must not assert):** **NC-6/SB-3.1** (no optimality of SSL protocol); **NC-17**.

**Source interpretation rules:**
- **SIR-1 (lit-GAP central)** — #73 (Cheplygina survey) is general SSL/MIL/TL in medical imaging; do NOT amplify it into evidence for in-domain retinal SSL efficacy. Primary DINO/BYOL/SimCLR/MoCo-on-fundus sources are ABSENT — flag explicitly; mark candidate-original analysis / [UNSOURCED CLAIM] where a claim would otherwise read as sourced. Do not fabricate citations.

**Scope boundaries:**
- **DGL-6** — integrated arm = ophthalmology-specific SSL of the SAME CNN backbone on an unlabeled fundus corpus; no DR labels in pretraining; transfer not guaranteed.
- **SB-1.4 / IT-1** — fundus only.
- **SB-2.2** — if the SSL corpus includes private clinical data, note reproducibility limits (only if asserted; otherwise corpus = unlabeled fundus, generic).

**Evidence thresholds:** None evaluated. EH-3/EH-4 apply in §4.2.

---

### CONTENT SPECIFICATION

**Section objective:** Specify the integrated-arm initialization — ophthalmology-specific self-supervised pretraining of the same CNN backbone (ResNet-50 / EfficientNet-B3) on an unlabeled retinal fundus corpus, via a CNN-compatible SSL protocol from the DINO/BYOL/SimCLR/MoCo family selected empirically — explain why a CNN-native in-domain initialization (rather than RETFound's ViT-Large or plain ImageNet) is the design that isolates the architecture factor, and bind the whole specification under CFC-2.8 (composite IV) while honestly flagging the literature gap and the untrained status.

**Argumentative spine (required):**
- **Thesis of this section:** In the integrated arm of Experiment 1, the CNN backbone is initialized not from ImageNet but from ophthalmology-specific self-supervised pretraining on an unlabeled retinal fundus corpus, performed on the same backbone used in the baseline arm; this in-domain, CNN-native initialization is the pretraining half of the composite (preprocessing × pretraining) independent variable, so that the H-1 effect is, by construction, attributable only to the integrated configuration and not to preprocessing or pretraining alone (CFC-2.8).
- **Reasoning chain:**
  1. From §3.3.1: adaptation (head) is common; the *initialization* is what distinguishes the arms. Baseline arm = ImageNet; integrated arm = ophthalmology-specific SSL (DGL-6).
  2. SSL protocol: a CNN-compatible domain-adaptive self-supervised protocol from the DINO/BYOL/SimCLR/MoCo family, selected empirically and recorded once chosen; no DR labels used; objective = representation learning over fundus imagery (vascular topology, OD/macular morphology, retinal texture, illumination variability, imaging artifacts) (RESEARCH_ARCHITECTURE §4.2bis).
  3. Why CNN-native and not RETFound (ViT-Large): adopting a ViT foundation model would change both architecture and initialization, confounding the preprocessing contribution with an architecture change; a CNN-native SSL changes only the initialization, preserving the backbone across arms — this resolves AOQ-1→(b), restores the 2×2 symmetry (AOQ-4), and makes the RETFound license moot (AOQ-3) (INVARIANTS v6.0.0 §X).
  4. Input channels: because SSL is performed in-house, the encoder can be pretrained directly on the 4-channel tensor (AOQ-2 simplified); copy-and-mean-init fallback for channel 3.
  5. **CFC-2.8 (central):** the integrated arm differs from baseline along both preprocessing and pretraining; the composite IV is retained; single-factor attribution is forbidden and explicitly relinquished; permissible framing stated verbatim.
  6. **lit-GAP + honesty:** primary in-domain retinal SSL sources (DINO/BYOL/SimCLR/MoCo on fundus) are absent from the corpus (#73 general survey only); the in-domain SSL initialization is a candidate methodological contribution; the SSL arm has not yet been trained — this is a specification, not a result.
- **Conclusion / hand-off:** With the integrated-arm initialization specified, §3.3.3 specifies the fine-tuning schedule applied to the adapted, initialized network, and §3.3.4 the loss.

**Required content elements:**
1. Initialization distinguishes arms (baseline ImageNet vs ophthalmology-SSL); DGL-6.
2. SSL protocol family (DINO/BYOL/SimCLR/MoCo, empirically selected), unlabeled fundus corpus, no DR labels, representation objective.
3. CNN-native rationale vs RETFound/ViT (AOQ-1→b, AOQ-3 moot, AOQ-4 symmetry).
4. 4-channel SSL pretraining (AOQ-2 simplified) / channel-3 fallback.
5. CFC-2.8 composite IV — no single-factor attribution; permissible framing verbatim.
6. lit-GAP flag (#73-only; absent primary SSL sources; candidate contribution) + untrained-status honesty (spec, not result).

**Required tables/figures:** None (Resource IDs: —). Do not invent.
**Required equations:** None required (SSL objective named at family level; specific protocol selected empirically — do not fabricate a loss).

---

### SOURCE MAPPING

| Literature Card | Role | Specific Content to Extract |
|----------------|------|---------------------------|
| `cheplygina-2018.md` (**#73**) | general framing only (lit-GAP) | Survey of semi-/self-supervised, MIL, transfer learning under limited labels in medical imaging. **General** — not in-domain retinal SSL evidence (SIR-1). |

**⚠️ Coverage gaps (MATERIAL):** Primary in-domain retinal SSL sources (DINO/BYOL/SimCLR/MoCo on fundus; any ophthalmology-SSL foundation work beyond the retired RETFound) are ABSENT (Coverage Matrix §3.3.2 GAP). The specification is governance/own-design (INVARIANTS v6.0.0; RESEARCH_ARCHITECTURE §4.2bis). Flag the gap explicitly; mark candidate-original/[UNSOURCED CLAIM]; do not fabricate SSL citations or results.

---

### CROSS-REFERENCES

**Backward:** §3.3.1 (adaptation common; initialization distinguishes arms); §3.2.1 (CNN-native rationale, AOQ-2); §2.3.1/§2.3.2 (transfer/SSL theory, named-only §2.3.3 deferred); §1.3.2 (SSL in ophthalmics).
**Forward:** §3.3.3 (fine-tuning schedule); §4.2 (Exp 1, H-1, CFC-2.8 attribution at results time); §2.3.3 (deferred SSL theory, to be drafted after SSL-card acquisition).

---

### BOUNDARY WARNINGS

1. **CFC-2.8 (most important):** never attribute (future) H-1 effect to preprocessing alone OR pretraining alone; only the integrated configuration. State the permissible framing verbatim.
2. **lit-GAP honesty:** #73 is a general survey; flag absent primary SSL sources; candidate contribution; [UNSOURCED CLAIM] not fabrication.
3. **Untrained status:** spec, not result; do not present SSL performance (project memory: SSL B/D arm not trained; shipped Config-D is retired ImageNet artifact).
4. **CFC-2.2:** no superiority over RETFound/foundation models; RETFound discussed only as the retired design and the confound rationale.
5. **DGL-6:** no guaranteed transfer; no DR labels in pretraining.
6. **NC-6/SB-3.1:** SSL protocol not claimed optimal.

---

### COUNTER-ARGUMENT & INHERITED LIMITATIONS

- **Counter-position (examiner-facing):** that the composite IV is a weakness — it prevents attributing the effect to preprocessing, the dissertation's headline contribution. Answer: the v6.0.0 design deliberately accepts the composite IV (CFC-2.8) in exchange for an architecture-clean contrast (CNN-native SSL), and frames the H-1 claim at the integrated-configuration level; single-factor decomposition is explicitly out of scope and named as future ablation. This is a bounded, honest claim, not a flaw concealed.
- **Inherited limitations (SIR-2):** #73 general survey, not retinal-SSL-specific; the in-domain SSL initialization has no primary-source backing in the corpus and no trained result yet — both flagged.

---

### ACCEPTANCE CRITERIA

A draft is **strong** when:
- [ ] integrated-arm initialization (ophthalmology-SSL of same backbone, unlabeled fundus, no DR labels) specified; baseline = ImageNet contrast stated (DGL-6).
- [ ] SSL protocol family (DINO/BYOL/SimCLR/MoCo, empirically selected) and representation objective specified.
- [ ] CNN-native vs RETFound/ViT rationale given (AOQ-1→b, AOQ-3 moot, AOQ-4 symmetry); 4-channel SSL (AOQ-2).
- [ ] **CFC-2.8 composite IV stated with the permissible framing verbatim; single-factor attribution explicitly relinquished.**
- [ ] lit-GAP flagged (#73-only; absent primary SSL sources; candidate contribution); untrained status stated (spec not result); [UNSOURCED CLAIM]/candidate-original used, no fabrication.
- [ ] Opening connects to §3.3.1 continuity; hand-off to §3.3.3.
- [ ] Every governance code satisfiable with a verbatim PART 3 quote.
- [ ] Word count 900–1,100, mid-band.

A **passable** draft merely satisfies word count and forbidden-claim compliance. Aim above passable.

---

### WRITING DIRECTIVES (section-specific)

- **Tense:** Present for the design specification; conditional/future where the protocol is empirically selected or the arm is yet to be trained (state as "is selected empirically," "is to be trained," not as completed).
- **Self-citation handling:** none required (no self-card maps here); #73 general framing only.
- **Terminology watch:** "self-supervised pretraining," "in-domain," "ophthalmology-specific," "CNN-native," "composite independent variable," "DINO/BYOL/SimCLR/MoCo" per INVARIANTS/RESEARCH_ARCHITECTURE.
- **Analytical depth:** Justify the design (why in-domain CNN-native; why composite IV is accepted); be explicit and honest about the gap and the untrained status — this is the section's integrity test.
