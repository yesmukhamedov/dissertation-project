# SECTION BRIEF
## §3.3.3 Two-Stage Fine-Tuning Protocol Design

**Chapter:** 3 — Methodology
**Section Function:** methodological (training-schedule specification)
**Word Count Target:** 600–800 words (aim 700, mid-band) (2 pages)
**Paragraph Count Target:** 4–6 paragraphs

> Generated per `prompts/section-brief-template.md` v6.0.0 · Binding reference: INVARIANTS.md v6.0.0 · Plan row: PLAN.md §6. Carries the §3.3.2 continuity note.

---

### GOVERNANCE BINDINGS

**Hypotheses tested/supported:** None. **the training-method comparison hypothesis was DROPPED in V3** (it then carried the label H-3; that label now denotes the unrelated domain-distance hypothesis of §4.4) — the frozen-vs-progressive strategies are a training *method*, not a tested hypothesis. State this.

**Paradigm positioning:** Not invoked.

**Primary claims advanced:** PC-1 at construct level (schedule applied identically across arms). No experimental claim.

**Forbidden claims (must not appear):** CFC-2.2 (no superiority); CFC-2.7 (#19/#21 as-is); CFC-2.1 (no universality).

**Non-claims (must not assert):** SIR-7 (B0 not generalized); H-3 not reinstated.

**Source interpretation rules:** SIR-1; SIR-4 (#19/#21 prior own work); SIR-5 (#19/#21 one thread); SIR-7.

**Scope boundaries:** DGL-6 (transfer not guaranteed); SB-3.1 (same schedule both backbones); IT-1.

**Evidence thresholds:** None evaluated.

---

### CONTENT SPECIFICATION

**Section objective:** Specify the two-stage fine-tuning schedule applied — identically across both backbones and both arms — to the adapted, initialized network: Stage 1 trains the new classification head with the backbone frozen, Stage 2 progressively unfreezes upper backbone layers for joint fine-tuning; frame the two strategies as a training method (H-3 dropped), grounded in the candidate's prior frozen-vs-fine-tuned record (#19/#21, one thread), and applied uniformly so the factorial stays clean (EH-4).

**Argumentative spine (required):**
- **Thesis of this section:** The adapted network — under either initialization — is trained by a two-stage fine-tuning schedule (frozen-base head training, then progressive unfreezing), applied identically across configurations; the choice between frozen and progressive strategies is a training-method decision informed by prior own work, not a hypothesis under test (H-3 having been dropped).
- **Reasoning chain:**
  1. Two-stage schedule: Stage 1 — backbone frozen, train head only (stabilizes the new head against pretrained features); Stage 2 — progressively unfreeze upper layers for joint fine-tuning (adapts higher-level features to DR) (RESEARCH_ARCHITECTURE §4.3).
  2. Prior empirical basis: the candidate's frozen-vs-progressive record on EfficientNetB0 (F1 ≈ 0.62 frozen → 0.74 fine-tuned; one thread #19/#21; SIR-4/5; B0-only SIR-7) motivates the progressive schedule.
  3. H-3 dropped (V3): the frozen/progressive comparison is a training method here, not a tested hypothesis; no H-3 claim is made or reinstated.
  4. Applied identically across both backbones and both arms (baseline ImageNet, ophthalmology-SSL) — keeps the factorial a clean contrast (EH-4); transfer/fine-tuning efficacy not guaranteed (DGL-6).
- **Conclusion / hand-off:** With the schedule fixed, §3.3.4 specifies the loss the schedule optimizes (weighted focal loss for the imbalanced ordinal task).

**Required content elements:**
1. Two-stage schedule (frozen head → progressive unfreezing); rationale per stage.
2. Prior frozen-vs-progressive record (#19/#21, one thread; SIR-4/5/7).
3. H-3 dropped → training method not hypothesis.
4. Identical across arms/backbones (EH-4); DGL-6.

**Required tables/figures:** None (Resource IDs: —).
**Required equations:** None.

---

### SOURCE MAPPING

| Literature Card | Role | Specific Content to Extract |
|----------------|------|---------------------------|
| `yesmukhamedov-conf.md` (**#19**, 🔹SELF) | self: frozen-vs-fine-tuned record | F1 ≈ 0.62 frozen → 0.74 fine-tuned, EfficientNetB0 (one thread #21, SIR-5; B0-only SIR-7). |
| `yesmukhamedov-kbtu.md` (**#21**, 🔹SELF) | self: same thread | precision 65%→75% fine-tuned (one thread with #19). |

**⚠️ Coverage gaps:** Schedule is governance/own-design (RESEARCH_ARCHITECTURE §4.3). Prior record is self-work (SIR-4/5/7). No fabricated metrics; the cited 0.62/0.74 are the source's reported figures, framed as prior own work, not as present results.

---

### CROSS-REFERENCES

**Backward:** §3.3.1 (adaptation); §3.3.2 (initialization); §2.3.2 (frozen vs progressive fine-tuning theory).
**Forward:** §3.3.4 (loss); §4.2 (Exp 1).

---

### BOUNDARY WARNINGS

1. **H-3 dropped:** never present frozen-vs-progressive as a tested hypothesis or reinstate H-3.
2. **SIR-4/5/7:** #19/#21 one thread, prior own work, B0-only (not generalized).
3. **SB-3.1 / EH-4:** schedule identical across arms/backbones.
4. **DGL-6:** transfer/fine-tuning efficacy not guaranteed.
5. **CFC-2.2:** the 0.62→0.74 is a source finding within one self-thread, not a dissertation superiority claim.

---

### COUNTER-ARGUMENT & INHERITED LIMITATIONS

- **Counter-position:** that the frozen-vs-progressive choice should itself be an experiment. Answer: it was studied in prior own work and H-3 has been dropped; here it is a fixed training method so the experimental degrees of freedom are spent on the preprocessing/pretraining contrast, not on the schedule. Concede the prior record is B0-only and not generalized (SIR-7).
- **Inherited limitations (SIR-2):** #19/#21 B0-only, APTOS/private, frozen-vs-fine-tuned only.

---

### ACCEPTANCE CRITERIA

A draft is **strong** when:
- [ ] Two-stage schedule specified with per-stage rationale.
- [ ] Prior frozen-vs-progressive record cited as one self-thread (SIR-4/5), B0-only (SIR-7).
- [ ] H-3-dropped / training-method framing explicit.
- [ ] Identical across arms/backbones (EH-4); DGL-6 stated.
- [ ] Opening connects to §3.3.2 continuity; hand-off to §3.3.4.
- [ ] Every governance code satisfiable with a verbatim PART 3 quote.
- [ ] Word count 600–800, mid-band.

A **passable** draft merely satisfies word count and forbidden-claim compliance. Aim above passable.

---

### WRITING DIRECTIVES (section-specific)

- **Tense:** Present for the schedule spec; past for the prior self-work.
- **Self-citation handling:** #19/#21 one thread; prior own work + extension (schedule adopted as method).
- **Terminology watch:** "frozen-base strategy" / "frozen-feature" vs "progressive fine-tuning" disambiguation per GLOSSARY (§2.3.2); "two-stage fine-tuning."
- **Analytical depth:** Explain each stage's purpose and why the schedule is fixed not tested; avoid restating §2.3.2 theory.
