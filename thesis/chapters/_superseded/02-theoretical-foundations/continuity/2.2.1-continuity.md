# CONTINUITY NOTE — §2.2.1 Convolution, Pooling, and Feature Extraction Operations

**Version:** 6.0.0 · Produced as PART 2 of the §2.2.1 writing session · Carry into the §2.2.2 session (step c).

---

**Key concepts established:**
- **Convolution**: learnable filters over local receptive fields with **weight sharing** → translation-equivariant **feature maps**; parameter efficiency vs dense connection (AlexNet #65 established large-scale CNN viability).
- **Nonlinearity (ReLU)** and **pooling** (max-pooling, downsampling → local invariance + receptive-field growth); the conv→nonlinearity→pooling block stacked into a **feature hierarchy** (low-level edges/textures → abstract patterns; FIG-2.2).
- **Depth → trainability problem (degradation)**, resolved by **residual learning** (#66 He, identity shortcuts; ResNet-50 = dissertation backbone) and **dense connectivity** (#67 Huang, feature reuse, parameter efficiency) — two connectivity mechanisms, not ranked.
- **Fundus tension**: pooling discards small-feature resolution; even attention-augmented, pooling-limited architectures (#08 EAD-Net) nearly fail on microaneurysms → architecture alone does not solve small-low-contrast-lesion detection → **model = preprocessing + CNN** (CENTRAL_THESIS), and CFC-2.8 (no isolated-factor attribution) pre-emptively honored.
- SB-3.1: backbones are established choices, **not** claimed optimal.

**Terms introduced (now available without re-definition):** convolution, learnable filter, local receptive field, weight sharing, feature map, translation equivariance, ReLU, max-pooling, downsampling, feature hierarchy, residual learning / identity shortcut, ResNet-50, dense connectivity / DenseNet, feature reuse, parameter efficiency, dilated convolution, attention (named via #08).

**Argument thread:** The CNN feature-extraction substrate is fixed. The active thread is now *how that representation is trained* — specifically under the severe class imbalance of DR datasets. The chapter moves from feature extraction to optimization/loss.

**Final topic:** The last paragraph states that feature extraction yields the representation the classifier head/loss act on, and hands off to §2.2.2 (loss functions, class imbalance). Reader is primed for loss/optimization theory.

**Setup for next section (§2.2.2 — Loss Functions and Optimization for Imbalanced Medical Datasets):** Open by taking the CNN feature representation as established and turn to the loss that trains it. Sources: #01 (Pratt — DR CNN, class imbalance), #19 (`yesmukhamedov-conf`, 🔹SELF), #21 (`yesmukhamedov-kbtu`, 🔹SELF), #39 (Senapati — survey, imbalance gap), #40 (Araf — cost-sensitive-learning survey), #42 (Arora — EfficientNetB0). **Governance:** **SIR-4** (#19/#21 self — prior own work) and **SIR-5** (#19 and #21 report *overlapping* experiments — treat as ONE self-cited thread, not independent confirmation); **SIR-7** (no generalization of EfficientNetB0 results to the efficient-CNN class). Develop cross-entropy → class-weighted / focal loss; tie to focal loss (γ=2, α=inverse-frequency, FIG-3.12) and SC-1.4 (class imbalance → weighted F1/κ). No equations mandated but focal-loss / weighted-CE expressions appropriate.

**Unresolved threads:**
- Regularization (dropout, batch normalization, data augmentation — all introduced by #65) → **§2.2.3** (deliberately deferred from §2.2.1).
- 4-channel input adaptation (RGB + FOV mask) at first convolution → **§3.3.1 / §3.2**.
- ResNet-50 / EfficientNet-B3 architecture design specifics → **§3.2**.
