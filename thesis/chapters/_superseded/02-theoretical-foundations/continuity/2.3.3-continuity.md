# CONTINUITY NOTE — §2.3.3 In-Domain Self-Supervised Pretraining for Retinal Imaging

**Version:** 6.0.0 · Produced as PART 2 of the §2.3.3 writing session (deferral lifted 2026-06-16) · **Structural note:** §2.3.3 was written out of sequence to close the deferred gap. In the assembled order it sits **§2.3.2 → §2.3.3 → §2.4.1**. §2.4.1 already exists and its opening bridges from §2.3.2's "experimentally-exercised foundations complete; in-domain SSL (§2.3.3) treated separately" framing — that bridge still holds, so **§2.4.1 needs no rewrite**. The true forward hand-off of §2.3.3 is to **§3.3.2** (methodology) and **§4.2** (H-1 results).

---

**Key concepts established:**
- **In-domain (ophthalmology-specific) self-supervised pretraining** of the CNN backbone on an unlabelled retinal corpus → retina-aware initialization before any DR label is seen; the integrated arm's pretraining half (vs ImageNet-supervised baseline).
- **SSL principle and design space** (from #92, #73): solve a label-free pretext objective on unlabelled data, then fine-tune. Families: **contrastive** (SimCLR #86 NT-Xent; MoCo #87 momentum encoder + queue), **negative-free** (BYOL #88; SimSiam #91 stop-gradient anti-collapse), **self-distillation** (DINO #89), **masked generative** (MAE #90, 75% masking). Reported on natural images (ImageNet) → design space, not retinal performance (SIR-3).
- **In-domain > natural-image evidence:** MICLe (Azizi #85, non-retinal: +6.7% derm top-1, +1.1% CXR mAUC over ImageNet baseline — extrapolation caveat) and **RETFound (Zhou #84, keystone)**: MAE/ViT on 1.6M retinal images; the source reported it outperformed SimCLR/DINO/MoCo-v3, largest gains under limited labels. Two independent lines (SIR-5).
- **CNN-vs-ViT nuance (key analytical move):** RETFound/MAE/DINO are ViT; the dissertation's backbones are CNNs (ResNet-50/EfficientNet-B3, §3.2.1). The convolution-native in-domain path rests on the ResNet-compatible contrastive/negative-free family + MICLe; RETFound establishes the *principle*, the CNN realization is chosen empirically.
- **Bounding:** DGL-6 (transfer benefit not guaranteed, empirical only); **CFC-2.8** (composite IV — SSL initialization reported only within the integrated configuration, never independently attributable, never paired with "preprocessing dominates"); no clinical-validation register (CFC-2.4).
- **Inherited limitations (SIR-2):** RETFound retrospective, corpus provenance uncontrolled, **no preprocessing ablation** (cannot adjudicate preprocessing-vs-pretraining); MICLe non-retinal.

**Terms introduced (now available without re-definition):** self-supervised pretraining, pretext objective, in-domain / ophthalmology-specific pretraining, contrastive learning, NT-Xent, momentum encoder, negative-free SSL, stop-gradient, self-distillation, masked autoencoder / masked image modelling, foundation model, label efficiency, multi-instance contrastive (MICLe).

**Argument thread:** The transfer-learning foundations are now complete on both axes — *how* to adapt (frozen/progressive, §2.3.2) and *from where* to initialize (natural-image vs in-domain SSL, §2.3.3). The pretraining-source axis is framed as the partner of preprocessing (composite IV), not its rival, pre-empting the "pretraining is the real lever" counter-position.

**Final topic:** The last paragraph reframes pretraining source as preprocessing's partner under CFC-2.8, denies any clinical-validation claim, and points to §3.3.2 (operational procedure) and Chapter 4 (H-1 evaluation).

**Setup for next section:** No new prose section is gated on §2.3.3 (the TOC-next §2.4.1 already exists and remains consistent). The dependents are **§3.3.2** — which must instantiate the corpus, the convolution-native SSL objective, and the two-stage fine-tuning (§3.3.3), carrying the **narrowed [UNSOURCED CLAIM]** marker for the untrained CNN-native-on-4-channel-V5 configuration — and **§4.2**, where the integrated arm's contribution is evaluated as part of the composite IV.

**Unresolved threads:**
- CNN-native in-domain SSL on the 4-channel V5 input is **spec-not-result** (shipped Config-D = retired ImageNet artifact; SSL B/D arm untrained) → §3.3.2 marker + §4.2 results, not claimable until run. See [[config-d-pretraining]], [[preprocessing-od-fovea-polar]].
- Composite-IV reporting discipline (CFC-2.8) carries into every H-1 results statement (§4.2, §5.x).
- RETFound's no-preprocessing-ablation limitation is the hinge for the preprocessing-vs-pretraining non-rivalry argument → reused in §4.2 discussion.
