# CONTINUITY NOTE — §2.1.3 Spatial Filtering and Noise Reduction Methods

**Version:** 6.0.0 · Produced as PART 2 of the §2.1.3 writing session · Carry into the §2.2.1 session (step c).

---

**Key concepts established:**
- The **noise-suppression vs structure-preservation trade-off** as the central constraint on low-level fundus preprocessing (low-contrast targets — microaneurysms, thin vessels — make indiscriminate smoothing dangerous).
- **Bilateral filtering** (Tomasi & Manduchi, 1998): similarity-weighted averaging combining geometric closeness $c(\xi-x)$ and photometric similarity $s(f(\xi),f(x))$; non-iterative, edge-preserving.
- **Non-local means (NLM)** (Buades et al., 2011): patch self-similarity averaging across the whole image; exploits structural redundancy to denoise while keeping fine detail.
- Both are **qualitative, foundational method papers** (no quantitative/retinal validation) — cited for principle, not DR gains (SIR-1/SIR-2).
- **non-adoption**: neither is a pipeline stage; the noise-vs-structure trade-off is instead managed by the contrast-limiting clip (Stage 5) and adaptive flat-field correction (Stage 4) — design rationale, not a superiority claim.

**Terms introduced (now available without re-definition):** linear/domain-only smoothing, bilateral filtering, geometric closeness, photometric similarity, edge-preserving smoothing, non-local means (NLM), patch self-similarity, content-adaptive filtering, structural redundancy.

**Argument thread:** The §2.1 low-level image-operation block is now complete (contrast enhancement + noise-reduction principle). The chapter pivots from operations that *prepare* the image to operations that *consume* it — the CNN. The active thread is the transition into convolutional-network theory.

**Final topic:** The last paragraph closes the §2.1 image-operation foundations and transitions explicitly to §2.2.1 (convolution, pooling, feature extraction). Reader is primed to move from preprocessing theory to CNN mechanics.

**Setup for next section (§2.2.1 — Convolution, Pooling, and Feature Extraction Operations):** Open by taking the preprocessed-image foundations as complete and turn to how a CNN extracts features from that input. Sources: #08 (Wan — segmentation, feature extraction context), #65 (Krizhevsky AlexNet), #66 (He ResNet — dissertation backbone), #67 (Huang DenseNet). FIG-2.2 is **✅ AVAILABLE** (`defense/presentation/assets/architecture/07_cnn/cnn_architecture.png`) — reference by Resource ID, not embedded. Governance: GLOSSARY (CNN, convolution, pooling, feature map definitions). Establish the convolution → nonlinearity → pooling → hierarchy-of-features account, grounding ResNet-50 (#66) and the residual/dense-connectivity lineage; foreshadow §2.2.2 (loss) and §3.2 (architectures). No self-pub here (so SIR-4 not engaged in §2.2.1, but #19/#21/#24 self appear in §2.2.2/§2.2.3).

**Unresolved threads:**
- 4-channel input (RGB + FOV mask, Stage 3) interaction with the first convolution → detailed in §3.3.1 / §3.2; note here only as forward reference if relevant.
- noise handling specifics (flat-field σ=0.07·D, Stage 4) → §3.1.1; σ sweep (H-2) → §4.3.3.
