# CONTINUITY NOTE — §2.3.1 Feature Transferability Across Visual Domains

**Version:** 6.0.0 · Produced as PART 2 of the §2.3.1 writing session · Carry into the §2.3.2 session (step c).

---

**Key concepts established:**
- **Layer-dependent transferability** (Yosinski #71): lower layers learn generic structure (edges/colors/textures) and transfer near-perfectly; higher layers specialize; transferability **decays with source–target domain distance**; fine-tuned transferred features can beat training on the target alone.
- **Source-quality → transfer** (Kornblith #72): ImageNet accuracy predicts transfer (r=0.99 fixed, 0.96 fine-tuning); **fine-tuning > fixed-feature transfer** (179/192); 17× convergence speed-up; but benefits diminish with divergence from source labels and more target data.
- **Natural-image→fundus = large domain gap**: ImageNet transfer to DR is a cross-domain shift (limited-label medical regime, #73); transfer useful but bounded (#04: AUC 0.94 EyePACS / 0.89 Messidor-2 from 2% labels).
- **DGL-6 two regimes**: baseline arm = ImageNet (cross-domain); integrated arm = ophthalmology-specific in-domain SSL of the same CNN backbone (reduces domain gap); transfer **not guaranteed, evaluated empirically**.
- **CFC-2.8**: integrated arm differs in both preprocessing AND pretraining axes → no isolated-axis attribution of any future H-1 effect.
- In-domain SSL theory itself is **DEFERRED to §2.3.3** (named only here).

**Terms introduced (now available without re-definition):** feature transferability, layer-wise transferability, general vs task-specific features, domain distance / domain gap, fixed-feature transfer, fine-tuning (transfer mode), source-model quality, in-domain self-supervised pretraining (named), two pretraining regimes (ImageNet vs ophthalmology-SSL).

**Argument thread:** Transferability is real but bounded (layer- and domain-dependent), and the natural-image→fundus gap motivates the two-regime H-1 design under DGL-6. The open question is the **adaptation strategy** for transferred weights — freeze vs progressively fine-tune — which §2.3.2 resolves.

**Final topic:** The last paragraph poses the frozen-vs-fine-tuning adaptation question and hands it to §2.3.2. Reader is primed for the adaptation-strategy comparison.

**Setup for next section (§2.3.2 — Frozen-Layer versus Progressive Fine-Tuning Strategies):** Open by taking bounded transferability and the two regimes as given, and resolve *how* transferred weights are adapted. Sources: #19 (`yesmukhamedov-conf`, 🔹SELF), #21 (`yesmukhamedov-kbtu`, 🔹SELF — same experiment, SIR-5), #71 (Yosinski — co-adaptation/fine-tuning), #72 (Kornblith — fine-tuning > fixed features), #76 (Ganin — DANN/domain-adversarial, domain-invariant features). **Governance:** GLOSSARY (canonical fine-tuning terms — "frozen-base strategy"/"feature extraction" vs "progressive fine-tuning"; disambiguate per verification §C); SIR-4/5 (#19/#21 one thread). The self-work's central result (frozen Method 1: F1 0.62; fine-tuned Method 2: F1 0.74, +12 pp) is the empirical anchor; frame as prior own work extended by the two-stage protocol (§3.3.3). NOTE: **after §2.3.2, the loop SKIPS §2.3.3 (deferred) and proceeds to §2.4.1** — the §2.3.2 continuity must hand off to §2.4.1.

**Unresolved threads:**
- Frozen vs progressive fine-tuning adaptation → **§2.3.2** (immediate next).
- In-domain SSL theory → **§2.3.3 (DEFERRED this pass — awaiting DINO/BYOL/SimCLR/MoCo-on-fundus cards; #73 general survey only)**; methodology §3.3.2.
- Two-stage fine-tuning protocol design → §3.3.3.
- H-1 composite-IV evaluation → §4.2 (not claimable until run).
