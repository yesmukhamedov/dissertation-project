# CONTINUITY NOTE — §2.2.3 Regularization Techniques: Dropout, Batch Normalization, and Data Augmentation

**Version:** 6.0.0 · Produced as PART 2 of the §2.2.3 writing session · Carry into the §2.3.1 session (step c).

---

**Key concepts established:**
- Regularization at **three levels**: weight-level (**dropout** — prevents co-adaptation, #65 origin, dropout 0.5/0.4 in DR CNNs; **batch normalization** — activation normalization, mild regularizer, enhanced-arch component, absent in baseline; L2), training-control (**early stopping** on validation κ; **LR scheduling** / ReduceLROnPlateau), and data-level (**data augmentation**).
- **Data augmentation's dual role**: regularizer (invariance, effective-data expansion) AND imbalance mitigation (augment minority classes) — bridges §2.2.2 / SC-1.4.
- **Stage 6** (OD-3): train-only unified affine + ColorJitter (brightness/contrast/saturation/hue) + Gaussian noise + JPEG compression, before normalization (renders in §3.1.3, FIG-3.8).
- **Honest framing**: regularization mitigates but does not eliminate overfitting under limited/imbalanced data (self-work residual gap; "would require additional data and class balancing").
- SIR-5 (#19/#21 one thread), SIR-7 (no EfficientNetB0 generalization).

**Terms introduced (now available without re-definition):** dropout, co-adaptation, batch normalization, L2 regularization, early stopping, learning-rate scheduling (ReduceLROnPlateau), data augmentation, ColorJitter, label-preserving transformation, invariance.

**Argument thread:** The §2.2 CNN-training block is complete (feature extraction → loss under imbalance → regularization). The active thread now moves to **initialization**: what the network knows before fine-tuning — transfer learning and feature transferability — which is the §2.3 arc and is foundational to the H-1 composite-IV pretraining axis (ImageNet baseline vs ophthalmology-SSL integrated arm).

**Final topic:** The last paragraph declares the §2.2 fundamentals complete and transitions explicitly to §2.3 (transfer learning / feature transferability / how the network is initialized). Reader is primed for transfer-learning theory.

**Setup for next section (§2.3.1 — Feature Transferability Across Visual Domains):** Open by taking the trained-and-regularized CNN as given and ask what its weights should be *initialized* to. Sources: #04 (Arrieta — semi-supervised, 2% labels, transfer context), #71 (Yosinski — layer-wise feature transferability: lower layers general, higher task-specific; fine-tuning > scratch), #72 (Kornblith — ImageNet accuracy predicts transfer, r=0.99 fixed / 0.96 fine-tuning), #73 (Cheplygina — SSL/MIL/TL medical survey). **Governance:** **DGL-6** (transfer not guaranteed; baseline arm = ImageNet, integrated arm = ophthalmology-specific SSL — state the two regimes; transfer empirically evaluated, not assumed). Establish the natural-image→medical domain gap and layer-wise transferability; foreshadow §2.3.2 (frozen vs fine-tuning) and §2.3.3 (in-domain SSL — NOTE: §2.3.3 is DEFERRED in this pass pending SSL cards). No self-pub in §2.3.1 (so SIR-4 not engaged there); #19/#21 return in §2.3.2.

**Unresolved threads:**
- Frozen vs progressive fine-tuning (the #19/#21 central result) → **§2.3.2**.
- In-domain self-supervised pretraining for retinal imaging → **§2.3.3 (DEFERRED this pass — awaiting DINO/BYOL/SimCLR/MoCo-on-fundus cards; #73 general survey only)**; §3.3.2 methodology.
- Focal-loss formalization → §3.3.4; two-stage fine-tuning protocol → §3.3.3.
- Stage 6 augmentation renders → §3.1.3 (FIG-3.8).
