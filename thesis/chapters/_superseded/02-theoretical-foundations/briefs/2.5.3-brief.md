# SECTION BRIEF
## §2.5.3 ALO and IoU as Quantitative Explainability Metrics

**Chapter:** 2 — Theoretical Foundations
**Section Function:** theoretical (metric formalization)
**Word Count Target:** 700–900 words (aim 800, middle of band) (2.5 pages)
**Paragraph Count Target:** 6–7 paragraphs

> Generated per `prompts/section-brief-template.md` v6.0.0 · Binding reference: INVARIANTS.md v6.0.0 · Plan row: PLAN.md §5. Carries the §2.5.2 continuity note into its opening.

---

### GOVERNANCE BINDINGS

**Hypotheses tested/supported:** None tested. Defines the metrics for H-5 (Exp 4); ALO is H-5's primary explainability metric, IoU secondary.
**Paradigm positioning:** None / not invoked. #63/#64 N/A object-detection references.
**Primary claims advanced:** None of PC-x (PC-7 validated in Exp 4).
**Sub-claims sourced here:** None.
**Supporting contributions evidenced:** None.

**Forbidden claims (must not appear):**
- **CFC-2.4** — no clinical-validation register; overlap metrics are interpretability measures.
- **CFC-2.2** — no superiority claim (ALO_preproc > ALO_baseline is an Exp-4 hypothesis, not asserted here).
- **CFC-2.1** — no universal claim.

**Non-claims (must not assert):**
- **NC-14 (central):** ALO/IoU overlap quantifies attention-lesion correspondence as interpretability, NOT clinical localization; IoU is a borrowed overlap metric, NOT a detection benchmark.

**Source interpretation rules:**
- **SIR-1** (non-amplification) — #63/#64 cited for the IoU/overlap definitions they provide.
- **SIR-2** (limitation inheritance, at first citation) — #63 (GIoU) arXiv, object-detection, no CI/medical validation; #64 (PASCAL VOC) object-detection benchmark descriptor, non-medical, predates DL-era standards.

**Scope boundaries:** define **ALO as PRIMARY**, **IoU as SECONDARY**; frame IoU as an overlap metric **borrowed** from object detection, not a detection benchmark (LITERATURE_INDEX note 47). IT-1.
**Evidence thresholds:** None (H-5 evaluation in §4.6).

---

### CONTENT SPECIFICATION

**Section objective:** Formalize the two quantitative explainability metrics — Attention–Lesion Overlap (ALO, primary) and Intersection-over-Union (IoU, secondary) — that measure correspondence between a Grad-CAM map and pixel-level lesion masks, justify ALO's primacy given the coarse/class-discriminative nature of attention maps, and bind both as interpretability measures (NC-14), with IoU explicitly a borrowed overlap metric rather than a detection benchmark.

**Argumentative spine (required):**
- **Thesis of this section:** The attention-plausibility judgment of §2.5.2 is quantified by overlap between the Grad-CAM activation region and the annotated lesion mask; the Attention–Lesion Overlap (ALO), measuring the fraction of the lesion covered by attention, is the primary metric because it matches the interpretability question and tolerates the inherent coarseness of attention maps, while the symmetric Intersection-over-Union — borrowed from object detection — is retained as a stricter secondary measure; both are interpretability metrics, not detection benchmarks (NC-14).
- **Reasoning chain:**
  1. The need: turn "does attention fall on clinically relevant structures?" into a number by overlapping the (thresholded) Grad-CAM region with the lesion mask.
  2. ALO (primary): $\mathrm{ALO} = \dfrac{\mathrm{area}(\text{GradCAM} \cap \text{lesion})}{\mathrm{area}(\text{lesion})}$ — asymmetric, lesion-referenced; the fraction of the annotated lesion the attention covers; H-5's primary metric.
  3. IoU (secondary): $\mathrm{IoU} = \dfrac{\mathrm{area}(\text{GradCAM} \cap \text{lesion})}{\mathrm{area}(\text{GradCAM} \cup \text{lesion})}$ — symmetric; penalizes both missed lesion and attention spilling beyond it. Provenance in object detection: PASCAL VOC defines a correct detection as IoU > 0.5 (Everingham et al., 2010); GIoU (Rezatofighi et al., 2019) extends IoU to retain gradient for non-overlapping regions.
  4. Why ALO primary: Grad-CAM maps are coarse and class-discriminative (§2.5.1–§2.5.2) and systematically extend beyond precise lesion boundaries; a symmetric IoU penalizes that inherent coarseness as if it were localization error, whereas ALO measures lesion coverage — the interpretability question of interest. Hence ALO primary, IoU secondary/supporting.
  5. NC-14: both are interpretability measures; high ALO means attention covers the lesion, not that pathology was clinically localized; IoU is borrowed, not used as a detection threshold/benchmark (index note 47).
- **Conclusion / hand-off:** ALO (primary) and IoU (secondary) formalize attention-lesion overlap as interpretability measures, completing the explainability formalization (§2.5); the H-5 test (ALO_preproc > ALO_baseline) is reserved for Exp 4 (§4.6). The chapter's final theoretical strand — the image-quality metrics that evaluate preprocessing directly — follows in §2.6.

**Required content elements:**
1. The quantification need (from §2.5.2).
2. ALO definition + equation (primary, lesion-referenced).
3. IoU definition + equation (secondary, symmetric); provenance (#64 IoU>0.5; #63 GIoU).
4. Justification of ALO primacy over IoU given coarse attention maps.
5. NC-14: interpretability not detection benchmark; IoU borrowed (index note 47).
6. Hand-off to §4.6 (H-5) and §2.6.

**Required tables/figures:** None.
**Required equations:** ALO (primary), IoU (secondary) — Markdown/LaTeX, per INVARIANTS H-5.

---

### SOURCE MAPPING

| Literature Card | Role | Specific Content to Extract |
|----------------|------|---------------------------|
| `everingham-2010-pascal-voc.md` (**#64**) | IoU provenance: detection-correctness criterion | "area(B_p ∩ B_gt)/area(B_p ∪ B_gt) > 0.5" for a correct detection (§8); AP definition; object-detection benchmark, non-medical (SIR-2). |
| `rezatofighi-2019-giou.md` (**#63**) | IoU as metric; GIoU extension | "IoU … if two objects do not overlap, the IoU value will be zero" (§1); GIoU "a new metric for comparing any two arbitrary shapes," gradient in non-overlapping cases (§3); arXiv, object-detection, no CI (SIR-2). |
| INVARIANTS H-5 (governance) | ALO/IoU definitions for the dissertation | ALO = area(GradCAM ∩ lesion)/area(lesion) [primary]; IoU = area(GradCAM ∩ lesion)/area(GradCAM ∪ lesion) [secondary]; ALO_preproc > ALO_baseline is the H-5 condition (tested in §4.6). |

**⚠️ Coverage gaps:** ALO is defined by governance (H-5), not by an external card — attributed as the dissertation's metric. IoU provenance is object-detection (#63/#64); the dissertation's *use* of IoU as a lesion-overlap interpretability measure (not a detection benchmark) is dissertation-original framing per index note 47.

---

### CROSS-REFERENCES

**Backward:** §2.5.1 (Grad-CAM map, coarse resolution); §2.5.2 (attention plausibility; activation≠pathology).
**Forward:** §2.6 (image-quality metrics); §3.4.1 (evaluation framework); §4.6 (Exp 4, H-5: ALO primary/IoU secondary, IDRiD masks + Clinical qualitative); §5.1 (explainability results).

---

### BOUNDARY WARNINGS

1. **ALO primary / IoU secondary:** State explicitly per H-5; justify ALO primacy.
2. **NC-14 / index note 47:** IoU is a borrowed overlap metric, NOT a detection benchmark; both are interpretability measures, not clinical localization.
3. **CFC-2.2:** ALO_preproc > ALO_baseline is the Exp-4 hypothesis, not asserted here.
4. **SIR-2:** #63/#64 object-detection, non-medical; caveat at first citation.
5. **CFC-2.4:** No clinical-validation register.

---

### COUNTER-ARGUMENT & INHERITED LIMITATIONS

- **Counter-position:** "IoU is the standard overlap metric, so it should be primary." The section refutes this for the explainability use-case: IoU's symmetry penalizes the inherent coarseness of Grad-CAM maps (which extend beyond lesion boundaries by construction) as if it were localization error, so it understates genuine attention–lesion correspondence; ALO, measuring lesion coverage, is the metric matched to the interpretability question — hence primary, with IoU retained as a stricter secondary check. (Framed as metric-design rationale.)
- **Inherited limitations (SIR-2):** #63 arXiv, object-detection, no CI/medical validation; #64 object-detection benchmark descriptor, non-medical, predates DL-era standards.

---

### ACCEPTANCE CRITERIA

Strong when:
- [ ] ALO and IoU defined with equations; ALO explicitly primary, IoU secondary (H-5).
- [ ] ALO primacy justified via coarse/class-discriminative attention maps.
- [ ] IoU provenance (object detection, #64 IoU>0.5; #63 GIoU) given; framed as borrowed overlap metric, NOT detection benchmark (NC-14, index note 47).
- [ ] Both bound as interpretability measures; ALO_preproc>ALO_baseline reserved for §4.6.
- [ ] SIR-2 caveats at first citation.
- [ ] Opening connects to §2.5.2; hand-off to §2.6.
- [ ] 800 words (mid-band).
- [ ] Every governance code satisfiable with a verbatim PART 3 quote.

---

### WRITING DIRECTIVES (section-specific)

- **Tense:** Present for metric definitions; past for cited object-detection findings.
- **Self-citation handling:** None.
- **Terminology watch:** "Attention–Lesion Overlap (ALO)," "Intersection-over-Union (IoU)," "lesion mask," "overlap metric." Keep "detection" strictly in the object-detection-provenance context; the dissertation's use is interpretability overlap, not detection.
- **Analytical depth:** Metric-design section; the analytical core is the *justification* of ALO primacy and the NC-14 framing — analysis over enumeration.
