# SECTION BRIEF
## §6.2.2 Inference Module with Model Selection Logic

**Chapter:** 6 — Architecture of an Automated DR Screening System for Resource-Limited Environments
**Section Function:** methodological / design-specification (module internals)
**Word Count Target:** 700–900 words — **aim MID-BAND (780–820)**
**Paragraph Count Target:** 5–6 paragraphs (no new table)

> **Pre-step gate check (PLAN §10):** PASSED. Design-only; **no experiment result required and none may be cited.** FIG-6.1 (`✅ AVAILABLE`, deployed web-app screenshots) referenced by path. No `❌ MISSING (real result)` dependency.

---

### GOVERNANCE BINDINGS

**Hypotheses:** None — design specification.
**Paradigm positioning:** P2 (light) — inference operates on the four-channel tensor produced by the Preprocessing Engine, consistent with model = preprocessing + CNN. No Gulshan/P1 citation.
**Primary claims advanced:** PC-5 (design-only).
**Sub-claims:** SC-5.1 (module specification).
**Forbidden claims:** **CFC-2.2** (no superiority — must NOT state any backbone "outperforms"/is "better than" another, or cite a performance ranking); **CFC-2.4** (no validated/clinical-grade/deployment-ready); CFC-2.3 (no deployment outcome as result); **CFC-2.8** (no preprocessing-alone H-1 attribution — N/A, no H-1 result cited but guard); CFC-2.1 (no universal claim).
**Non-claims:** **NC-14** (Grad-CAM = post-hoc interpretability overlay for clinician review, NOT clinical localization of pathology); NC-6 (no architectural-optimality claim).
**Source interpretation rules:** SIR-4 (#22 prior own work); SIR-1 (no amplification); SIR-2 (#22 §II.6 at first citation); SIR-7 (no generalization of EfficientNet results to the efficient-CNN class).
**Scope boundaries:** **SB-4.1** (design-only; restate); **SB-1.3** (decision-support, physician-in-the-loop — NOT standalone diagnosis); **DGL-2** (hardware-specific framing — model selection is conditioned on the deployment host, not a universal ranking). **The model-selection logic is a design specification justified by resource-envelope fit, NOT by any experimental performance result.**
**Evidence thresholds:** None — and **no Exp-6 (or any experiment) numbers may appear.**

---

### CONTENT SPECIFICATION

**Section objective:** Specify the Inference Module — which consumes the four-channel tensor, produces a five-class DR grade (FR-3) and a post-hoc Grad-CAM decision-support overlay (FR-4) — and its model-selection logic, designed to select among the Chapter-3 CNN backbones by deployment-host resource profile so the inference function fits the OD-6 envelope, as a design specification only.

**Argumentative spine (required):**
- **Thesis:** The Inference Module realizes FR-3/FR-4 by pairing a trained CNN backbone with a post-hoc explanation, and its model-selection logic is a *design* mechanism that chooses the backbone whose computational profile fits the deployment host (NFR-1/2/3) — a selection conditioned on the resource envelope (DGL-2), not on an experimental performance ranking.
- **Reasoning chain:**
  1. Open from §6.2.1: the module consumes the four-channel tensor. Restate design-only (SB-4.1).
  2. Specify outputs: five-class DR grade (FR-3) + post-hoc Grad-CAM overlay (FR-4) for clinician review — NC-14 (interpretability overlay, not pathology localization) and SB-1.3 (decision-support, not standalone) stated here.
  3. Specify model-selection logic: among the Chapter-3 backbones (ResNet-50, EfficientNet-B3), the module selects by host resource profile (memory/compute/latency under NFR-1/2/3). Frame the selection criterion as *resource fit*, explicitly NOT as a performance ranking — no experiment numbers; OD-5 effectiveness remains a target reserved to Phase 2.
  4. Note SIR-7 / NC-6: no generalization that one architecture class is superior; no optimality claim; the two backbones are design options, validation reserved.
  5. Tie to envelope: selecting the backbone to the host is the inference-side counterpart of the Preprocessing Engine's host-budgeting; together they keep the whole local inference path inside OD-6 and feed the store-and-forward topology.
- **Conclusion / hand-off:** Inference function fixed as a design spec; hands off to §6.3 (telemedicine deployment + physician-in-the-loop interface that consumes the grade + overlay).

**Required content elements:**
1. Design-only restatement; module input = four-channel tensor (from §6.2.1).
2. Outputs: FR-3 grade + FR-4 Grad-CAM overlay; NC-14 + SB-1.3 stated.
3. Model-selection logic as resource-envelope-conditioned design mechanism (DGL-2); NO experiment numbers.
4. SIR-7 / NC-6: no superiority / optimality; validation reserved.

**Required tables/figures:**
- FIG-6.1 (deployed web-app / dashboard) — reference by ID + caption + real path `defense/figures/figures_mine/fig10_webapp_screenshot_1.png`. Do NOT embed; cite as the design's interface illustration, NOT as evidence of results.

**Required equations:** None.

---

### SOURCE MAPPING

| Literature Card | Role | Content |
|----------------|------|---------|
| `yesmukhamedov-nan-rk.md` (#22, 🔹SELF) | Recognition/Diagnosis/CDSS precedent | EB-02 Recognition Model + Diagnosis + CDSS components (Table 3, p. 83; Fig. 1, p. 83); EB-05 physician-in-the-loop / decision-support (pp. 78–79); §II.6 no-validation limitation |
| Chapter 3 §3.2–§3.3 (internal) | the backbones | ResNet-50, EfficientNet-B3 specification (by reference; do not re-derive) |
| Governance OD-5 / OD-6 / DGL-2 / NC-14 / SB-1.3 | target; envelope; hardware-specific; interpretability bound; decision-support | OD-5 (target only); OD-6 (NFR envelope); DGL-2 (hardware-specific); NC-14; SB-1.3 |

**⚠️ Coverage gaps:** (a) The MASTER_OUTLINE §6.2.2 line names "EfficientNet-B4" as a third option, but governance / Chapter 3 fix only ResNet-50 + EfficientNet-B3 as primary architectures → **select among the two Chapter-3 backbones; do NOT introduce B4 as a fixed governance option** (treat extensibility to further backbones generically, or `[VERIFY]`-flag if B4 named). (b) Any latency/throughput/accuracy figure is unsourced and experiment-gated → must NOT appear (CFC-2.x / no demo numbers).

---

### CROSS-REFERENCES

**Backward:** §6.2.1 (four-channel tensor input); §3.2–§3.3 (backbones); §6.1.1 (FR-3/FR-4, NFR envelope); §6.1.2 (store-and-forward, local inference path).
**Forward:** §6.3.2 (physician-in-the-loop interface consumes grade + overlay); §6.3.1 (deployment of the inference path in telemedicine/portable settings); §6.4.1 (no security role here).

---

### BOUNDARY WARNINGS

1. Do **not** cite any experiment result — no Exp-6 device-shift numbers, no Exp-1 metrics, no demo-dashboard preview numbers (CFC-2.x / SIR-1). Model selection is justified by resource fit, not measured performance.
2. Do **not** state any backbone as superior/better, or assert a validated/optimal choice (CFC-2.2, CFC-2.4, NC-6).
3. Do **not** present Grad-CAM as clinical localization of pathology (NC-14) — it is a post-hoc interpretability overlay for clinician review.
4. Do **not** present the system as standalone diagnostic (SB-1.3) — decision-support, physician-in-the-loop.
5. Do **not** introduce EfficientNet-B4 as a fixed option without governance support (`[VERIFY]` if named).
6. Do **not** embed FIG-6.1 or cite it as evidence of results — interface illustration only.

---

### COUNTER-ARGUMENT & INHERITED LIMITATIONS

- **Counter-position:** a "model-selection logic" invites the reading that the dissertation has ranked the backbones and picks a winner. Pre-empt by defining the selection criterion strictly as resource-envelope fit (DGL-2) and by reserving all effectiveness questions (OD-5) to Phase 2 — the logic selects by host profile, not by a demonstrated performance ordering.
- **Inherited limitations (SIR-2):** #22 §II.6 — no empirical validation/prototype, inherited by the Recognition/Diagnosis-component reference at first citation.

---

### ACCEPTANCE CRITERIA

- [ ] Spine realized: outputs (FR-3/FR-4) specified with NC-14 + SB-1.3; model-selection logic specified as resource-envelope-conditioned (DGL-2); no experiment numbers; hand-off to §6.3.
- [ ] No backbone superiority/optimality/validated-choice claim; OD-5 = target; SB-4.1 restated.
- [ ] B4 not introduced as governance option (or `[VERIFY]`-flagged).
- [ ] FIG-6.1 referenced by ID + path; not embedded; not cited as results.
- [ ] Every binding checkable with a verbatim PART 3 quote.

---

### WRITING DIRECTIVES

- **Tense:** present (design/definitions); past only for what #22 reported.
- **Self-citation:** SIR-4 — #22 Recognition/Diagnosis component re-identified as prior own work at first use.
- **Terminology:** inference module, CNN backbone, five-class DR grade, Grad-CAM (post-hoc interpretability overlay), decision-support, physician-in-the-loop, model selection — per GLOSSARY / Chapter 3.
- **Analytical depth:** ≥ 1 analytical claim per paragraph; the resource-fit-not-ranking distinction is the analytical core.
