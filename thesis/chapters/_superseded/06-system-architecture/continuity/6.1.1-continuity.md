# CONTINUITY NOTE — §6.1.1 Functional and Non-Functional Requirements Specification

**Version:** 6.0.0 · PART 2 of the §6.1.1 writing session · Carry into the §6.1.2 session. **Chapter 6 OPENS here** (first drafted section of the design chapter); carry-in was the §3.C note (end of Chapter 3 / methodology).

---

**Key concepts established:** the chapter-wide design-only epistemic status (SB-4.1; DGL-4) — Chapter 6 is a design specification, no prototype, no field test, no projected outcome stated as a finding; the two determinants of system requirements (the Chapter-3 integrated model + the OD-6 deployment envelope); a formal functional-requirements set (FR-1…FR-7, Table 6.1) and non-functional-requirements set (NFR-1…NFR-8, Table 6.2); the analytical principle that the non-functional (OD-6) envelope prunes the admissible realizations of the functional requirements (NFRs constrain FR realizations — the binding design driver).

**Terms introduced:** functional requirement (FR) / non-functional requirement (NFR) as distinct specification categories; "design specification" as the binding epistemic register of Chapter 6; store-and-forward operation (NFR-4); GDPR/HIPAA-*aligned* (a design requirement, explicitly not a certification). Carried, already defined (available without re-definition): resource-limited environment (OD-6); PACS, EHR, DMP, FHIR/HL7, CDSS, physician-in-the-loop, Doctor-AI feedback loop (from §1.1.2 / #22); five-class DR grade and the eight-stage preprocessing pipeline (from Chapter 3); OD-5 diagnostic-effectiveness profile (here used as acceptance target only).

**Argument thread:** Chapter 6 reframes the integrated model from an object of measurement (Chapter 3) to a component for deployment, and fixes the requirements such a deployment must satisfy. The active claim at section end is that the OD-6 non-functional envelope — not the functional list, which any DR screening system shares — is the binding design driver, because it determines which realizations of each function are admissible. This licenses §6.1.2 to assume the requirements envelope as given and obligates it to show the architecture respects that envelope.

**Final topic:** the two closing boundaries (every entry in Tables 6.1–6.2 is a design spec, not an attained property — OD-5 a target, NFR-7 a GDPR/HIPAA-aligned requirement not a certification, the #22 p.88 Kazakhstan benefits third-party projections not outcomes; realization/validation reserved for future work) and the explicit hand-off to §6.1.2's modular decomposition and PACS/EHR integration.

**Setup for next section (§6.1.2 Modular Architecture with PACS and EHR Integration):** open by taking Tables 6.1–6.2 as the specification to be satisfied; decompose the system into modules and show how they realize the OD-6 envelope; reference the deferred UML asset as `[DIA-6.3: System architecture UML — ASSET TO BE CREATED]` (PLAN §3.2 — deferred conceptual asset, does NOT block prose) and the available DIA-6.1 component diagram by Resource ID + caption + real path (ASSET_INVENTORY); cite #22 EB-02 (component architecture) and EB-06 (DMP, FHIR/HL7, ER model) with SIR-4 self-citation + SIR-2 §II.6 limitation; keep the design-only register (SB-4.1) and the GDPR/HIPAA-as-design-spec qualification (SB-4.2) live; word target 1,100–1,300 (per PLAN §7).

**Unresolved threads:**
- GDPR/HIPAA detail deferred to §6.4.1 — must remain design spec, not certification (NC-9 / SB-4.2).
- Inference-module model-selection logic deferred to §6.2.2 — must NOT cite Exp-6 numbers (design-only).
- Preprocessing-engine realization of FR-2 deferred to §6.2.1.
- OD-5 acceptance target remains a target until the experimental chapters (Phase 2) report whether any trained model attains it.
- §3.C corpus-level carry-forward flags (CFC-2.8 composite IV; SSL B/D arm untrained; focal-loss + in-domain SSL lit-GAPs) remain open but are not engaged by the design sections of Chapter 6.
