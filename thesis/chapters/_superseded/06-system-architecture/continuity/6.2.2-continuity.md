# CONTINUITY NOTE — §6.2.2 Inference Module with Model Selection Logic

**Version:** 6.0.0 · Carry into §6.3.1. Carry-in was 6.2.1-continuity (four-channel tensor as the inference input).

---

**Key concepts established:** the Inference Module consumes the four-channel tensor and emits two outputs — a five-class DR grade (FR-3) via a trained CNN backbone, and a post-hoc Grad-CAM overlay (FR-4); the overlay's bounded status (interpretability artifact, NOT clinical localization of pathology — NC-14); the system as decision-support with physician-in-the-loop, NOT standalone diagnosis (SB-1.3); the model-selection logic as a **resource-envelope-conditioned design mechanism** (chooses ResNet-50 vs EfficientNet-B3 by host memory/compute/latency under NFR-1/2/3; DGL-2), explicitly **not** a performance ranking; the inference module as the resource-side counterpart of the configurable Preprocessing Engine, keeping the whole grade-producing path local (precondition for §6.1.2 store-and-forward).

**Terms introduced (framing):** inference module; model-selection logic (resource-fit, not ranking); post-hoc Grad-CAM overlay as decision-support payload. Carried, already defined: ResNet-50, EfficientNet-B3 (Ch3 §3.2–§3.3); four-channel tensor (§6.2.1); FR-3/FR-4, NFR envelope (§6.1.1); store-and-forward (§6.1.2); OD-5 (target).

**Argument thread:** the section's contribution is the resource-conditioned selection logic binding #22's Recognition/Diagnosis component to the OD-6 envelope; the architecture is agnostic to the eventual experimental verdict (selects what the host can run, not what performs best). Active claim at section end: the local grade-producing path (preprocessing + inference) is budgeted to the host, feeding the store-and-forward topology — effectiveness reserved (OD-5 target).

**Final topic:** FIG-6.1 dashboard as interface illustration (not results); explicit hand-off to §6.3 (deployment of the local inference path in telemedicine/portable settings + the physician-in-the-loop interface that consumes grade + overlay).

**Setup for next section (§6.3.1 Telemedicine and Portable Device Support, incl. 6.3.1.1–6.3.1.3):** open by taking the local grade-producing path (Preprocessing Engine + Inference Module, §6.2) as the unit deployed in distributed/portable settings. Three sub-sections (MASTER_OUTLINE): **6.3.1.1** Deployment in distributed telemedicine systems (remote diagnostic support; the Ingestion module + store-and-forward topology across sites); **6.3.1.2** Integration with national eHealth platforms (Kazakhstan eHealth/PACS context, #22 p.86–87; the §6.1.2 PACS/EHR layer is the integration point); **6.3.1.3** Real-time remote DR screening in low-resource regions (**design feasibility, NOT demonstrated capability — SB-4.1/SB-4.3**). Sources: #22 EB-03 (telemedicine/portable/Fundus-on-Phone/UK 80% — but Kazakhstan projections are SIR-8, NOT findings); external deployment-precedent cards #14 (Wewetzer meta-analysis — DL DR screening in primary care, pooled sens 87%/spec 90% with prevalence-dependent PPV caveat; SIR-2/SIR-3), #45 (Ruamviboonsuk — prospective LMIC national-programme deployment; infrastructure/connectivity dependency + no-EMR-integration limitation; **do NOT echo its "outperformed specialists"/p=0.024 — CFC-2.2**), #34 (Morya — 35–55% screening compliance, motivation). Word target 1,100–1,300 (PLAN §7), aim mid-band. **Guards:** SB-4.1 + SB-4.3 (no field testing claimed); SIR-8 (no projected Kazakhstan/telemedicine coverage/cost/complication figure as a finding); external metrics cited with dataset/validation context (SIR-3), never as this system's outcomes.

**Unresolved threads:**
- EfficientNet-B4 (MASTER_OUTLINE §6.2.2) deliberately NOT adopted — §6.2.2 selects among the two Chapter-3 backbones; if a later section needs B4, `[VERIFY]` against governance first.
- DIA-6.3 UML still deferred (App C).
- GDPR/HIPAA / security deferred to §6.4.1; OD-5 effectiveness + PC-1 validity remain Phase-2 targets.
- §6.4.2 will carry the Kazakhstan applicability argument; keep #22 p.87–88 benefits as third-party projections (SIR-8) — do not pre-spend them in §6.3.1.
