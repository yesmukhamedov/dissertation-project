# CONTINUITY NOTE — §6.2.1 Preprocessing Engine with Configurable Pipeline

**Version:** 6.0.0 · Carry into §6.2.2. Carry-in was 6.1.2-continuity (Table 6.3 deferred the Preprocessing Engine internals).

---

**Key concepts established:** the Preprocessing Engine as the configurable module realizing FR-2; its core is the Chapter-3 eight-stage pipeline *as specified* (not as validated — effectiveness is the OD-5 acceptance target, reserved to Phase 2); three configurability axes (stage on/off; parameterization of tunable stages e.g. flat-field σ, CLAHE clip-limit; train-time vs inference-time mode); the inference-time interface contract (deterministic, augmentation-free image→four-channel-tensor map) as the precondition for the §6.1.2 store-and-forward topology and as the locus of FR-7 explicit-rejection behaviour; the NFR realization (configuration budgets the engine to its host → NFR-1/2/3; defined interface → NFR-5 replaceable); the admissibility-not-optimality boundary (NC-17).

**Terms introduced (this section's framing):** preprocessing engine; configurable pipeline; stage selection / parameterization / mode axes; interface contract (image-in, four-channel-tensor-out). Carried, already defined: the eight stages (Chapter 3 §3.1); four-channel tensor (RGB + FOV mask); FR-1…FR-7, NFR-1…NFR-8 (§6.1.1); store-and-forward topology (§6.1.2); OD-3/OD-5/OD-6.

**Argument thread:** the design contribution is the *housing* — exposing the specified pipeline as a configurable first-class module on the inference path (P2; the architectural expression of model = preprocessing + CNN) — not any claim about the pipeline's measured effectiveness. The active claim at section end is that configurability makes FR-2 admissible inside OD-6 and replaceable across contexts (NFR-5), with effectiveness reserved.

**Final topic:** the engine produces the four-channel tensor under the OD-6 envelope; explicit hand-off to §6.2.2, which specifies the module that *consumes* that tensor.

**Setup for next section (§6.2.2 Inference Module with Model Selection Logic):** open by taking the four-channel tensor produced by the Preprocessing Engine as the inference module's input; specify model-selection logic as a **design specification only** — selecting among the Chapter-3 backbones (ResNet-50, EfficientNet-B3) by computational-resource / deployment context (the NFR-1/2/3 envelope), **NOT** by citing any experiment (no Exp-6 or any result numbers; DGL-2 hardware-specific framing). Produce a five-class DR grade (FR-3) plus the post-hoc Grad-CAM decision-support output (FR-4; NC-14 framing — interpretability overlay, not clinical localization). Reference FIG-6.1 (deployed web-app/dashboard) by ID + real path `defense/figures/figures_mine/fig10_webapp_screenshot_1.png`. Word target 700–900 (PLAN §7), aim mid-band. **Guard:** model-selection is a design spec; do NOT state any backbone as superior or as the validated choice (CFC-2.2/CFC-2.4); OD-5 remains a target.

**Unresolved threads:**
- Inference-module model-selection logic to be specified in §6.2.2 — design-only, no experiment numbers; note the MASTER_OUTLINE §6.2.2 line names "EfficientNet-B4" as a third option, but governance/Chapter 3 fix only ResNet-50 + EfficientNet-B3 as primary architectures → §6.2.2 should select among the two Chapter-3 backbones and treat any higher-capacity variant generically, or `[VERIFY]`-flag B4 if introduced.
- DIA-6.3 UML still deferred (PLAN §3.2; App C).
- GDPR/HIPAA / security deferred to §6.4.1; OD-5 effectiveness + PC-1 validity remain Phase-2 targets.
