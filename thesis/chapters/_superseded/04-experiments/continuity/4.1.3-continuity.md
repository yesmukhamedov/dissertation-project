# CONTINUITY NOTE — §4.1.3 Hardware and Reproducibility Protocol

**Version:** 6.0.0 · PART 2 of the §4.1.3 session · **Closes §4.1 (Datasets & Configuration). §4.2–§4.8 are Phase-2 blocked (experiments not run).**

---

**Key concepts established:**
- **Documented hardware:** single consumer-grade NVIDIA RTX 3060 (12 GB VRAM), WSL2 Ubuntu, fixed conda environment — stated as the documented setup, **DGL-2-bound** (efficiency/throughput claims do not transfer to other compute contexts).
- **Hardware as design parameter:** batch size 16 at 512×512 is a consequence of the 12 GB VRAM bound; mixed-precision split (ResNet-50 on / EfficientNet off, fp16-overflow) is a hardware-driven choice.
- **TAB-3.1 rendered inline** (as Table 4.2; identical to §3.4.2): Adam lr/wd 1e-4, batch 16, ≤20 epochs (patience 5), focal loss γ=2 inverse-freq, 512×512, 3/4 channels, mixed-precision split, 5-fold patient-level, seed 42 deterministic.
- **Reproducibility as engineered confound-removal:** fixed seed 42 + deterministic (removes run-to-run variation), fixed augmentation + LR schedule, pinned software stack (Python 3.11 + PyTorch/Torchvision/OpenCV/NumPy/Scikit-learn), versioned code in App A.
- **Bounds:** DGL-2 (hardware-bound efficiency); SB-2.2 (private Clinical set → structural reproducibility limit). OD-6 referenced as the framing the constrained rig instantiates — **no real-time inference demonstrated** (CFC-2.4 honored).

**Terms carried:** documented hardware configuration; VRAM; mixed precision (fp16); deterministic execution; random seed 42; learning-rate schedule; reproducibility controls; versioned code; resource-limited environment (OD-6, framing only).

**Argument thread:** §4.1 is COMPLETE. The experimental setup — datasets (§4.1.1), distribution/partitioning/harmonization (§4.1.2), and hardware/reproducibility (§4.1.3) — is fully specified and bounded. The thread hands off to the empirical chapters (§4.2 onward), which are Phase-2 blocked pending experiment execution.

**Final topic:** The closing paragraph states the two reproducibility bounds (DGL-2 hardware, SB-2.2 private data) and declares §4.1 complete, handing the fixed setup to the H-1…H-7 evaluation.

**Setup for next section (§4.2 Experiment 1 — Phase 2, BLOCKED):** Per PLAN §9.1, §4.2.2/§4.2.3 are ⛔ blocked-by RES-EXP1(full)/TAB-4.2/FIG-4.4–4.8/TAB-4.3 (Exp 1 full 100% A–D × 5-fold incl. ophthalmology-SSL B/D arm not yet trained). §4.2.1 (factorial design) is design-writable but grouped under Exp 1. **Do NOT draft §4.2 until the gating resources flip ❌→✅ in ASSET_INVENTORY.** When unblocked, §4.2 opens from this §4.1 setup and must honor **CFC-2.8** (composite IV — dominance over the integrated (preprocessing, pretrain) pair only, never preprocessing-alone) and **EH-3/EH-4**.

**Unresolved threads / flags (carry forward):**
- **CFC-2.8 composite IV** → governs §4.2 attribution; first live application point.
- **SSL B/D arm UNTRAINED** (project memory; shipped Config-D = retired ImageNet artifact) → §4.2 stays blocked for the v6.0.0 SSL arm.
- **No fabricated metrics / no demo-preview numbers** (CFC-2.x, SIR-1) → binding once results sections open.
- **SB-2.2 private Clinical** + **IDRiD no-class-distribution / single-centre** (SIR-2) → recur at Exp 4/5/7 reporting.
- **#47/#48/#49/#50/#51 index-only** (#51 unconfirmed) → acquire cards before deeper-than-index use in §4.4/§4.7.
- All H-1…H-7 outcomes, EH-3/EH-4 verdicts → §4.2 onward; not claimable until experiments run.

**⛔ STOP MARKER:** Per the task instruction, work stops after §4.1.3 is APPROVED and saved. §4.2–§4.8 (Phase 2) and Appendices A/D are NOT to be started in this pass.
