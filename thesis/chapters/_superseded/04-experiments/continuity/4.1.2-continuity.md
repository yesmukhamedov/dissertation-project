# CONTINUITY NOTE — §4.1.2 Class Distribution and Data Partitioning

**Version:** 6.0.0 · PART 2 of the §4.1.2 session · Carry into §4.1.3 (step c). **§4.1 Datasets & Configuration; design/setup only.**

---

**Key concepts established:**
- The **EyePACS class imbalance** (severe; majority no-DR, small severe/proliferative minorities; shown via FIG-4.1, no per-grade counts asserted) is the **principal lever (SC-1.4)** fixing three downstream choices: (1) the EH-1 metric hierarchy — weighted F1 + quadratic-weighted κ lead, accuracy demoted, ROC-AUC threshold-independent (§3.4.1); (2) the **inverse-frequency-weighted focal loss** (§3.3.4); (3) **patient-level grade-stratified 5-fold partitioning** (§3.4.2). Train-only Stage-6 augmentation = secondary lever (§3.1.3).
- **Label harmonization** to the 5-class ICDR target specified: Messidor-2 (referable/non-referable + grade) and multi-disease RFMiD/ODIR-5K (DR subset) mapped onto ICDR; **Experiment 6 uses DR labels only** (non-DR excluded/mapped to non-DR); mappings documented (SB-2.3, SIR-3).
- **One partitioning protocol for all datasets** (patient-level 5-fold stratified, leakage-controlled, composes with train-only augmentation) — reused from §3.4.2, not re-derived; FIG-3.13 cross-referenced.

**Terms carried:** class imbalance; weighted F1; quadratic-weighted Cohen's κ; ROC-AUC; inverse-frequency-weighted focal loss; label harmonization; ICDR 5-class scale; referable/non-referable DR; DR-labels-only mapping (Exp 6); patient-level grade-stratified 5-fold CV; leakage control.

**Argument thread:** §4.1.1 fixed the data substrate; §4.1.2 has characterized the training tier's distribution, anchored the metric/loss/partition choices to it (SC-1.4), and harmonized the cross-dataset labels. The thread passes to §4.1.3, which fixes the hardware and reproducibility protocol — the last setup element before §4.2's experiments.

**Final topic:** The closing paragraph handles the IDRiD partitioning caveat (no published class distribution, single-centre; native 413/103 split vs Exp-7 5-fold re-partition) and hands off to §4.1.3.

**Setup for next section (§4.1.3 Hardware & Reproducibility Protocol):** Word band 600–800, aim mid-band (~700). Bindings: **DGL-2** (hardware-specific reproducibility — efficiency/real-time claims bound to the documented hardware, do not generalize); fixed seeds / versioned code / fixed schedules (reproducibility conditions). Lit: #21 (`yesmukhamedov-kbtu.md`, 🔹SELF — config precedent, SIR-4). Resource: **TAB-3.1** ✅ AVAILABLE — **render inline** (already rendered in §3.4.2; reuse exact governance values from RESEARCH_ARCHITECTURE §4.0). Reuse §3.4.2's DGL-2/SB-2.2 framing; do not re-derive the CV protocol (just point to §4.1.2/§3.4.2). Still design/setup — NO results. Note the hardware envelope operationalizes OD-6 (resource-limited) per §3.4.2/§1.1.2 — but keep efficiency claims bounded (DGL-2). Reference RTX 3060 12GB / WSL2 / conda `dr-classifier` from CLAUDE.md only as the documented configuration, framed as DGL-2-bound; mixed-precision split (ResNet on / EfficientNet off) is in TAB-3.1.

**Unresolved threads / flags (carry forward):**
- **No fabricated EyePACS counts** — FIG-4.1 carries the distribution; held in §4.1.2, must hold in §4.2.
- **IDRiD no class distribution** (#06/#10 §14, SIR-2) → recurs at Exp 4/5/7 reporting.
- **SB-2.2 private Clinical** → reproducibility limit recurs.
- **CFC-2.8 composite IV** → §4.2 attribution; not engaged in §4.1.
- All H-1…H-7 outcomes → §4.2 onward; not claimable until run.
