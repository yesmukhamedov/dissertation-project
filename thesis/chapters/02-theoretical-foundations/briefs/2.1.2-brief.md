# SECTION BRIEF
## §2.1.2 Formalization of CLAHE with Dual-Constraint Clip Limit

**Chapter:** 2 — Theoretical Foundations
**Section Function:** theoretical (mathematical derivation/formalization)
**Word Count Target:** 1,300–1,500 words (aim 1,400, middle of band) (4–4.5 pages)
**Paragraph Count Target:** 9–11 paragraphs

> Generated per `prompts/section-brief-template.md` v6.0.0 · Binding reference: INVARIANTS.md v6.0.0 · Plan row: PLAN.md §5. Carries the §2.1.1 continuity note into its opening.

---

### GOVERNANCE BINDINGS

**Hypotheses tested/supported:** None tested. Sets up H-2 (CLAHE clip-limit sensitivity), whose empirical evaluation is reserved for §4.3.2; this section provides the parameterization that H-2 sweeps.

**Paradigm positioning:** **Minimal / not invoked.** This is a mathematical-formalization section advancing the P2 (integrated, formalized-preprocessing) commitment by formalizing the contrast stage as a model component, but it makes no P1/P2 attribution claim and cites no P1 source for a theoretical statement; CFC-2.9/SIR-9 not engaged. #27 (Hayati, P1) cited only for empirical context. Stated explicitly.

**Primary claims advanced:** Supports PC-2 (CLAHE dual-constraint parameterization, validated by the Exp 2 sweep) at the level of *definition*; the empirical validation is §4.3.2. No experimental claim advanced here.

**Sub-claims sourced here (SC-x.y):** None proven; the dual-constraint clip limit is *defined and motivated*, not validated.

**Supporting contributions evidenced (SC-A…SC-H):** None.

**Forbidden claims (must not appear):**
- **CFC-2.5** — the #23/#24 100%-accuracy and the T/80 "best enhancement" results may not be presented as transferable or as a demonstrated capability in the dissertation's context (SB-1.2).
- **CFC-2.1** — no universal-optimality or "always improves" claim for any clip-limit formulation (NC-17 adjacent).
- **CFC-2.2** — no superiority ranking of formulations as a general claim; comparisons stay contextual and parameter-bound (CFC-1.2 form).
- **CFC-2.7** — the self-publications are cited as-is; not retroactively recharacterized as having proven the dual-constraint form (which they did not — they used single-threshold T/80).

**Non-claims (must not assert):** **NC-17** (component ablation / parameterization ≠ universally optimal preprocessing configuration) — honored by framing the dual-constraint clip limit as a tunable, to-be-swept design, not an optimal one.

**Source interpretation rules:**
- **SIR-1** (non-amplification) — #54 cited only for the slope-limiting/clipping equivalence; #23/#24 only for what they state (conventional & T/80 forms, the reported figures).
- **SIR-3** (metric consistency) — **explicitly flag the sensitivity-formula anomaly** in #23/#24: Sen = TP/(TP+TN) (Eq. 3) deviates from the standard Sen = TP/(TP+FN); note this where the source's validation metrics are cited (per INVARIANTS SIR-3, verbatim requirement).
- **SIR-4** (self-citation transparency) — #23 (`scopus-q2`) and #24 (`scopus-q3`) are prior own work; frame as "previously published results" + extension statement (the dissertation replaces single-threshold T/80 with the dual-constraint rule and validates it within its own framework).
- **SIR-5** (no cross-source aggregation) — #23 and #24 are **duplicate cards for the same EEJET article** (DOI 10.15587/1729-4061.2025.335570); they may NOT be cited as two independent confirmations of the same result. Treat as a single self-cited thread.

**Scope boundaries:**
- **DGL-5** (CLAHE parameter portability) — central binding: the T/80 threshold was derived on STARE; transferability to EyePACS is NOT assumed; the dual-constraint parameters must be independently validated within the dissertation's framework (→ §4.3.2). State at the T/80 introduction and at the dual-constraint definition.
- **SB-1.2** — STARE 100% not transferable.
- IT-1 modality — fundus photography.

**Evidence thresholds:** None (non-experimental). H-2's EH criteria apply only in §4.3.2.

---

### CONTENT SPECIFICATION

**Section objective:** Formalize the contrast stage's clip-limit rule — moving from the conventional contrast-limiting clip limit, through the candidate's prior single-threshold T/80 upgrade, to the **dual-constraint** clip limit `CL = min(clip_factor × tile_area/256, global_threshold × tile_area)` — explaining precisely what each constraint controls and why the minimum of the two is the design adopted, with TAB-2.1 contrasting the three formulations and Eqs. (2.1)–(2.3) stated.

**Argumentative spine (required):**
- **Thesis of this section:** The contrast stage adopts a dual-constraint clip limit that takes the minimum of a histogram-relative term (a multiple of the uniform per-bin count) and a tile-relative global cap (a fraction of the tile's pixel count); this bounds noise amplification under both near-uniform and strongly peaked local histograms, generalizing both the conventional single-factor clip and the candidate's prior single-threshold T/80 formulation, whose parameters are not assumed portable and are validated empirically in Experiment 2.
- **Reasoning chain:**
  1. The clip limit (from §2.1.1) is the height at which each tile's histogram is truncated before the cumulative mapping is formed; the conventional contrast-limiting clip sets it as a multiple (clip factor) of the uniform per-bin count tile_area/L (Eq. 2.1; #54 grounds the slope-limiting equivalence).
  2. The candidate's prior work replaced the conventional fixed/derived clip with a single global-threshold control, CL = T/80 (Eq. 2.2; #23/#24, self) — simpler, but governed by one parameter and validated only on STARE.
  3. A single per-bin factor over-amplifies when the local histogram is strongly peaked (large near-uniform background tiles, where tile_area/256 is a poor proxy for the true average occupied-bin height), while a single absolute cap ignores the local distribution; the rule takes the minimum of both terms (Eq. 2.3), so the tighter constraint binds per tile.
  4. The two parameters (clip_factor, global_threshold) are tunable and are characterized by the Experiment 2 sweep (§4.3.2); portability of any specific value across datasets/architectures is not assumed (DGL-5).
- **Conclusion / hand-off:** The contrast stage is now fully specified as a parameterized, dual-constraint operation on the LAB L-channel applied stochastically at train time (p=0.8) and deterministically at inference (OD-3 Stage 5); §2.1.3 turns to the complementary spatial-filtering/noise-reduction foundations.

**Required content elements:**
1. Recap (one paragraph, from §2.1.1) of the clip limit as the contrast-vs-noise control; the slope-limiting ≡ histogram-clipping equivalence (#54).
2. Conventional contrast-limiting clip limit — Eq. (2.1); definition of tile_area, gray-level count L=256, clip factor; the #23 conventional form ⌈L/T⌉+β(φ−⌈L/T⌉) presented as the source's stated conventional definition.
3. The candidate's prior single-threshold upgrade CL = T/80 — Eq. (2.2); SIR-4 self-framing; what it improved (controllable threshold, tiny-vessel visibility per #23) and its limits (single parameter, STARE-only, DGL-5; 100% non-transferable per SB-1.2/CFC-2.5; SIR-3 sensitivity anomaly flagged).
4. The dual-constraint clip limit — Eq. (2.3); decompose the two terms (histogram-relative vs tile-relative global cap); analytical explanation of why min(·) is taken and what each term protects against; tile grid 8×8; LAB L-channel; stochastic p=0.8 train / deterministic inference (OD-3 Stage 5).
5. TAB-2.1 (inline): conventional vs T/80 vs dual-constraint — definition, control parameter(s), origin, portability/limitation.
6. Parameter-tunability and the explicit hand-off to §4.3.2 (H-2 sweep); DGL-5 non-portability; NC-17 (not claimed optimal).

**Required tables/figures:**
- **TAB-2.1** — `✅ AVAILABLE` as governance text (RESEARCH_ARCHITECTURE §3.2). Render **inline** as Markdown: CLAHE clip-limit formulations (conventional vs T/80 vs dual-constraint).

**Required equations:**
- **Eq. (2.1)** conventional clip limit (contrast-limiting; #54/#23 grounding).
- **Eq. (2.2)** upgraded single-threshold clip limit CL = T/80 (#23/#24, self).
- **Eq. (2.3)** dual-constraint clip limit CL = min(clip_factor·tile_area/256, global_threshold·tile_area) (OD-3 Stage 5; RESEARCH_ARCHITECTURE §3.2).

---

### SOURCE MAPPING

| Literature Card | Role | Specific Content to Extract |
|----------------|------|---------------------------|
| `yesmukhamedov-scopus-q2.md` (**#23**, 🔹SELF) | self precursor: conventional + T/80 clip-limit forms | Conventional CL = ⌈L/T⌉+β(φ−⌈L/T⌉) (Eq. 1, p. 5); upgraded CL = T/80 (Eq. 2, p. 5); "improved the image distinctiveness, especially the tiny veins" (p. 5); image-quality score conv 33.2284 → T/80 34.3483 (Fig. 4); STARE 100% (SB-1.2); **sensitivity anomaly Sen=TP/(TP+TN)** (Eq. 3, p. 9 — SIR-3). |
| `yesmukhamedov-scopus-q3.md` (**#24**, 🔹SELF) | self precursor: CLAHE params + downstream effect (SAME article as #23 — SIR-5) | CLAHE clip limit 2.0, grid 8×8 (p. 83); preprocessing 71%→86% and 88%→91%; ROC-AUC 0.9638 (p. 84). Do NOT aggregate with #23 as independent confirmation. |
| `pizer-1987-adaptive-histogram-equalization.md` (**#54**) | foundational: slope-limiting ≡ clipping | "Limiting the slope of the mapping function is equivalent to clipping the height of the histogram" (p. 363). |
| `hayati-2023.md` (**#27**) | empirical context (parameter-/architecture-dependence) | CLAHE β formula (Eq. 1) with clip factor α; architecture-dependent effect incl. ResNet34 −12.02% (pp. 62–64); accuracy-only, no CI (SIR-2/SIR-3). |

**⚠️ Coverage gaps:** None material. The dual-constraint rule is governance text (RESEARCH_ARCHITECTURE §3.2 / OD-3 Stage 5), not literature-derived; it is attributed to the dissertation's own design, with the conventional/T-80 precursors cited. No fabricated parameter values; numeric clip_factor/global_threshold choices are reserved for §4.3.2 (not yet run).

---

### CROSS-REFERENCES

**Backward:** §2.1.1 (clip limit concept, clipped-AHE provenance #54, CLAHE = Stage 5).
**Forward:** §2.1.3 (spatial filtering/noise reduction); §3.1.1 (Stage 5) / §3.1.2 (upgraded CLAHE implementation, FIG-3.7); §4.3.2 (CLAHE clip-limit sensitivity sweep, H-2).

---

### BOUNDARY WARNINGS

1. **SIR-5 (same-article duplicates):** #23 and #24 are the same EEJET article; never present them as two independent confirmations. One self-cited thread.
2. **SIR-3 (sensitivity anomaly):** When citing #23/#24 validation metrics, flag Sen=TP/(TP+TN) deviating from TP/(TP+FN).
3. **DGL-5:** T/80 and any specific clip parameters are STARE-derived / not assumed portable; dual-constraint parameters validated only in §4.3.2.
4. **CFC-2.5 / SB-1.2:** 100% STARE result not transferable.
5. **NC-17 / CFC-2.1:** Do not call the dual-constraint clip limit "optimal" or universally beneficial; it is a tunable design to be swept.
6. **CFC-2.7:** The prior self-work used single-threshold T/80, NOT the dual-constraint rule; do not retroactively credit it with the dual-constraint formulation (which is the dissertation's own advance).

---

### COUNTER-ARGUMENT & INHERITED LIMITATIONS

- **Counter-position to address:** "A single clip-limit parameter (a fixed clip factor, or the prior T/80) is sufficient." The section must show the failure mode each single-parameter form leaves open (per-bin factor over-amplifies peaked background histograms; absolute cap ignores local distribution) and motivate the dual-constraint min(·) as the response — while conceding that the superiority of the dual-constraint form over T/80 is a *design rationale* validated empirically only in §4.3.2, not a proven result here.
- **Inherited limitations (SIR-2):**
  - #23/#24 (self) — STARE 100% with no overfitting analysis; single-threshold validated on one dataset; sensitivity-formula anomaly; internal metric inconsistencies (#24 conclusion vs results).
  - #54 Pizer — qualitative, modality-dependent clip levels, no quantitative validation.
  - #27 Hayati — accuracy-only, no CI, hyperparameter confound.

---

### ACCEPTANCE CRITERIA

A draft is **strong** when:
- [ ] The derivation is complete: conventional clip (Eq. 2.1) → single-threshold T/80 (Eq. 2.2) → dual-constraint (Eq. 2.3), each term defined and motivated; min(·) rationale given analytically.
- [ ] TAB-2.1 rendered inline AND interpreted (not just presented).
- [ ] #23/#24 framed as one self-cited thread (SIR-4 + SIR-5), not independent confirmation.
- [ ] SIR-3 sensitivity-formula anomaly explicitly flagged at the validation-metric citation.
- [ ] DGL-5 non-portability stated at T/80 and at the dual-constraint definition; §4.3.2 hand-off explicit.
- [ ] CFC-2.5/SB-1.2 (100% non-transferable) and NC-17 (not optimal) honored.
- [ ] CFC-2.7 honored: prior work credited only with single-threshold T/80, dual-constraint attributed to the dissertation.
- [ ] Opening connects to §2.1.1 continuity (clip-limit concept).
- [ ] Every governance code satisfiable with a verbatim PART 3 quote.

A **passable** draft merely satisfies word count and forbidden-claim compliance. Aim above passable.

---

### WRITING DIRECTIVES (section-specific)

- **Tense:** Present for definitions/derivations; past for cited study findings.
- **Self-citation handling:** #23/#24 — "previously published results" (SIR-4), one thread (SIR-5), with extension statement (dissertation introduces the dual-constraint rule and validates parameters in its own framework).
- **Terminology watch:** Use "CLAHE," "clip limit," "tile area," "tile grid" consistently; this section **resolves** the glossary `[DEFINITION REQUIRED]` flags for "clip limit" and "tile grid" by formalizing them. "Dual-constraint clip limit" is the canonical name (INVARIANTS/RESEARCH_ARCHITECTURE). Stage 5 numbering per OD-3.
- **Analytical depth:** Derivation-heavy; each formulation paragraph must interpret (what the parameter controls, what failure mode it addresses), not just state the formula. Equations in Markdown/LaTeX.
