# CONTINUITY NOTE — §2.5.1 CAM / Grad-CAM Theory and Formalization

**Version:** 6.0.0 · Produced as PART 2 of the §2.5.1 writing session · Carry into the §2.5.2 session (step c).

---

**Key concepts established:**
- **Class activation mapping (CAM)** (Zhou #57): GAP-based projection of class weights onto final-conv activations, $M_c(x,y)=\sum_k w_k^c f_k(x,y)$; requires a GAP architecture; weakly-supervised localization < fully supervised.
- **Grad-CAM** (Selvaraju #46, index-only/no card): gradient generalization — neuron-importance $\alpha_k^c=\frac{1}{Z}\sum_{i,j}\partial y^c/\partial A_{ij}^k$, map $L^c=\mathrm{ReLU}(\sum_k \alpha_k^c A^k)$; applies to any CNN without retraining; reduces to CAM for GAP nets; ReLU keeps positive-influence features.
- **Grad-CAM++** (Chattopadhyay #58): pixel-wise weighted (higher-order) gradients; better localization/multi-instance (avg-drop 36.84% vs 46.56%, ImageNet) — source benchmark, NOT an adopted ranking (CFC-2.2).
- **NC-14 (central, binding)**: Grad-CAM maps are coarse final-conv gradient-weighted activations upsampled for display — interpretability visualization, NOT pixel-level clinical lesion delineation; overlap with lesion masks = attention signal, not proof of clinical localization/reasoning.
- #46 Grad-CAM literature card is **absent (index-only)** — flagged for corpus completion.

**Terms introduced (now available without re-definition):** class activation mapping (CAM), global average pooling (in CAM context), Grad-CAM, neuron-importance weights (α_k^c), localization map (L^c), ReLU gating of activations, Grad-CAM++, gradient-weighted activation, final-conv resolution / upsampling.

**Argument thread:** The interpretability map is formally defined and strictly bounded (NC-14). The active thread now splits into *interpretation* (how to read the map, its ambiguities) and *quantification* (overlap metrics) — §2.5.2 then §2.5.3.

**Final topic:** The last paragraph poses the two remaining questions — how a Grad-CAM map is interpreted (§2.5.2) and how it is quantified against lesion masks (§2.5.3) — and hands off to §2.5.2. Reader holds a formal, NC-14-bounded localization map.

**Setup for next section (§2.5.2 — Attention Map Interpretation):** Open by taking the Grad-CAM formalization as given and address what a localization map does and does not license a reader to conclude. Sources: #46 (Selvaraju, index-only), #57 (Zhou/CAM), #58 (Grad-CAM++). **Governance:** **NC-14** (reinforce — interpretation must not slide into clinical-localization claims). Short section (600–800, aim 700). No equations mandated. Discuss: coarse resolution, class-discriminativeness, sensitivity to architecture/layer choice, the gap between "attention concentrates here" and "pathology is here." Foreshadow §2.5.3 (ALO/IoU) and Exp 4 (§4.6, qualitative Clinical overlays + quantitative IDRiD).

**Unresolved threads:**
- Attention-map interpretation → **§2.5.2** (immediate next).
- ALO (primary) / IoU (secondary) quantification → **§2.5.3**.
- Grad-CAM explainability results → §4.6 (Exp 4, H-5) and §5.1 (not claimable until run).
- #46 Selvaraju Grad-CAM card creation → corpus completion before Phase 3.
- FIG-2.3 (deferred conceptual diagram) → asset queue, Phase 3 §11.2.
