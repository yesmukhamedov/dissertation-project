# CONTINUITY NOTE — §2.3.2 Frozen-Layer versus Progressive Fine-Tuning Strategies

**Version:** 6.0.0 · Produced as PART 2 of the §2.3.2 writing session · **Carry into the §2.4.1 session (step c)** — the loop SKIPS the deferred §2.3.3 this pass.

---

**Key concepts established:**
- **Adaptation spectrum**: frozen-base / fixed-feature extraction (backbone frozen, only new head trained) ↔ progressive / two-stage fine-tuning (head first, then unfreeze upper layers). Glossary-disambiguated terms.
- **Theory favors fine-tuning under domain shift**: Yosinski (#71, fine-tuned > target-only; co-adaptation penalty of freezing); Kornblith (#72, fine-tuning > fixed features in 179/192) — advantage largest where target diverges from source (the fundus case).
- **Prior-DR empirical confirmation** (#19/#21, one thread): frozen test F1 0.62 → progressive 0.74 (+12 pp); macro 0.72→0.77, weighted 0.74→0.81; train/test gap narrowed; accuracy unchanged at 0.80 (gains in imbalance-aware metrics).
- **two-stage progressive fine-tuning protocol** (§3.3.3) adopted; it is a **shared training method** across H-1 configs, NOT an IV (H-3 dropped; CFC-2.8).
- **Domain-adversarial training** (DANN, #76): representation-level domain-invariance via gradient-reversal — distinct, non-adopted alternative, relevant to device domain shift (§4.8).
- SIR-5 (#19/#21 one thread), SIR-7 (EfficientNetB0 not generalized).

**Terms introduced (now available without re-definition):** frozen-base / feature-extraction strategy, progressive / two-stage fine-tuning, co-adaptation penalty, domain-adversarial training (DANN), gradient-reversal layer, domain-invariant features, shared training method (vs IV).

**Argument thread:** The transfer-learning foundations that the experiments actively exercise are complete (feature extraction → loss → regularization → transfer → adaptation). Chapter 2 now pivots to its ONE purely theoretical, **simulation-only** strand — the coupled thermal-optical (laser-tissue) model — which is epistemically distinct (theoretical, not experimentally validated) from everything preceding.

**Final topic:** The last paragraph declares the experimentally-exercised foundations in place, notes in-domain SSL (§2.3.3) is treated separately, and transitions explicitly to §2.4.1 (coupled thermal-optical model). Reader is primed for a theoretical/simulation strand and for its strict epistemic bounding.

**Setup for next section (§2.4.1 — Coupled Thermal-Optical Model of Fundus Tissue Response):** Open by marking the epistemic shift — from the experimentally-exercised CNN/preprocessing/transfer foundations to a **simulation-only theoretical model**. Source: **#20** (`yesmukhamedov-kazutb`, 🔹SELF, THIN — the only source; FDM thermal model of laser-fundus interaction, qualitative). FIG-2.4 is a **deferred conceptual diagram** (ASSET TO BE CREATED). **Governance (critical):** **SB-1.5** (model is NOT an experimentally validated clinical model — qualitative simulation only); **SIR-6** (modeling-study non-extrapolation — cite as theoretical/computational grounding only, NOT empirical validation; the source's "confirms effectiveness of laser therapy" is the SOURCE's claim, cite as such, do not absorb); **CFC-2.4** (no clinical-validation language); **SIR-4** (self-work). Equations Eq.1–8 (thermal/optical model) where the row calls for them. THIN-coverage flag (SIR-1) — single self-source, frame honestly. Do NOT connect to DR-classification performance.

**Unresolved threads:**
- In-domain SSL theory → **§2.3.3 (DEFERRED — awaiting DINO/BYOL/SimCLR/MoCo-on-fundus cards)**; methodology §3.3.2.
- Two-stage fine-tuning protocol formalization → §3.3.3.
- DANN / device domain shift → §4.8 (H-6).
- Thermal-optical model's place: a theoretical contribution (PC-4, secondary/supplementary per RESEARCH_ARCHITECTURE) — §2.4.1 develops it, §0.9 theoretical significance references it.
