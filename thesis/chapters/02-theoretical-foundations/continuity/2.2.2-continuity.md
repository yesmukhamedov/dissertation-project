# CONTINUITY NOTE — §2.2.2 Loss Functions and Optimization for Imbalanced Medical Datasets

**Version:** 6.0.0 · Produced as PART 2 of the §2.2.2 writing session · Carry into the §2.2.3 session (step c).

---

**Key concepts established:**
- **Severe class imbalance** of DR data (no-DR 73.5%; DR 3/4 2–2.5% in the self-work APTOS data; <3% classes 3–4 in #01) as the dominant constraint on loss design.
- **Softmax cross-entropy** and its failure under imbalance: majority-class gradient dominance → minority (severe-grade) under-learning + accuracy inflation + overfitting (train F1 0.87 vs test 0.62).
- **Cost-sensitive / class-weighted loss**: inverse-frequency / cost reweighting raises minority-error gradient; distribution-preserving and efficient vs resampling (#40), but thin field validation (2/173).
- **Focal loss** (adopted loss): modulating factor down-weights easy/majority examples, concentrates on hard/minority; γ=2, α=inverse-frequency — established technique, **formalization deferred to §3.3.4** (FIG-3.12); canonical source absent from corpus (flagged).
- **Imbalance-appropriate metrics**: weighted F1, ROC-AUC, quadratic-weighted Cohen's κ (EH-1, SC-1.4); accuracy demoted; κ's quadratic weighting matches the DR ordinal/unequal-cost structure (§1.1.1).
- SIR-5 (#19/#21 = one thread) and SIR-7 (no EfficientNetB0→efficient-CNN-class generalization) applied.

**Terms introduced (now available without re-definition):** softmax cross-entropy, gradient dominance, cost-sensitive / class-weighted loss, inverse-frequency weighting, misclassification cost, focal loss, focusing parameter γ, class-balancing weight α, modulating factor, weighted F1-score, quadratic-weighted Cohen's κ, accuracy inflation, resampling (vs reweighting).

**Argument thread:** Loss design under imbalance is fixed (CE → CSL → focal loss) and tied to imbalance-aware evaluation. The closing beat flags that imbalance + limited data also drive **overfitting** (train/test gaps already shown), which the loss does not address — handing the thread to regularization.

**Final topic:** The last paragraph summarizes the loss/metric choices and explicitly transitions to §2.2.3 (regularization: dropout, batch normalization, data augmentation) as the overfitting control. Reader is primed for regularization theory.

**Setup for next section (§2.2.3 — Regularization Techniques: Dropout, Batch Normalization, Data Augmentation):** Open by taking the imbalance-driven overfitting (train F1 0.87 vs test 0.62; #42's 11-pt gap) as the established problem and turn to its control. Sources: #01 (Pratt — dropout 0.5, L2, batch norm), #19 (`conf`, 🔹SELF — EarlyStopping/ReduceLROnPlateau, augmentation), #21 (`kbtu`, 🔹SELF — same thread, SIR-5), #65 (Krizhevsky — dropout + PCA color augmentation originate here). **Governance:** **OD-3** (augmentation operational definition — Stage 6: unified affine + ColorJitter (brightness/contrast/saturation/hue) + Gaussian noise + JPEG compression, train-only); **SIR-4/5** (#19/#21 one thread); **SIR-7** (EfficientNetB0 bound). Tie batch normalization (glossary: enhanced architecture only) and dropout to the pipeline/architecture design; frame data augmentation as both regularizer AND imbalance mitigation (bridges from §2.2.2), connecting to Stage 6 (FIG-3.8) and SC-1.4. No equations mandated.

**Unresolved threads:**
- Focal-loss formalization (modulating-factor expression, γ/α rationale) → **§3.3.4** (FIG-3.12); acquire Lin et al. 2017 source for corpus.
- Two-stage fine-tuning (frozen vs progressive, the #19/#21 central result) → **§2.3.2** / §3.3.3.
- Weighted-F1 / κ multi-metric framework → **§3.4.1** (TAB-3.2).
- Class distribution specifics → **§4.1.2** (FIG-4.1).
