# CONTINUITY NOTE — §2.1.2 Formalization of CLAHE with Dual-Constraint Clip Limit

**Version:** 6.0.0 · Produced as PART 2 of the §2.1.2 writing session · Carry into the §2.1.3 session (step c).

---

**Key concepts established:**
- The clip limit formalized in three forms: **conventional** $\text{CL}=\beta\cdot A/L$ (Eq. 2.1; clip factor $\beta$ relative to uniform per-bin count $A/L$); **single-threshold T/80** $\text{CL}=T/80$ (Eq. 2.2; prior own work, STARE); **dual-constraint** $\text{CL}=\min(\text{clip\_factor}\cdot A/256,\ \text{global\_threshold}\cdot A)$ (Eq. 2.3; OD-3 Stage 5).
- The analytical rationale for the **min(·) of two constraints**: histogram-relative term governs well-spread tiles; tile-relative absolute cap governs strongly peaked (background) tiles, bounding mapping slope regardless of local distribution. Generalizes both single-parameter forms.
- The contrast stage fully specified: LAB **L-channel**, **8×8 tile grid**, clipped excess redistributed, **stochastic at train (p=0.8)**, deterministic at inference (OD-3 Stage 5).
- The two hyperparameters (clip_factor, global_threshold) are **tunable, not fixed**; values validated only in Exp 2 (§4.3.2), not imported from STARE (DGL-5).
- TAB-2.1 rendered (conventional / T-80 / dual-constraint).
- Glossary `[DEFINITION REQUIRED]` flags for "clip limit" and "tile grid" are now **resolved** by this section's formalization.

**Terms introduced (now available without re-definition):** clip factor ($\beta$), tile area ($A$), gray-level count ($L=256$), global threshold ($T$), single-threshold (T/80) clip, dual-constraint clip limit, clip_factor / global_threshold hyperparameters, histogram-relative vs tile-relative constraint, clipped-mass redistribution, stochastic train-time application (p=0.8).

**Argument thread:** The contrast stage is now mathematically closed — defined as a tunable dual-constraint operation with an explicit design rationale and an explicit deferral of parameter selection to Experiment 2. The §2.1 contrast sub-thread is complete; the chapter now broadens to the other low-level image-operation foundations (noise reduction / edge-preserving filtering) that complement contrast enhancement.

**Final topic:** The last paragraph confirms the stage's full specification and transitions to §2.1.3 (spatial filtering and noise reduction). Reader ends holding a complete, parameterized CLAHE definition, primed for the complementary noise-suppression theory.

**Setup for next section (§2.1.3 — Spatial Filtering and Noise Reduction Methods):** Open by taking the contrast-stage formalization as complete and pivot to noise reduction / edge-preserving smoothing as the complementary low-level operation. Sources: #55 (Tomasi & Manduchi — bilateral filtering), #56 (Buades et al. — non-local means). **Governance:** cite these as **algorithmic foundations only**, NOT as evidence of DR-classification gains (both are qualitative method papers; SIR-1/SIR-2); shortest section in §2.1 (600–800 words, aim 700). No equations mandated. Connect to the pipeline conceptually: the pipeline's de-noising/illumination handling is carried by flat-field correction (Stage 4) and the contrast-limiting design (Stage 5), so frame bilateral/NLM as the theoretical backdrop for edge-preserving, structure-conserving operations rather than as adopted stages.

**Unresolved threads:**
- CLAHE clip-limit parameter sweep / optimum (H-2) → **§4.3.2** (empirical; not claimable until run).
- CLAHE implementation specifics (polar variant, vessel-density adaptivity, FIG-3.7) → **§3.1.2**.
- #23/#24 card consolidation (same EEJET article; #23 ID/self-authorship integrity flag) → corpus-level cleanup before Phase 3; in project memory.
- Glossary update: point "Clip Limit"/"Tile Grid" entries to §2.1.2 → editorial, before Phase 3.
