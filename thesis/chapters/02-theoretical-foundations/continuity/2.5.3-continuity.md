# CONTINUITY NOTE — §2.5.3 ALO and IoU as Quantitative Explainability Metrics

**Version:** 6.0.0 · Produced as PART 2 of the §2.5.3 writing session · Carry into the §2.6 session (step c).

---

**Key concepts established:**
- **ALO (primary, H-5)**: $\mathrm{ALO}=\dfrac{\mathrm{area}(\text{GradCAM}\cap\text{lesion})}{\mathrm{area}(\text{lesion})}$ — asymmetric, lesion-referenced fraction of the lesion covered by attention.
- **IoU (secondary)**: $\mathrm{IoU}=\dfrac{\mathrm{area}(\text{GradCAM}\cap\text{lesion})}{\mathrm{area}(\text{GradCAM}\cup\text{lesion})}$ — symmetric; provenance in object detection (PASCAL VOC IoU>0.5, #64; GIoU, #63).
- **ALO primacy justified**: coarse, class-discriminative Grad-CAM maps extend beyond lesion boundaries by construction; symmetric IoU penalizes that coarseness as error, ALO measures lesion coverage → ALO primary, IoU a stricter secondary check (low IoU + high ALO = method-resolution property, not localization failure).
- **NC-14 / borrowed-not-benchmark (index note 47)**: both are interpretability measures, NOT detection/clinical-localization; IoU borrowed as overlap measure, not the detection threshold it originally defined.
- H-5 condition $\mathrm{ALO}_{\text{preproc}}>\mathrm{ALO}_{\text{baseline}}$ reserved for Exp 4 (§4.6).

**Terms introduced (now available without re-definition):** Attention–Lesion Overlap (ALO), Intersection-over-Union (IoU), lesion mask, thresholded activation region, symmetric vs asymmetric overlap, Generalized IoU (GIoU), overlap-as-interpretability vs detection-benchmark.

**Argument thread:** The §2.5 explainability block is complete (Grad-CAM formalization → interpretation → quantification, all NC-14-bounded). One theoretical strand remains: the **image-quality metrics** that evaluate the preprocessing pipeline's effect on the image itself, independent of any classifier — §2.6.

**Final topic:** The last paragraph completes the explainability formalization, reserves the H-5 ALO comparison for §4.6, and transitions explicitly to §2.6 (image-quality metrics). Reader is primed for the preprocessing-evaluation metrics.

**Setup for next section (§2.6 — Image Quality Metrics for Preprocessing Evaluation: CNR, VVI, Entropy, SSIM):** Open by taking the explainability metrics as the last classifier-referenced measures and turn to metrics that evaluate the *image* directly. Sources: #52 (Guo — calibration/ECE; note: primarily a calibration reference, see index — used for the calibration-metric family, but §2.6 core metrics are CNR/VVI/Entropy/SSIM), #53 (Wang — SSIM). **Governance:** **SIR-1 (THIN)** — #52/#53 are GENERAL references, NOT DR-specific; flag thin coverage where DR-specific application sources are absent; do not over-claim. TAB-3.3 (image-quality metric definitions) is the governance-text table (defer full table to §3.4.1, reference here). FIG-2.5 is a **deferred conceptual diagram** (ASSET TO BE CREATED). Define CNR, Vessel Visibility Index (VVI), Image Entropy, SSIM (glossary: Image Entropy expected to increase after contrast enhancement; SSIM preserves structure). Frame these as evaluating preprocessing effect (Exp 2, §4.3.3), independent of classification. 900–1,100 (aim 1,000).

**Unresolved threads:**
- Image-quality metrics → **§2.6** (immediate next), then **§2.C** (chapter conclusions).
- Explainability results → §4.6 (Exp 4, H-5), §5.1 (not claimable until run).
- §2.3.3 in-domain SSL → DEFERRED (this pass); §3.3.2 methodology.
- #46 Selvaraju Grad-CAM card → corpus completion before Phase 3.
- Deferred conceptual diagrams FIG-2.1/2.3/2.4 logged; FIG-2.5 to be logged in §2.6.
