# CONTINUITY NOTE — §2.5.2 Attention Map Interpretation

**Version:** 6.0.0 · Produced as PART 2 of the §2.5.2 writing session · Carry into the §2.5.3 session (step c).

---

**Key concepts established:**
- **Legitimate reading** of an attention map: class-discriminative activation — where the network's evidence for the predicted class concentrates; supports a judgment of **attention plausibility**.
- **Three interpretive limits**: (1) coarse final-conv **resolution** (boundaries approximate; fine lesions attended-not-delimited); (2) **method/layer dependence** (Grad-CAM++ reweighting changes localization → maps are a model-and-method property, comparable only with method fixed); (3) **activation ≠ pathology gap** (NC-14) — concentrated activation is not a pathology determination; the network may attend co-occurring context.
- **Overlap with lesions = suggestive, not probative**; the "where did class evidence concentrate / is it plausible?" question (interpretability) is distinct from "where is the pathology?" (clinical determination).
- #46 Grad-CAM card still pending (index-only).

**Terms introduced (now available without re-definition):** attention plausibility, class-discriminative activation (interpretation), coarse-resolution limit, method/layer dependence, model-and-method pairing, activation-vs-pathology gap, suggestive-not-probative overlap.

**Argument thread:** Interpretation of attention maps is fixed at the level of plausibility, strictly below clinical localization (NC-14). The active thread now moves to **quantifying** the attention–lesion relationship — turning "plausibility" into measurable overlap — which §2.5.3 formalizes.

**Final topic:** The last paragraph distinguishes the interpretability register from clinical determination and hands off to §2.5.3 (ALO/IoU as interpretability measures, not a detection benchmark). Reader is primed for the overlap-metric formalization.

**Setup for next section (§2.5.3 — ALO and IoU as Quantitative Explainability Metrics):** Open by taking the bounded attention-map interpretation as given and formalize the overlap metrics. Sources: #63 (Rezatofighi — Generalized IoU), #64 (Everingham — PASCAL VOC, AP and IoU>0.5 criterion). **Governance (PLAN/INVARIANTS):** define **ALO as the PRIMARY metric** — ALO = area(GradCAM ∩ lesion_mask) / area(lesion_mask) (per H-5/INVARIANTS) — and **IoU as SECONDARY** — IoU = area(GradCAM ∩ lesion_mask) / area(GradCAM ∪ lesion_mask); frame IoU as an **overlap metric borrowed from object detection, NOT a detection benchmark** (LITERATURE_INDEX note 47). Reinforce **NC-14** (overlap is interpretability, not clinical localization). Provide both equations. SIR-1/SIR-2 (#63/#64 object-detection references, non-medical). Foreshadow Exp 4 (§4.6, H-5) where ALO_preproc > ALO_baseline is tested (not claimable until run).

**Unresolved threads:**
- ALO/IoU formalization → **§2.5.3** (immediate next).
- Explainability results → §4.6 (Exp 4, H-5: ALO primary, IoU secondary, IDRiD masks + Clinical qualitative) and §5.1 (not claimable until run).
- #46 Selvaraju Grad-CAM card → corpus completion before Phase 3.
- After §2.5.3 → §2.6 (image-quality metrics), then §2.C.
