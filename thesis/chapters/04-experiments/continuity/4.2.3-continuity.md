# CONTINUITY NOTE — §4.2.3 Quantitative Comparison of Diagnostic Metrics

**Version:** 7.0.0 · PART 2 of the §4.2.3 session · **H-1 adjudicated and supported. §4.2 (Experiment 1) COMPLETE.**

---

**Key concepts established:**
- **Five tables rendered inline:** Table 4.6 (TAB-4.2, A–D × five metrics, mean ± std), Table 4.7 (pairwise Δ with 95 % CIs — all six exclude zero), Table 4.8 (EH-3 dominance assessment), Table 4.9 (per-class F1), Table 4.10 (referable-threshold clinical metrics).
- **H-1 SUPPORTED on the pre-committed conjunctive criterion, both backbones.** ΔwF1 **+6.54 / +6.55 pp** (threshold 5 pp); ΔAUC **+0.0320 / +0.0360** (threshold 0.02); Δκ **+0.1129 / +0.1103** (requirement: no degradation).
- **EH-4 met by test, not by inspection:** mixed-effects model, **no arm × backbone interaction (p = 0.31)**. Reinforced by non-overlapping CV intervals on all four primary metrics and by a per-instance bootstrap agreeing to the fourth decimal (gap ≈0.058 between the top of A and the bottom of B).
- **Significance, calibrated:** DeLong p = 0.0041 / 0.0028 (**z ≈ 2.87 / 2.99 — moderate but stable**); McNemar p = 0.0057 / 0.0041; **Holm over four configurations** p = 0.0082 / 0.0056. House rule adopted and to be kept chapter-wide: *"significant at the 0.05 level after Holm correction"* — **no intensifiers, no exponent-notation p-values.**
- **McNemar discordant balance** (2 190/2 010; 2 265/2 075) used to rule out mere error rearrangement.
- **The gain is minority-weighted:** macro-F1 rises **more** than weighted F1 (+0.104 / +0.106 vs +0.065 / +0.066); every grade improves on both backbones.
- **DR1 handled honestly:** F1 roughly doubles (0.0999 → 0.2141; 0.0976 → 0.2188) **but remains ≈0.21** — "substantially mitigates a difficulty that it does not resolve". Tied back to §1.1.1 (microaneurysm scale) and §4.1.2 (minority + least distinctive).
- **The κ mechanism is derived, not asserted:** quadratic weighting penalizes ∝ squared grade distance; distant cells empty (DR0→DR4 26 → 4; DR0→DR3 127 → 33; DR0→DR1 ≈2 950 → ≈1 890) ⟹ disproportionate κ gain. Residual errors are predominantly adjacent-grade — the kind of disagreement that also occurs between human graders.
- **Curve shift, not threshold shift — the §4.2.2 debt paid.** Referable (grade ≥ 2) sensitivity +11.2 pp on both backbones **with specificity also rising**; referable AUC +0.041 / +0.042; PPV ≈ +9 pp, NPV ≈ +2.6 pp. Simultaneous improvement is impossible by moving an operating point along a fixed ROC.
- **FIG-4.7 not cited** — per-class ROC-AUC was never recorded; per-class discrimination is reported through F1 only. This constraint is chapter-wide.

**Terms introduced:** dominance criterion (applied); effect homogeneity; discordant pairs; Holm correction; referable diabetic retinopathy; positive/negative predictive value; adjacent-grade confusion.

**Argument thread:** Experiment 1 is closed. The integrated configuration dominates the baseline as a *unitary system*, in-domain, with the gain concentrated where it matters diagnostically (minority grades, distant errors, the referral threshold). The thread now carries one large unpaid debt forward — the CFC-2.8 separation — plus the mechanism question.

**Final topic:** The three bounds (CFC-2.8 at the verdict; DGL-1 in-domain-only; SB-1.3/NC-3/CFC-2.4 no clinical benefit or readiness), closing with the hand-off to §4.3.

**Setup for next section (§4.3.1 Ablation Design, Levels L0–L7):**
- §4.3.1 **must redeem the promise made twice** (in §4.2.1 and again at the §4.2.3 verdict): a cumulative ablation on the same corpus, the same patient-level folds, and a **single initialization held constant at every level**, so that the preprocessing contribution is measured independently of the initialization source.
- The load-bearing result to state: **L0 = 0.7538 and L7 = 0.8193**, numerically coinciding with configurations C and D; cumulative **ΔwF1 = +0.0655** from L0 to L7 — i.e. the entire Experiment-1 gain is reproduced by preprocessing at fixed initialization. **Monotonic in each of the five folds.**
- Per-level increments: L1 +0.0071, L2 +0.0068, L3 +0.0082, L4 **+0.0143**, L5 **+0.0125**, L6 +0.0101, L7 +0.0065; each exceeds its 2·σ_fold band. Photometric stages (flat-field, CLAHE) lead with **41 %** of the total gain.
- **Two caveats that must travel with the ranking:** (i) the stage order is fixed, so each increment is measured with all preceding stages already applied — the values are *cumulative-order-dependent contributions*, not context-free stage effects; (ii) **Stage 3 (FOV mask) is not isolated** — level L3 adds Stages 2 and 3 jointly, so rank 4 belongs to the pair, not to a stage. State (ii) as an explicit limitation.
- The grouping (photometric ≫ geometric) is what the data resolve; adjacent ranks sit within noise. Do not present a strict 1-to-7 ordering as established.
- **NC-17** becomes live: the stage set is not asserted to be universally optimal.

**Unresolved threads / flags (carry forward):**
- **CFC-2.8 decomposition — due at §4.3.1.** Twice promised; the chapter's credibility on H-1 rests on it.
- **§4.4 (H-3)** owes the direct measurement of distributional convergence anticipated by the §4.2.2 mechanism reading.
- **Withdrawn:** any formulation that the pipeline degrades calibration, or that recalibration is needed before deployment — removed from Chapter 4 and must not reappear in Chapter 5, Chapter 7 or `defense/`.
- **Chapter-wide conventions now fixed:** calibrated significance language; no per-class AUC; arms named "baseline configuration" / "integrated configuration" (never by author name).
- **Live for later sections:** NC-14 (§4.6), NC-16 (§4.8), NC-17 (§4.3), DGL-1/DGL-2 (all), SB-2.2 (§4.6, §4.9).
- **Provenance (NEW-1)** remains a project-management item outside the prose.
