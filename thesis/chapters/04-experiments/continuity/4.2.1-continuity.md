# CONTINUITY NOTE — §4.2.1 Restored 2×2 Factorial Design (Configurations A–D)

**Version:** 7.0.0 · PART 2 of the §4.2.1 session · **Opens §4.2 (Experiment 1, H-1). Design only — no metric reported.**

---

**Key concepts established:**
- **The 2×2 factorial**: factor 1 = preprocessing arm (baseline 3-channel, OD-3 stretch-resize + ImageNet normalize | integrated 4-channel, full 8-stage pipeline); factor 2 = backbone (ResNet-50 | EfficientNet-B3). Configurations **A, B, C, D** rendered inline as **Table 4.3** with their initialization source and paradigm tag.
- **"Restored" factorial**: the CNN-native in-domain initialization is what restores full crossing — both backbones appear in both arms, so the architecture factor is orthogonal to the preprocessing factor by construction. (An earlier transformer-based foundation-model formulation would have moved architecture and initialization together.)
- **Paradigmatic reading (PC-0 / SB-1.12 / CFC-2.9 / SIR-9)**: A/C = **P1** as an *internal operational construct defined by OD-3*, explicitly **not** Gulshan's system; B/D = **P2**, preprocessing as an integral model component. Gulshan designation = observable methodological practice ("defer preprocessing details to the supplement"), never an attributed theoretical position.
- **Two design commitments**: (i) *matching* — corpus, folds, optimizer, schedule, focal loss, stopping rule, resolution, metric suite held identical across all four cells; (ii) *pre-commitment* — EH-3/EH-4 stated before the data.
- **EH-3 stated conjunctively** with its rationale: ΔwF1 ≥ 5 pp **and** ΔAUC ≥ 0.02 **and** no κ degradation. The argument for conjunctivity: a single-metric criterion is satisfiable by trading discrimination against agreement, which the ordinal five-grade scale makes easy to do inadvertently.
- **EH-4 made internal to the experiment**: B−A and D−C are two instances of the same manipulation differing only in backbone → replication is *measured*, not assumed.
- **The composite independent variable (CFC-2.8), stated openly and routed forward**: baseline arm ⟹ ImageNet, integrated arm ⟹ gate-selected in-domain init; the factor is *(preprocessing, initialization)*; Experiment 1 licenses a claim about the **integrated configuration as a unitary system** only. **The decomposition is assigned to §4.3** (cumulative ablation, same corpus, same partitioning, single initialization held constant across levels).

**Terms introduced:** factorial structure; preprocessing arm; baseline configuration; integrated configuration; composite independent variable; paradigm P1 / P2; operational construct (OD-3); gate-selected in-domain initialization; pre-committed acceptance criterion; conjunctive dominance criterion.

**Argument thread:** §4.1 fixed the substrate; §4.2.1 fixes the design that turns it into a test of H-1. The active line is that Experiment 1 measures a *paradigm contrast under matched conditions* with an acceptance rule fixed in advance — and that its one asymmetry (initialization) is declared rather than concealed, with the identification it forecloses explicitly delegated to §4.3.

**Final topic:** The evaluation protocol (EyePACS n = 35 126, 5-fold patient-level grade-stratified CV, four primary metrics in governance priority order) and the DGL-1/DGL-2 bounding, closing with the hand-off to §4.2.2 and §4.2.3.

**Setup for next section (§4.2.2 Training Dynamics and Convergence Analysis):**
- Open from the fixed design; §4.2.2 reports **how the four configurations behaved during fitting**, not their end-state diagnostic quality (which is §4.2.3).
- Material to use: per-fold best epochs, train/val loss at best epoch and the **loss gap**, and the **calibration** quantities (ECE, Brier). Render the convergence table and TAB-4.3 (calibration) inline.
- The analytical spine available to §4.2.2 is the **regularization reading**: the integrated arms converge roughly twice as early with a loss gap ~2.5× smaller, while carrying a *higher* train loss at comparable val loss — i.e. the model fits the training set less closely and generalizes no worse. Frame this as narrowing of the hypothesis space by illumination normalization plus the FOV-mask channel, and note the small within-arm spread of best epochs (±1–1.5) as evidence the regime is reproducible across folds rather than a seed artifact.
- **Calibration is an improvement, not a drawback.** ECE falls by ≈1.7× in the integrated arms and Brier falls on both backbones. Any inherited formulation asserting that the pipeline *degrades* calibration or that recalibration is required before deployment is **withdrawn** and must not appear.
- Keep EH-3 unspoken until §4.2.3 — §4.2.2 supplies mechanism, not verdict.

**Unresolved threads / flags (carry forward):**
- **CFC-2.8 → §4.3.1 must actually deliver the decomposition** promised here (L0 = baseline level, L7 = full pipeline, single initialization). This forward promise is now on the record and §4.3.1 is obliged to redeem it.
- **NC-6 / SIR-7** recur wherever backbones are compared (§4.2.3, §5.3.2).
- **DGL-1 / DGL-2** recur at every result section.
- **NC-14** (Grad-CAM ≠ clinical localization) becomes live at §4.6.
- **NC-16** (device results ≠ certification) becomes live at §4.8.
- **Provenance (NEW-1):** the run's raw artifacts are not yet published to the experiment output tree, so Chapter-4 numbers are not yet traceable to a primary output file. This is a project-management item, **not** dissertation content — it must not appear in prose (rule 16), but it must be closed before the defense.
